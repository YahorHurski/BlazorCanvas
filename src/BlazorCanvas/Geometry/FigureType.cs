namespace BlazorCanvas.Geometry;

/// <summary>
/// The four figure types the canvas supports (D-12). There are only ever four.
/// </summary>
public enum FigureType
{
    Line,
    Rectangle,
    Circle,
    Triangle
}
