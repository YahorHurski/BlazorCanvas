---
phase: BC-15-draw-preview-render-persist-a-star
plan: 04
type: execute
wave: 3
depends_on:
  - 15-03
files_modified:
  - src/BlazorCanvas/Components/Pages/Home.razor
  - tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
autonomous: true
gap_closure: true
gap_ids:
  - G-15-1
requirements:
  - FIG-06
must_haves:
  truths:
    - "G-15-1: A live preview is visible for every armable drawing figure, including star5."
    - "G-15-1: Home.razor passes FigureShape the active preview session's type value, not a literal member name."
    - "G-15-1: FigureShape receives a PreviewType value accepted by ShapeRegistry.Contains, so PreviewPlacement geometry is rendered."
    - "G-15-1: Star commit and immediate persistence behavior remain unchanged."
  artifacts:
    - path: "src/BlazorCanvas/Components/Pages/Home.razor"
      provides: "Correct PreviewType expression binding from DrawingPreviewSession into FigureShape."
    - path: "tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs"
      provides: "Source-contract coverage that rejects literal string binding and confirms preview FigureShape wiring."
  key_links:
    - from: "src/BlazorCanvas/Components/Pages/Home.razor"
      to: "src/BlazorCanvas/Components/Canvas/FigureShape.razor"
      via: "PreviewPlacement plus PreviewType parameters on the active preview FigureShape."
      pattern: "PreviewType expression binding must evaluate preview.Type at runtime."
    - from: "tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs"
      to: "src/BlazorCanvas/Components/Pages/Home.razor"
      via: "Source contract fails when a string parameter is passed as static text instead of a Razor expression."
---

<objective>
Close UAT gap G-15-1 by binding the active preview FigureShape to the actual DrawingPreviewSession type value and pinning that contract in tests.

Purpose: FIG-06 requires a visible live preview. The preview architecture from 15-03 is present, but the string parameter is currently passed as static text, causing FigureShape's registry guard to reject every preview type and render no SVG child.
Output: Home.razor uses expression binding for PreviewType, HomePreviewSourceTests fails on the previous static-text binding, and focused/full test commands account for the running-app executable lock.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-UAT.md
@.planning/debug/phase-15-preview-missing.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-03-PLAN.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-03-SUMMARY.md
@src/BlazorCanvas/Components/Pages/Home.razor
@src/BlazorCanvas/Components/Canvas/FigureShape.razor
@tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin preview type expression binding for G-15-1</name>
  <files>tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs</files>
  <read_first>
    - .planning/phases/BC-15-draw-preview-render-persist-a-star/15-UAT.md
    - .planning/debug/phase-15-preview-missing.md
    - src/BlazorCanvas/Components/Pages/Home.razor
    - src/BlazorCanvas/Components/Canvas/FigureShape.razor
    - tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
  </read_first>
  <behavior>
    - The source test fails if the active preview FigureShape passes PreviewType as static markup text instead of evaluating DrawingPreviewSession.Type.
    - The source test still proves the active preview FigureShape includes PreviewPlacement from preview.Placement and Selectable set false.
    - The source test keeps the ordering guard that preview rendering occurs after SelectionTrace.
    - The test explains G-15-1 by checking for the binding shape that lets FigureShape's Registry.Contains guard accept the runtime type.
  </behavior>
  <action>Update HomePreviewSourceTests for G-15-1. Replace the current assertion that accepts static text for the string parameter with assertions that require Razor expression binding for the preview type value. Add a negative assertion for the previous static-text form, but keep it in test code only rather than in comments or helper names. Preserve the existing PreviewPlacement, Selectable, SelectionTrace ordering, and no-notifier assertions. Do not add browser automation, bUnit, package dependencies, or tests for commit/persistence paths; those already passed and are outside this gap.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --no-build --filter "FullyQualifiedName~HomePreviewSourceTests"</automated>
    <automated>powershell -NoProfile -Command "Get-Process BlazorCanvas -ErrorAction SilentlyContinue | Stop-Process -Force; dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter 'FullyQualifiedName~HomePreviewSourceTests'"</automated>
  </verify>
  <done>HomePreviewSourceTests would have failed on the diagnosed G-15-1 binding and passes after the source uses expression binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind PreviewType to the active session value for G-15-1</name>
  <files>src/BlazorCanvas/Components/Pages/Home.razor, tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/Home.razor
    - src/BlazorCanvas/Components/Canvas/FigureShape.razor
    - tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
  </read_first>
  <behavior>
    - During an active preview, Home.razor passes FigureShape PreviewPlacement from preview.Placement.
    - During an active preview, Home.razor passes FigureShape PreviewType as the evaluated preview.Type string.
    - FigureShape's preview branch can satisfy Registry.Contains(PreviewType), assign Geometry from PreviewPlacement, and emit the preview SVG.
    - Commit, persistence, toolbar arming, pointer movement, stale preview cleanup, and CanvasSyncNotifier behavior are not changed.
  </behavior>
  <action>Fix G-15-1 in Home.razor by changing only the active preview FigureShape parameter binding needed for the string PreviewType parameter. Use Razor expression syntax so the component receives the current DrawingPreviewSession.Type value at runtime. Leave PreviewPlacement, Selectable, the existing preview IsActive guard, commit flow, persistence flow, JS cleanup behavior, and FigureShape rendering logic unchanged unless a compile error proves a tiny test alignment is necessary. Do not redesign preview architecture, change DrawingPreviewSession, change FigureShape's Registry.Contains guard, alter star commit/persistence behavior, or address unrelated pointercancel debt.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Get-Process BlazorCanvas -ErrorAction SilentlyContinue | Stop-Process -Force; dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter 'FullyQualifiedName~HomePreviewSourceTests|FullyQualifiedName~DrawingPreviewSessionTests|FullyQualifiedName~CanvasInteractionCoordinatorTests|FullyQualifiedName~V11RenderContractTests'"</automated>
    <automated>powershell -NoProfile -Command "Get-Process BlazorCanvas -ErrorAction SilentlyContinue | Stop-Process -Force; dotnet test BlazorCanvas.sln --no-restore"</automated>
  </verify>
  <done>PreviewType is bound to the actual session value, focused preview tests pass, and the full solution test suite passes after clearing any running BlazorCanvas.exe lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-Blazor-circuit | Pointer coordinates and active tool state drive preview placement. |
| Blazor-circuit-to-FigureShape | Home.razor passes in-memory preview data into the renderer component. |
| local-preview-to-persistence | Preview state must remain visual-only until the existing commit flow completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-G1-01 | Tampering | Home.razor preview binding | medium | mitigate | Bind PreviewType from the active session value so FigureShape validates against the real registry type instead of static text. |
| T-15-G1-02 | Denial of Service | FigureShape preview branch | low | mitigate | Keep the existing Registry.Contains guard and only feed it a valid runtime type; invalid preview types continue to render nothing rather than tearing down the circuit. |
| T-15-G1-03 | Information Disclosure | CanvasSyncNotifier boundary | medium | mitigate | Preserve the existing no-notifier source assertion so local preview state is not broadcast. |
| T-15-G1-SC | Tampering | package installs | low | accept | No package-manager install is planned; tests reuse the existing xUnit project. |
</threat_model>

<verification>
Use no-build focused tests only for quick evidence while the local UAT app is running. Before any build-backed test, stop the running BlazorCanvas process because it can lock src/BlazorCanvas/bin/Debug/net10.0/BlazorCanvas.exe. Then run the focused preview/source tests and the full solution test suite.
</verification>

<success_criteria>
- G-15-1 is closed: live previews are visible because FigureShape receives a registry-known PreviewType value.
- The bad static string binding is rejected by HomePreviewSourceTests.
- No commit, persistence, toolbar, sync, database, or pointercancel behavior is changed.
- Focused preview/source tests and full solution tests pass after clearing any running executable lock.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Phase 15 live preview works while drawing a star end-to-end | 15-04 | COVERED | G-15-1 specifically broke preview visibility after 15-03.
REQ | FIG-06 | Live star preview follows cursor and matches committed shape | 15-04 | COVERED | Binding fix allows FigureShape to render the active registry-derived placement.
REQ | FIG-05 | User can arm and draw star | 15-04 | COVERED | Existing passed behavior is preserved; no toolbar or commit changes planned.
REQ | FIG-07 | Edge clamp and degenerate/sliver draw behavior | 15-04 | COVERED | Existing DrawingPreviewSession and commit path remain unchanged.
REQ | RENDER-02 | Persisted star renders after commit/reload | 15-04 | COVERED | Existing FigureShape persisted rendering remains unchanged.
REQ | DATA-05 | Star persists immediately with no Save button | 15-04 | COVERED | Existing repository commit path remains unchanged.
RESEARCH | - | Diagnosed root cause: static string parameter binding in Home.razor | 15-04 | COVERED | Evidence from phase-15-preview-missing.md is implemented directly.
CONTEXT | G-15-1 | User reports no preview for any figure; star button and commit/persistence work | 15-04 | COVERED | Scope is limited to preview binding and source test guard.
</source_audit>

<output>
Create `.planning/phases/BC-15-draw-preview-render-persist-a-star/15-04-SUMMARY.md` when done.
</output>
