---
phase: 09-schema-entity-data-preserving-migration
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/BlazorCanvas/Migrations/
  - src/BlazorCanvas/Migrations/CanvasDbContextModelSnapshot.cs
  - scripts/verify-live-schema.sh
autonomous: true
requirements: [STOR-01, MIG-01]
user_setup:
  - service: postgres (Docker Compose)
    why: "The migration must be applied to a live Postgres to prove STOR-01 success criterion 1 (live schema, not a green build)."
    dashboard_config:
      - task: "Ensure the Compose database is running before this plan executes"
        location: "repo root: docker compose up -d --wait (host port 5433 -> container 5432, per the D-27 deviation)"

must_haves:
  truths:
    - "A new EF Core migration (AnchorGeometryRewrite) captures the full schema delta: add x,y,geometry,z; change id from integer identity to uuid default gen_random_uuid(); drop x1,y1,x2,y2; drop the three geometry CHECKs; keep figures_type_is_known; drop ix_figures_user_id and add ix_figures_user_id_z on (user_id, z); update the table comment (D-42, D-59)."
    - "A hand-written migrationBuilder.Sql backfill, ordered inside Up() so it runs AFTER the new columns are added (nullable) and BEFORE they are set NOT NULL and BEFORE the old id/columns are dropped, transforms every row x1,y1,x2,y2 -> x,y,geometry per type and sets z = the old integer id (D-59)."
    - "Per-type backfill: rectangle/triangle -> x=x1, y=y1, geometry={w:x2-x1,h:y2-y1}; circle -> x=x1+(x2-x1)/2, y=y1+(y2-y1)/2, geometry={r:(x2-x1)/2}; line -> x=x1, y=y1, geometry={dx:x2-x1,dy:y2-y1} (dy signed, endpoint pair never swapped)."
    - "The whole migration is one transaction: any error rolls back the schema change and the backfill together, and the retained figures_type_is_known CHECK turns a bad transform into an immediate rollback rather than silent corruption (D-46, D-09)."
    - "Applied to the live Compose Postgres, the figures table matches THE SCHEMA: uuid id with gen_random_uuid() default, integer x and y, jsonb geometry, numeric z, text type with only the figures_type_is_known CHECK, composite index ix_figures_user_id_z, NO created_at column, NO CHECK on geometry — confirmed by querying the live catalog, not the EF model (STOR-01, D-59, D-12)."
    - "Running the migration against a figures table with zero rows applies the schema change and leaves zero figures with no error; a single-row table migrates that one figure faithfully (MIG-01 empty/single input)."
    - "Each migrated row gets a distinct z (z = old id, which was unique), so no two figures collide in layer order, the migration merges no rows, and the row count is identical before and after (MIG-01 adjacency)."
    - "Post-migration load order (ORDER BY z, id) reproduces the pre-migration paint order exactly, because z is assigned ascending by the old integer id with no ties (MIG-01 ordering)."
  artifacts:
    - "src/BlazorCanvas/Migrations/<timestamp>_AnchorGeometryRewrite.cs (+ .Designer.cs) — the schema delta + hand-written backfill."
    - "Updated src/BlazorCanvas/Migrations/CanvasDbContextModelSnapshot.cs."
    - "scripts/verify-live-schema.sh — queries the live catalog and asserts the D-59 shape."
  key_links:
    - "The backfill SQL's per-type formulas MUST match GeometryCodec (09-01) exactly — they are the same transform in two languages; a divergence is a silent-corruption bug the round-trip test (09-06) is designed to catch."
    - "z = old id is what makes FigureStore's ORDER BY z, id reproduce the old ORDER BY id paint order."
  prohibitions:
    - "The migration must never silently alter or drop a figure — every pre-existing figure keeps its exact position and on-screen appearance; the row count is identical before and after."
    - "The backfill must never normalise a line by sorting x/y axes independently — a down-right diagonal must not flip to the opposite diagonal (swap the whole endpoint pair only, D-41)."
    - "Do not drop the figures_type_is_known CHECK during the migration (D-46/D-59); do not add any CHECK on geometry (D-09)."
---

<objective>
Produce the EF Core migration that moves the live database from the four-integer bounding box to the D-59 anchor+geometry schema AND preserves every existing figure. EF auto-generates the schema delta; the row transform is a hand-written `migrationBuilder.Sql` backfill placed at the correct point in `Up()` (after new columns exist, before they go NOT NULL and before the old columns/id are dropped), assigning `z` from the old integer id so layer order is preserved 1:1. Then apply the migration to the live Compose Postgres and assert the live schema shape directly from the catalog — STOR-01 success criterion 1 is "confirmed against a live database, not merely a green build."

Purpose: MIG-01 (data-preserving migration) and the schema half of STOR-01, landed and proven on a real database.
Output: the AnchorGeometryRewrite migration with its backfill; an applied live schema; a live-schema verification script.

NOTE ON BUILD STATE: this plan builds and runs the app project only (`src/BlazorCanvas`). The test project is still uncompilable from 09-03 until 09-05; do not gate this plan on `dotnet test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md
@src/BlazorCanvas/Data/CanvasDbContext.cs
@src/BlazorCanvas/Data/Figure.cs
@src/BlazorCanvas/Migrations/20260714212457_InitialSchema.cs
@src/BlazorCanvas/Program.cs
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate the AnchorGeometryRewrite migration (schema delta)</name>
  <files>src/BlazorCanvas/Migrations/, src/BlazorCanvas/Migrations/CanvasDbContextModelSnapshot.cs</files>
  <read_first>
    - src/BlazorCanvas/Migrations/20260714212457_InitialSchema.cs (the migration this one follows — the old columns/CHECKs/index it must transform)
    - src/BlazorCanvas/Data/CanvasDbContext.cs (the new model the delta is generated against)
    - docs/DECISIONS.md §THE SCHEMA + D-59, D-42
  </read_first>
  <action>
    Run `dotnet ef migrations add AnchorGeometryRewrite --project src/BlazorCanvas/BlazorCanvas.csproj` to generate the migration and the updated model snapshot from the 09-03 model. Inspect the generated Up(): it should add x, y, geometry, z; drop x1, y1, x2, y2; drop circle_is_a_circle, box_is_a_box, line_is_a_line (keeping figures_type_is_known); replace ix_figures_user_id with ix_figures_user_id_z on (user_id, z); update the comment; and change id from integer identity to uuid with a gen_random_uuid() default. Because the id type change is destructive, verify EF's generated ordering does not drop the old integer id BEFORE the backfill can read it — if it does, reorder in Task 2. Do NOT yet worry about data; this task is the schema scaffold. Keep the migration name and file under src/BlazorCanvas/Migrations. If EF cannot express the uuid default or the id swap cleanly, note exactly what it emitted so Task 2 can hand-correct it via raw SQL.
  </action>
  <acceptance_criteria>
    - A new migration class AnchorGeometryRewrite exists under src/BlazorCanvas/Migrations with an Up() and Down(); the model snapshot is updated to the anchor+geometry model.
    - Up() contains operations to add x/y/geometry/z, drop x1..y2, drop the three geometry CHECKs, keep figures_type_is_known, and create ix_figures_user_id_z.
    - `dotnet build src/BlazorCanvas/BlazorCanvas.csproj` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/BlazorCanvas/BlazorCanvas.csproj</automated>
  </verify>
  <done>The schema-delta migration is generated and compiles; the model snapshot reflects the new schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hand-write and correctly order the data-preserving backfill inside Up()</name>
  <files>src/BlazorCanvas/Migrations/</files>
  <read_first>
    - The AnchorGeometryRewrite Up()/Down() generated in Task 1
    - .planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md (per-type transform, z-from-old-id, backfill = migrationBuilder.Sql)
    - src/BlazorCanvas/Geometry/GeometryCodec.cs (the C# mirror of the same per-type maths — keep them identical)
    - docs/DECISIONS.md D-59 (resolutions), D-46 (keep type CHECK through backfill), D-09
  </read_first>
  <action>
    Restructure Up() so the sequence is: (1) add x, y, geometry, z as NULLABLE columns and add the new uuid id column with a gen_random_uuid() default while the old integer id still exists; (2) a single migrationBuilder.Sql that UPDATEs every row with the per-type transform and z — one UPDATE per type keyed on the type literal, computing rectangle/triangle {w,h}, circle centre-anchor + {r} using (x2-x1)/2 (exact because the old even-diameter CHECK guaranteed it), and line {dx,dy} by copying x2-x1 and y2-y1 with sign (NEVER sorting axes), and setting z = the old integer id cast to numeric; (3) ALTER x, y, geometry, z to NOT NULL; (4) drop the three geometry CHECKs (if Task 1 didn't) but keep figures_type_is_known; (5) drop x1,y1,x2,y2; (6) swap the primary key to the new uuid id (drop the old PK, drop the old integer id column, promote the uuid column to the primary key) — done only AFTER step 2 has captured z from the old id; (7) drop ix_figures_user_id and create ix_figures_user_id_z; (8) set the new table comment. Keep everything inside the one migration so it is atomic. Write a Down() that either reverses to the old schema or, if a faithful reverse is impossible, throws a clear "irreversible data migration" so nobody assumes a silent rollback path. Emit the backfill SQL with jsonb_build_object (or equivalent) so geometry members are integers. Add a header comment on the Sql block naming the per-type formulas and that they mirror GeometryCodec.
  </action>
  <acceptance_criteria>
    - Up() adds the new columns nullable, runs the migrationBuilder.Sql backfill, sets them NOT NULL, drops the old columns/CHECKs, and swaps id to uuid — in that order, with the backfill reading the old id for z BEFORE the old id is dropped.
    - The backfill has one UPDATE per type; circle uses (x2-x1)/2 for both r and the centre offset; line copies dx=x2-x1 and dy=y2-y1 with sign and does not reorder points; z = old id.
    - figures_type_is_known survives the migration; no CHECK on geometry is added.
    - Down() is defined and either reverses faithfully or fails loudly (no silent no-op).
    - `dotnet build src/BlazorCanvas/BlazorCanvas.csproj` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/BlazorCanvas/BlazorCanvas.csproj</automated>
  </verify>
  <done>The migration atomically transforms every row to anchor+geometry with z=old id, keeps the type CHECK, and swaps id to uuid — the data-preserving heart of MIG-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply the migration to live Postgres and assert the D-59 schema from the catalog</name>
  <files>scripts/verify-live-schema.sh</files>
  <read_first>
    - src/BlazorCanvas/Program.cs (startup applies migrations via Database.Migrate) 
    - docker-compose.yml (the Compose Postgres, host 5433)
    - tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs (the shape assertions this script mirrors at the CLI level — reference for what "the D-59 shape" means)
    - docs/DECISIONS.md §THE SCHEMA (the exact live shape to assert)
  </read_first>
  <action>
    Ensure the Compose database is up (docker compose up -d --wait). Apply the migration to the live database — either `dotnet ef database update --project src/BlazorCanvas/BlazorCanvas.csproj` or by booting the app once (Program.cs migrates on startup). Then write scripts/verify-live-schema.sh that connects to the live database (Host localhost, Port 5433, Database canvas, per the D-27 deviation) and asserts, by querying information_schema/pg_catalog: figures has columns exactly id,user_id,type,x,y,geometry,z; id is uuid with a gen_random_uuid() default; x and y are integer; geometry is jsonb; z is numeric; the only figures CHECK constraint is figures_type_is_known; index ix_figures_user_id_z exists on (user_id, z); there is NO created_at column and NO CHECK whose definition references geometry. The script must exit non-zero if any assertion fails. Run it and confirm it passes against the migrated live database.
  </action>
  <acceptance_criteria>
    - The migration is applied to the live Compose database (the __EFMigrationsHistory table lists AnchorGeometryRewrite).
    - `bash scripts/verify-live-schema.sh` exits 0 and prints the confirmed shape: uuid id (gen_random_uuid()), integer x/y, jsonb geometry, numeric z, single figures_type_is_known CHECK, ix_figures_user_id_z, no created_at, no geometry CHECK.
    - Re-running the script is idempotent (asserts state, does not mutate).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-live-schema.sh</automated>
  </verify>
  <done>The live figures table matches THE SCHEMA (D-59), proven against the running Postgres catalog — STOR-01 success criterion 1.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces:
- `AnchorGeometryRewrite` EF migration class (+ Designer) under `src/BlazorCanvas/Migrations/`, including the hand-written `migrationBuilder.Sql` backfill.
- Updated `CanvasDbContextModelSnapshot.cs`.
- `scripts/verify-live-schema.sh` (live-catalog schema assertion).
- The migrated live Compose database (schema state).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration transaction -> existing rows | The backfill is a bulk transform over every existing figure — the single highest-risk operation in the milestone. Static SQL, no user input. |
| server (sole writer) -> Postgres | D-09: geometry well-formedness is a code/migration guarantee, not a DB CHECK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Tampering | Backfill row transform | high | mitigate | A wrong per-type formula silently corrupts or loses figures. Mitigated by mirroring the tested GeometryCodec formulas, one atomic transaction (rolls back on any error), keeping the figures_type_is_known CHECK as a rollback tripwire through the bulk update, and the MIG-02 round-trip proof in 09-06. |
| T-09-09 | Tampering | id-swap ordering | high | mitigate | Dropping the old integer id before z is computed would lose paint order. Mitigated by ordering the backfill (z = old id) strictly before the id column is dropped, asserted in Task 2 acceptance and re-proven by the round-trip ordering assertion (09-06). |
| T-09-10 | Denial of Service | non-atomic partial migration | medium | mitigate | A half-applied migration leaves a broken schema. Mitigated by keeping schema delta + backfill in one migration/transaction and a Down() that fails loudly rather than silently no-op'ing; Program.cs already fails boot loudly on a half-built schema. |
| T-09-11 | Information Disclosure | no DB CHECK on geometry | low | accept | Deliberate (D-09); server is sole writer. Recorded to keep the boundary conscious. |
| T-09-SC | Tampering | Package installs | low | accept | No new packages introduced. |
</threat_model>

<verification>
- `dotnet build src/BlazorCanvas/BlazorCanvas.csproj` is clean.
- The migration applies cleanly to the live Compose database and `scripts/verify-live-schema.sh` exits 0 with the confirmed D-59 shape.
- The backfill per-type formulas match GeometryCodec (manual cross-read, and definitively re-proven by 09-06).
- KNOWN/INTENDED: `dotnet test` is not run here — the test project is restored to compilable/green in 09-05.
</verification>

<success_criteria>
The AnchorGeometryRewrite migration transforms every existing figure to anchor+geometry (z = old id, type CHECK retained, id swapped to uuid) atomically, and the live Postgres schema — asserted from the catalog — matches THE SCHEMA exactly. MIG-01 and the schema half of STOR-01 are landed and proven on a real database.
</success_criteria>

<output>
Create `.planning/phases/BC-09-schema-entity-data-preserving-migration/09-04-SUMMARY.md` when done.
</output>
