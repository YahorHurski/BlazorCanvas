---
phase: 09-schema-entity-data-preserving-migration
plan: 06
type: execute
wave: 5
depends_on: ["09-05", "09-04", "09-02"]
files_modified:
  - tests/BlazorCanvas.Tests/Migrations/MigrationRoundTripTests.cs
  - tests/BlazorCanvas.Tests/Migrations/FixtureManifest.cs
autonomous: true
requirements: [MIG-02]
user_setup:
  - service: postgres (Docker Compose)
    why: "The round-trip test provisions a disposable database on the live Compose Postgres server (host 5433) to seed the fixture and run the real migration."
    dashboard_config:
      - task: "docker compose up -d --wait before running the suite"
        location: "repo root"

must_haves:
  truths:
    - "An automated test seeds a disposable Postgres database from the immutable v1.1-pre-rewrite.sql, runs the REAL AnchorGeometryRewrite migration (not a re-implementation), and asserts every migrated row's anchor+geometry+z equals the re-derived MANIFEST for all four types (MIG-02, D-49, D-59)."
    - "The migrated row count equals the fixture row count — no figure is lost, merged, or duplicated (MIG-02 adjacency)."
    - "The round-trip test asserts a down-right diagonal line (dx>0, dy>0) migrates to {dx>0, dy>0} without swapping the endpoint pair — the D-41 flip landmine (MIG-02 adjacency)."
    - "The test guards against a vacuously-passing empty fixture: it asserts the fixture yields at least one figure of each of the four types before comparing to the MANIFEST (MIG-02 empty input)."
    - "The test asserts migrated figures load ORDER BY z, id in the exact sequence the MANIFEST pins, matching the pre-migration id order (MIG-02 ordering)."
    - "The test asserts circle migration is exact with no rounding: r=(x2-x1)/2 is an integer (the old even-diameter CHECK guaranteed it), cx=x1+r and cy=y1+r are exact, and no figure's geometry acquires a fractional or rounded value (MIG-02 precision)."
    - statement: "The test preserves figures at coordinate extremes present in the immutable fixture (e.g. an anchor at 0, or a minimal even-diameter circle) with exact anchor+geometry — over whatever boundary rows the fixture actually contains."
      verification: backstop
    - "Migrated ids are uuid values (the round-trip test reads them as Guids), confirming the id swap ran (D-59)."
  artifacts:
    - "tests/BlazorCanvas.Tests/Migrations/MigrationRoundTripTests.cs — the MIG-02 proof."
    - "tests/BlazorCanvas.Tests/Migrations/FixtureManifest.cs — parses/encodes the MANIFEST expected values for assertion (or an equivalent loader)."
  key_links:
    - "The test runs the actual AnchorGeometryRewrite.Up() (schema delta + migrationBuilder.Sql backfill) via EF against the seeded old-schema fixture — this is what makes it a proof of the real migration, not a parallel maths check."
    - "MANIFEST (09-02) supplies the expected values; the fixture (09-02) supplies the input; the migration (09-04) is the transform under test."
  prohibitions:
    - "The migration must never silently alter or drop a figure — the test fails if any row's migrated anchor/geometry/z diverges from the MANIFEST or if the row count changes."
    - "The test must never edit or regenerate the immutable v1.1-pre-rewrite.sql fixture (D-49); it seeds a throwaway database from it read-only."
    - "The test must exercise the real EF migration; it must never re-implement the transform in C# and assert against its own copy (that would prove nothing)."
---

<objective>
Prove the milestone's headline guarantee: running the real EF migration against the immutable v1.1 fixture preserves every figure exactly. The test seeds a disposable Postgres database from `v1.1-pre-rewrite.sql` (old four-integer schema + real rows), applies the actual `AnchorGeometryRewrite` migration (schema delta + hand-written backfill), then asserts every migrated row's anchor + geometry + z matches the re-derived `MANIFEST` for all four shape types — with explicit checks on the circle centre-anchor `{r}` (no rounding), the down-right diagonal line (no flip), row-count preservation, and z-ordering.

Purpose: MIG-02 — the automated round-trip test that turns "every existing figure survives" from a claim into a proof.
Output: `MigrationRoundTripTests` (+ a manifest loader), green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md
@tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql
@tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md
@tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
@src/BlazorCanvas/Data/CanvasDbContext.cs
@src/BlazorCanvas/Geometry/GeometryCodec.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Round-trip harness — disposable DB, seed fixture, run the real migration</name>
  <files>tests/BlazorCanvas.Tests/Migrations/MigrationRoundTripTests.cs, tests/BlazorCanvas.Tests/Migrations/FixtureManifest.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql (the old-schema seed)
    - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md (expected values to load)
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs (connection-string convention, host 5433, D-27 deviation)
    - src/BlazorCanvas/Data/CanvasDbContext.cs (the migration is applied via this context)
    - src/BlazorCanvas/Migrations/ (the AnchorGeometryRewrite migration under test)
  </read_first>
  <action>
    Create a test that, per run, provisions a throwaway database on the same Compose Postgres server (a uniquely-named database, e.g. canvas_migtest_&lt;guid&gt;, created and dropped by the test so it never touches the shared canvas DB). Seed it by executing the fixture .sql read-only (which creates the OLD schema and inserts the rows), then bring the database to the pre-rewrite EF migration state so ONLY AnchorGeometryRewrite applies next: stamp the __EFMigrationsHistory with the InitialSchema migration id (the fixture already provides that schema), then call context.Database.MigrateAsync() so EF runs AnchorGeometryRewrite — the real schema delta plus the migrationBuilder.Sql backfill — against the seeded rows. Confirm the migration applied (history now lists AnchorGeometryRewrite). Provide FixtureManifest.cs to parse the MANIFEST (or an equivalent typed loader) into expected (old id, type, anchor, geometry, z) records for Task 2. Ensure the disposable database is dropped in a finally/dispose path regardless of assertion outcome. The test must run the ACTUAL migration; do not re-implement the transform.
  </action>
  <acceptance_criteria>
    - The test creates a uniquely-named disposable database, seeds it from the immutable fixture without modifying the fixture file, and applies AnchorGeometryRewrite via EF MigrateAsync.
    - __EFMigrationsHistory in the disposable DB lists AnchorGeometryRewrite after the run.
    - FixtureManifest loads the MANIFEST's expected values for all four types.
    - The disposable database is dropped after the test (no leftover canvas_migtest_* databases).
    - `dotnet build BlazorCanvas.sln` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build BlazorCanvas.sln</automated>
  </verify>
  <done>The harness seeds the immutable fixture into a throwaway DB and runs the real migration, with the manifest loaded and the DB cleaned up.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Assert every migrated figure matches the MANIFEST (the MIG-02 proof)</name>
  <files>tests/BlazorCanvas.Tests/Migrations/MigrationRoundTripTests.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md (expected anchor/geometry/z per row)
    - .planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md (per-type shapes, line-flip landmine, circle centre-anchor exactness)
    - src/BlazorCanvas/Geometry/GeometryCodec.cs (independent decode for cross-checking appearance if desired)
  </read_first>
  <action>
    After the migration runs, read the migrated figures ordered by z, id and assert against the MANIFEST: (1) the migrated row count equals the fixture row count (no figure lost/merged/duplicated); (2) the fixture yields at least one figure of EACH of the four types before comparing — fail loudly on a vacuous/empty fixture; (3) for every row, the migrated anchor (x,y), geometry JSON, and z equal the MANIFEST's expected values; (4) the circle rows have integer r=(x2-x1)/2 with the centre anchor (x1+r, y1+r) and NO fractional value anywhere in geometry (precision); (5) the down-right diagonal line migrates to {dx>0, dy>0} with the endpoint pair intact (no flip to the opposite diagonal); (6) the load order (z, id) matches the MANIFEST's pinned sequence, reproducing the pre-migration id order; (7) ids read back as uuid/Guid values. Where the fixture happens to contain coordinate-extreme rows (anchor at 0, minimal even-diameter circle), assert those preserve exactly too (best-effort over whatever boundary rows exist — the backstop truth). Give each assertion a message naming the offending old id so a failure is diagnosable.
  </action>
  <acceptance_criteria>
    - The test asserts row-count preservation, all-four-types-present, and per-row anchor+geometry+z equality against the MANIFEST.
    - Circle assertions confirm exact integer r and centre anchor with no fractional geometry; line assertions confirm the down-right diagonal did not flip.
    - Load-order (z, id) matches the MANIFEST sequence; migrated ids are uuid/Guid.
    - `dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --filter FullyQualifiedName~MigrationRoundTrip` passes; the full `dotnet test` suite is green.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --filter FullyQualifiedName~MigrationRoundTrip</automated>
  </verify>
  <done>The round-trip test proves every fixture figure survives the real migration with its exact anchor+geometry+z — MIG-02 satisfied.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces:
- `MigrationRoundTripTests` (new, `tests/BlazorCanvas.Tests/Migrations/MigrationRoundTripTests.cs`) — the MIG-02 round-trip proof.
- `FixtureManifest` (new, `tests/BlazorCanvas.Tests/Migrations/FixtureManifest.cs`) — the MANIFEST loader for expected values.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| immutable fixture -> throwaway DB (read-only) | The fixture is captured evidence; the test seeds from it without mutating it, on a disposable database isolated from the shared canvas DB. |
| real migration under test -> migrated rows | The test's whole value is that it drives the ACTUAL AnchorGeometryRewrite, so a backfill bug shows up here, not in a parallel re-implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-14 | Tampering | Silent figure alteration/loss | high | mitigate | The core risk the phase exists to close. Mitigated by asserting row-count preservation + per-row anchor+geometry+z equality against an independently re-derived MANIFEST, including the circle-precision and line-flip cases, over the real migration. |
| T-09-15 | Repudiation | Vacuous pass | high | mitigate | An empty/typeless fixture could make the test pass while proving nothing. Mitigated by the all-four-types-present precondition and the byte-identical fixture (09-02). |
| T-09-16 | Tampering | Re-implemented transform | medium | mitigate | Asserting against a C# copy of the maths would not test the migration. Mitigated by driving the real EF MigrateAsync and the prohibition against re-implementation. |
| T-09-SC | Tampering | Package installs | low | accept | No new packages introduced (Npgsql + EF already referenced by the test project). |
</threat_model>

<verification>
- `dotnet build BlazorCanvas.sln` is clean (0 warnings, 0 errors).
- `dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj` passes in full, including MigrationRoundTripTests.
- No `canvas_migtest_*` databases remain after the run (disposable DB cleaned up).
- The immutable fixture is unchanged (git shows no diff to v1.1-pre-rewrite.sql).
</verification>

<success_criteria>
The round-trip test drives the real migration against the immutable fixture and proves every figure of all four types survives with its exact anchor, geometry (circle {r} centre-anchor exact, line {dx,dy} unflipped), and z-order — row count preserved, no vacuous pass. MIG-02 is satisfied and the full suite is green: the phase ends build-green.
</success_criteria>

<output>
Create `.planning/phases/BC-09-schema-entity-data-preserving-migration/09-06-SUMMARY.md` when done.
</output>
