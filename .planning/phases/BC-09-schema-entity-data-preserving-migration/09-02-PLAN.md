---
phase: 09-schema-entity-data-preserving-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql
  - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md
autonomous: true
requirements: [MIG-02]
user_setup: []

must_haves:
  truths:
    - "tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql exists on this branch, ported verbatim from commit de89dcd, holding the pre-rewrite (four-integer bbox, integer id, three geometry CHECKs) schema plus real figure rows (D-49)."
    - "The fixture contains at least one row of each of the four types (line, rectangle, circle, triangle) and at least one line whose second endpoint is down AND right of the first (dx>0, dy>0) — the D-41 flip probe."
    - "v1.1-pre-rewrite-MANIFEST.md pins, for every fixture row, the expected post-migration anchor (x,y), geometry JSON, and z, re-derived on THIS branch's per-type shapes (circle centre-anchor {r}; line {dx,dy}) — not copied from commit de89dcd's manifest (D-59)."
    - "Every circle's expected r in the MANIFEST is an exact integer ((x2-x1)/2 with an even old side) and its expected anchor is the centre (x1+r, y1+r)."
    - "Every expected z in the MANIFEST equals the row's old integer id, so the manifest encodes the 1:1 preserved paint order."
  artifacts:
    - "tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql — the immutable captured snapshot (old schema + data)."
    - "tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md — expected post-migration values per row, re-derived for this branch."
  key_links:
    - "The MANIFEST's expected values are the assertion source for the round-trip test in 09-06; the per-type derivation must match GeometryCodec (09-01) and the backfill SQL (09-04)."
  prohibitions:
    - "The v1.1-pre-rewrite.sql fixture must never be regenerated, hand-edited, or 'cleaned up' after this port — it is immutable captured evidence (D-49)."
    - "The MANIFEST's expected values must never be trusted from commit de89dcd blindly — they are re-derived against this branch's per-type shapes, because that branch evolved to D-60..D-69."
---

<objective>
Bring the immutable v1.1 migration fixture onto this branch and author its expected-values manifest so the round-trip test (09-06) has something authoritative to assert against. The raw pre-rewrite `.sql` (old four-integer schema + real rows) is ported unchanged from commit `de89dcd`; the MANIFEST — the expected post-migration anchor+geometry+z for every row — is RE-DERIVED here against this branch's per-type shapes (D-59), because the source branch's own manifest may reflect later, different decisions (D-60..D-69).

Purpose: the phase goal ("every existing figure survives") is only provable against a captured, immutable snapshot whose expected transform is independently re-derived, not assumed.
Output: the fixture `.sql` (immutable) and the re-derived MANIFEST.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md
@src/BlazorCanvas/Geometry/CircleEncoding.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the immutable v1.1-pre-rewrite.sql fixture from commit de89dcd</name>
  <files>tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql</files>
  <read_first>
    - .planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md (Claude's Discretion note: fixture exists on branch Milestone-v1.11, commit de89dcd, plan BC-09-03)
    - docs/DECISIONS.md §THE SCHEMA "Pre-v1.11 schema (historical)" (the old four-integer DDL the fixture uses)
  </read_first>
  <action>
    Retrieve the fixture from git history without checking out the other branch: use `git show de89dcd:tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql` (adjust the in-repo path if `git show de89dcd --stat` reveals a different location) and write its exact bytes to tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql on this branch. Do not reformat, re-order, or edit any row. After writing, inspect the contents and record (for Task 2) every row's old id, type, and x1,y1,x2,y2. Confirm the snapshot contains at least one row of each of the four types and at least one down-right diagonal line (a line row where x2>x1 AND y2>y1). If the ported snapshot is missing a type or the down-right diagonal, STOP and surface it as a flagged assumption in the SUMMARY (the fixture is immutable and cannot be edited to add coverage) rather than silently proceeding.
  </action>
  <acceptance_criteria>
    - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql exists and is byte-identical to `git show de89dcd:&lt;path&gt;` (verify with a diff of the two).
    - The file defines the OLD schema (integer id identity, x1,y1,x2,y2 columns, the three geometry CHECKs) and inserts figure rows.
    - Grepping the INSERT rows shows all four type literals ('line','rectangle','circle','triangle') present and at least one line row with x2>x1 and y2>y1.
    - No row was added, removed, or altered relative to the source commit.
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql &amp;&amp; git show de89dcd:tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql | diff - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql</automated>
  </verify>
  <done>The immutable pre-rewrite fixture is on this branch verbatim, with all four types and a down-right diagonal line present (or the gap flagged).</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the expected post-migration MANIFEST against this branch's per-type shapes</name>
  <files>tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql (the rows just ported — the source of truth for the derivation)
    - .planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md (per-type JSON shapes + z-from-old-id rule under D-59)
    - src/BlazorCanvas/Geometry/CircleEncoding.cs (circle centre/radius maths)
  </read_first>
  <action>
    For every row in the ported fixture, compute the expected post-migration values using this branch's per-type formulas: rectangle/triangle -> anchor (x1,y1), geometry {w:x2-x1, h:y2-y1}; circle -> anchor (x1+r, y1+r) with r=(x2-x1)/2, geometry {r}; line -> anchor (x1,y1), geometry {dx:x2-x1, dy:y2-y1} (dy signed, pair never swapped); z = the row's old integer id. Author v1.1-pre-rewrite-MANIFEST.md as a table keyed by old id, with columns: old id, type, old (x1,y1,x2,y2), expected anchor (x,y), expected geometry JSON, expected z. Add a short header stating the derivation rule and that these values are re-derived for this branch (NOT copied from de89dcd). Include an explicit callout row/line for the down-right diagonal line proving its expected {dx,dy} are both positive. Do NOT trust or import de89dcd's own manifest values; derive independently and, if you consult it, only to sanity-check, noting any divergence.
  </action>
  <acceptance_criteria>
    - v1.1-pre-rewrite-MANIFEST.md lists one expected-values row per fixture figure, covering all four types.
    - Each circle row's expected geometry is {r:&lt;integer&gt;} with r=(x2-x1)/2 and expected anchor equal to the centre (x1+r, y1+r); no fractional r.
    - Each line row's expected geometry is {dx:x2-x1, dy:y2-y1} with the sign of dy preserved; the down-right diagonal shows dx>0 and dy>0.
    - Every expected z equals the row's old integer id.
    - The manifest header states the values are re-derived on this branch's D-59 shapes, not copied from commit de89dcd.
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md &amp;&amp; grep -Eiq 'anchor|geometry|z' tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md</automated>
  </verify>
  <done>The MANIFEST pins independently re-derived expected anchor+geometry+z for every fixture row, ready to drive the round-trip test.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces:
- `tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite.sql` (ported immutable snapshot).
- `tests/BlazorCanvas.Tests/Fixtures/v1.1-pre-rewrite-MANIFEST.md` (re-derived expected post-migration values).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> working tree | The fixture is captured evidence; its integrity (byte-identical, unedited) is what makes MIG-02 a real proof rather than a self-fulfilling one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Tampering | Immutable fixture | high | mitigate | An edited fixture would let a buggy migration "pass" against doctored data. Mitigated by porting byte-identical from de89dcd and diffing against the source commit; the fixture is declared immutable (prohibition). |
| T-09-04 | Repudiation | Re-derived MANIFEST | medium | mitigate | Blindly trusting de89dcd's manifest could bake in that branch's later, different decisions (D-60..D-69). Mitigated by re-deriving expected values from this branch's per-type shapes and stating so in the header. |
| T-09-SC | Tampering | Package installs | low | accept | No new packages introduced. |
</threat_model>

<verification>
- The `.sql` fixture is byte-identical to the source commit (diff is empty).
- The MANIFEST covers every fixture row across all four types with re-derived anchor+geometry+z.
- No production code and no other test changes here — this plan is purely test data + documentation, so the build and existing suite are unaffected.
</verification>

<success_criteria>
The immutable fixture is on this branch verbatim and its expected-values manifest is independently re-derived for this branch's D-59 shapes, with the circle centre-anchor {r} and the down-right diagonal line explicitly pinned. The round-trip test (09-06) can now assert against it.
</success_criteria>

<output>
Create `.planning/phases/BC-09-schema-entity-data-preserving-migration/09-02-SUMMARY.md` when done.
</output>
