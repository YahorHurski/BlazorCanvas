---
phase: 09-schema-entity-data-preserving-migration
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
  - tests/BlazorCanvas.Tests/Data/FigureStoreTests.cs
  - tests/BlazorCanvas.Tests/Sync/CanvasSyncNotifierTests.cs
  - tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs
  - tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs
  - tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs
autonomous: true
requirements: [STOR-01]
user_setup:
  - service: postgres (Docker Compose)
    why: "The Database-collection tests run against the live migrated Postgres (host 5433)."
    dashboard_config:
      - task: "docker compose up -d --wait before running the suite"
        location: "repo root"

must_haves:
  truths:
    - "The whole test project compiles on the new model: no reference to Figure.X1/Y1/X2/Y2 or an integer figure Id remains anywhere in tests (D-59)."
    - "SchemaShapeTests asserts the NEW live shape — figures columns id,user_id,type,x,y,geometry,z; id uuid (not identity); geometry jsonb; z numeric; the only figures CHECK is figures_type_is_known; index ix_figures_user_id_z; no created_at (STOR-01, D-59, D-46)."
    - "The DB tests that asserted the three removed geometry CHECKs (circle_is_a_circle, box_is_a_box, line_is_a_line) or the guard-mirrors-DB-CHECK invariant are trimmed to what still holds on the new schema — the figures_type_is_known whitelist cases and well-formed inserts — because those CHECKs no longer exist (D-59, D-09)."
    - "FigureStoreTests proves, on the new model: load returns an empty non-null list for a user with no figures; IDOR (a user cannot update/delete another user's figure by Guid id); insert returns a database-assigned Guid; and load order follows z, id."
    - "SyncMessage factory tests assert Guid ids and the retained box-shaped payload (the payload rework is Phase 10, not asserted here)."
    - "After this plan `dotnet build BlazorCanvas.sln` is clean (0 warnings, 0 errors) and `dotnet test` passes for the whole existing suite (the MIG-02 round-trip test is added separately in 09-06)."
  artifacts:
    - "Adapted DatabaseFixture raw-insert helper (anchor+geometry, no x1..y2)."
    - "Adapted FigureStoreTests, CanvasSyncNotifierTests (Guid ids, anchor+geometry, z-order)."
    - "Rewritten SchemaShapeTests (new live shape) and trimmed CheckConstraintTests/GuardMirrorsChecksTests."
  key_links:
    - "SchemaShapeTests is the in-suite mirror of scripts/verify-live-schema.sh (09-04) — both assert the D-59 live shape; keep them consistent."
    - "The DatabaseFixture raw-insert helper is shared by the remaining DB tests; adapting it once unblocks CheckConstraintTests and GuardMirrorsChecksTests."
  prohibitions:
    - "Do not add new no-clamp behavioural assertions, code-side MinSizeGuard re-expression tests, sync anchor+geometry payload tests, or TEST-01 silent-failure re-evaluations here — that is the TEST-02 rework and belongs to Phase 10. This plan only restores the EXISTING suite to green on the new schema."
    - "Do not delete a test merely because it now fails — first decide whether it asserts something D-59 removed (trim it) or something still true that just needs adapting (adapt it). No bulk deletion."
---

<objective>
Restore the whole solution to green by adapting the EXISTING test suite to the D-59 model — the build-continuity counterpart of 09-03, on the test side. Changing the entity and dropping the three geometry CHECKs broke the tests at two levels: compile (tests referencing `X1..Y2` and an integer `Id`) and runtime (DB tests asserting the old schema shape and the removed CHECKs). This plan fixes both, minimally.

Scope discipline (important): this is NOT the TEST-02 rework. We only (a) make the suite compile on Guid ids + anchor+geometry, (b) re-point the schema-shape assertions at the new live shape (which STOR-01 success criterion 1 needs), and (c) trim the assertions tied to CHECKs/invariants D-59 deliberately removed. The behavioural test rework — repurposing edge-clamp tests to assert no-clamp, re-expressing MinSizeGuard code-side, sync payload tests, TEST-01 re-evaluation — is Phase 10 and must NOT appear here.

Purpose: end the phase with `dotnet build BlazorCanvas.sln` clean and the existing `dotnet test` suite green on the new storage.
Output: adapted DatabaseFixture + FigureStoreTests + CanvasSyncNotifierTests + SchemaShapeTests + CheckConstraintTests + GuardMirrorsChecksTests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-09-schema-entity-data-preserving-migration/09-CONTEXT.md
@tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
@tests/BlazorCanvas.Tests/Data/FigureStoreTests.cs
@tests/BlazorCanvas.Tests/Sync/CanvasSyncNotifierTests.cs
@tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs
@tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs
@tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs
@src/BlazorCanvas/Geometry/GeometryCodec.cs
@src/BlazorCanvas/Data/FigureStore.cs
@src/BlazorCanvas/Sync/SyncMessage.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adapt the compile-surface tests — DatabaseFixture, FigureStoreTests, CanvasSyncNotifierTests</name>
  <files>tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs, tests/BlazorCanvas.Tests/Data/FigureStoreTests.cs, tests/BlazorCanvas.Tests/Sync/CanvasSyncNotifierTests.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs (TryInsertRawFigureAsync / TryInsertFigureAsync build Figure{X1..Y2})
    - tests/BlazorCanvas.Tests/Data/FigureStoreTests.cs (Guid vs int Id, moved.X1.., Box round-trips, Update/Delete int ids)
    - tests/BlazorCanvas.Tests/Sync/CanvasSyncNotifierTests.cs (SyncMessage.Draw(Figure{X1..}), int-id Delete/Move/Rollback)
    - src/BlazorCanvas/Geometry/GeometryCodec.cs, src/BlazorCanvas/Data/FigureStore.cs, src/BlazorCanvas/Sync/SyncMessage.cs
  </read_first>
  <action>
    DatabaseFixture: rewrite the raw-insert helper so it inserts a Figure on the new columns (x, y, geometry, z) rather than x1..y2 — provide a helper that takes an anchor + a geometry value (and a z, defaulting to a monotonic value) so the DB tests can still push arbitrary raw rows to exercise Postgres directly; keep the transaction-always-rolled-back pattern. CreateTestUserAsync is unchanged (users table untouched). FigureStoreTests: retype every figure Id from int to Guid; replace assertions like `inserted.Id > 0` with a Guid non-empty check; replace the strict-ascending `loaded[i].Id > loaded[i-1].Id` order proof with an assertion on the returned sequence / on Z ordering (load returns them in insertion order because z appends); replace `moved.X1..Y2` reads with a GeometryCodec.DecodeToBox(...) round-trip (or direct X/Y/Geometry assertions); pass Guid literals (e.g. Guid.NewGuid() for a missing-id case) where int 999_999 was used. Keep the IDOR, empty-list, id-after-insert, and round-trip test INTENTS intact — only their mechanics change. CanvasSyncNotifierTests: change the SyncMessage id arguments from ints to Guids; build the Draw figure with X/Y/Geometry (via GeometryCodec.Encode of the same box the old test used) and assert the message still carries that box and a Guid id.
  </action>
  <acceptance_criteria>
    - No `X1`/`Y1`/`X2`/`Y2` and no int figure-Id usage remain in these three files.
    - FigureStoreTests still contains tests for: empty-user load (non-null empty), IDOR on Update and Delete (Guid id), insert-returns-db-Guid, and load ordering; all pass against the live migrated DB.
    - CanvasSyncNotifierTests asserts Guid ids and the box-shaped payload for Draw/Move/Delete/Rollback.
    - The three files compile.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj</automated>
  </verify>
  <done>The compile-surface tests build on Guid ids + anchor+geometry with their security/behaviour intents preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-point SchemaShapeTests at the new live shape and trim the removed-CHECK tests</name>
  <files>tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs, tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs, tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs (asserts 7 old columns, 4 CHECKs, identity id, ix_figures_user_id, "inscribed in" comment)
    - tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs (geometry-CHECK rejection matrix + type-whitelist cases)
    - tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs (guard-mirrors-DB matrix + type-literal round-trip + persistence-survives-teardown)
    - scripts/verify-live-schema.sh (09-04 — the CLI mirror of the shape to assert)
    - docs/DECISIONS.md §THE SCHEMA + D-59, D-46
  </read_first>
  <action>
    SchemaShapeTests: update the figures column assertion to exactly id,user_id,type,x,y,geometry,z; assert id is uuid (data_type 'uuid') with a gen_random_uuid() default rather than an identity column; assert geometry is jsonb and z is numeric; change the CHECK-constraint assertion from four names to exactly one (figures_type_is_known); change the index assertion from ix_figures_user_id to ix_figures_user_id_z over (user_id, z); update the comment assertion to match the new anchor+geometry comment (drop the "inscribed in" expectation); add an assertion that no created_at column exists. Keep the users-table, FK-cascade, unique-username, and no-canvases-table tests as they are (still true). CheckConstraintTests: remove the rejection cases for circle_is_a_circle, box_is_a_box, and line_is_a_line (those CHECKs no longer exist — keeping them would assert removed behaviour); keep and adapt the figures_type_is_known cases (unknown literal, PascalCase literal) and the well-formed-insert cases, routing inserts through the adapted DatabaseFixture helper on the new columns. GuardMirrorsChecksTests: remove the guard-mirrors-database-CHECK matrix and the shared-box opposite-verdict pair (the DB no longer enforces geometry, so the invariant is gone — its code-side re-expression is Phase 10); keep the type-literal-round-trips-against-figures_type_is_known test and the FiguresWrittenViaEfCore_SurviveContainerTeardown test, adapting the latter to Guid ids and z-order (assert survival + order by z, not int-id ascension). Add a one-line comment in each trimmed file pointing to Phase 10 / STOR-03 for the code-side guard re-expression, so the removal is deliberate and traceable, not a silent drop.
  </action>
  <acceptance_criteria>
    - SchemaShapeTests asserts the exact new column set, uuid id with gen_random_uuid() default, jsonb geometry, numeric z, single figures_type_is_known CHECK, ix_figures_user_id_z, and absence of created_at; the users/FK/unique/no-canvases tests are retained.
    - CheckConstraintTests no longer references circle_is_a_circle/box_is_a_box/line_is_a_line; it retains figures_type_is_known and well-formed-insert coverage on the new columns.
    - GuardMirrorsChecksTests no longer asserts the guard-mirrors-DB invariant; it retains the type-literal round-trip and the persistence-survives-teardown test (Guid + z-order).
    - Each trimmed file carries a comment pointing forward to Phase 10 / STOR-03.
    - `dotnet build BlazorCanvas.sln` is clean and `dotnet test` passes for the whole existing suite against the live migrated DB.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build BlazorCanvas.sln &amp;&amp; dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj</automated>
  </verify>
  <done>The live-schema tests assert the D-59 shape and the removed-CHECK/guard-mirror tests are deliberately trimmed; the full solution builds clean and the existing suite is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces (adapted test symbols):
- `DatabaseFixture` raw-insert helper on the new (x, y, geometry, z) columns.
- Adapted `FigureStoreTests` and `CanvasSyncNotifierTests` (Guid ids, anchor+geometry, z-order).
- Rewritten `SchemaShapeTests` (new live shape) and trimmed `CheckConstraintTests` / `GuardMirrorsChecksTests` (removed-CHECK assertions dropped, type-whitelist + persistence retained).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> live Postgres | The Database-collection tests are the in-suite proof that the live schema and the IDOR/user-scoping guarantees hold on the new model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Repudiation | Over-trimming the suite | high | mitigate | Deleting failing tests wholesale would hide regressions and silently drop the IDOR/schema guarantees. Mitigated by the per-test classify-before-removing rule (adapt vs trim), retaining every still-true intent (IDOR, empty-load, schema shape, persistence), and forward-comments marking each deliberate removal. |
| T-09-13 | Elevation of Privilege (IDOR) | FigureStoreTests coverage | medium | mitigate | The user-scoping proofs must survive the rewrite. Mitigated by keeping the Update/Delete IDOR tests, retyped to Guid ids, passing against the live DB. |
| T-09-SC | Tampering | Package installs | low | accept | No new packages introduced (no DI/mocking package added — the existing nested test factory pattern is reused). |
</threat_model>

<verification>
- `dotnet build BlazorCanvas.sln` is clean (0 warnings, 0 errors).
- `dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj` passes for the whole existing suite against the live migrated Postgres.
- Grep confirms no `X1/Y1/X2/Y2`, no removed-CHECK names, and no int figure-Id usage remain in the test project.
- The unchanged geometry unit tests (CircleEncoding, Normalisation, Clamp, DrawGesture, MinSizeGuard) still pass — Phase 9 did not touch those Box-based helpers.
</verification>

<success_criteria>
The full solution builds clean and the existing test suite is green on the D-59 model: schema-shape tests assert the new live shape, the removed-CHECK/guard-mirror tests are deliberately trimmed with forward-pointers to Phase 10, and every still-valid intent (IDOR, empty-load, persistence, id-after-insert, ordering) is preserved. No TEST-02 behavioural rework has leaked in.
</success_criteria>

<output>
Create `.planning/phases/BC-09-schema-entity-data-preserving-migration/09-05-SUMMARY.md` when done.
</output>
