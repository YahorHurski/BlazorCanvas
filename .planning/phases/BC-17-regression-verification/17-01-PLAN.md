---
phase: BC-17-regression-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/BC-17-regression-verification/17-UAT.md
  - .planning/phases/BC-17-regression-verification/17-01-SUMMARY.md
autonomous: false
requirements: [REG-02]

must_haves:
  truths:
    - "REG-02 is accepted only by human observation on the running application, not by automated tests alone."
    - "A human arms Star, draws with a live five-point preview, sees canvas-edge clamping, refreshes, and confirms the star persists unchanged."
    - "A human selects a star, sees the blue-and-white trace on the star outline, drags with edge clamping, and deletes it like the existing shapes."
    - "Two normal same-profile browser windows on the same disposable account show a committed star gliding live during a slow drag."
    - "Any failed human step records the first failure, evidence, and logs, and REG-02 remains incomplete."
  artifacts:
    - path: ".planning/phases/BC-17-regression-verification/17-UAT.md"
      provides: "Human checklist and evidence record for REG-02."
    - path: ".planning/phases/BC-17-regression-verification/17-01-SUMMARY.md"
      provides: "Execution outcome, preflight results, UAT status, evidence paths, and failure handling."
  key_links:
    - from: "One local dotnet run process"
      to: "Two normal same-profile browser windows"
      via: "shared cookie session and the process-local CanvasSyncNotifier"
      pattern: "http://localhost:5054"
    - from: "Phase 16 automated star guards"
      to: "Phase 17 human UAT"
      via: "pre-UAT smoke support only; human visual acceptance remains required"
---

<objective>
Run the final acceptance-only REG-02 regression gate: prove on the running application that `star5`
behaves end-to-end like the four existing shapes.

Purpose: Phase 15 and Phase 16 automated evidence proves the star draw, preview, persistence,
selection, drag, delete, sync, and guard contracts at code level. This plan supplies the separate
human visual and two-window acceptance those tests cannot infer.
Output: `17-UAT.md` and `17-01-SUMMARY.md` recording approval or the exact failed observation and its
preserved evidence.
No production code, package, migration, schema, browser automation, app harness, or feature work is
created.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-17-regression-verification/17-RESEARCH.md
@.planning/phases/BC-17-regression-verification/17-PATTERNS.md
@.planning/phases/BC-16-interaction-sync-test-guards/16-VERIFICATION.md
@.planning/phases/BC-16-interaction-sync-test-guards/16-01-SUMMARY.md
@.planning/phases/BC-16-interaction-sync-test-guards/16-02-SUMMARY.md
@.planning/phases/BC-16-interaction-sync-test-guards/16-03-SUMMARY.md
@.planning/phases/BC-16-interaction-sync-test-guards/16-04-SUMMARY.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-01-SUMMARY.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-03-SUMMARY.md
@.planning/milestones/v1.11-phases/BC-12-regression-verification/12-01-PLAN.md
@.planning/milestones/v1.11-phases/BC-12-regression-verification/12-UAT.md
@docs/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare UAT record, run smoke checks, and start one acceptance host</name>
  <files>.planning/phases/BC-17-regression-verification/17-UAT.md</files>
  <action>
    Create `.planning/phases/BC-17-regression-verification/17-UAT.md` as a machine-readable human
    checklist with frontmatter fields `status`, `phase`, `source`, `started`, and `updated`; three
    test sections for star draw/preview/clamp/refresh, star select/edge-drag/delete, and second-window
    live star glide; and a Summary counter block initialized as pending. This file is the REG-02
    evidence record, not a product artifact.

    From the repository root, run `docker compose up -d --wait`, `docker compose ps`,
    `dotnet build BlazorCanvas.sln --nologo`, and the focused star smoke test filter from
    `17-RESEARCH.md`. Stop on any non-zero exit, record failed preflight in `17-UAT.md` and the final
    summary, and do not begin human verification.

    After preflight passes, create a unique directory under the system temporary path. Start exactly
    one hidden app process with `dotnet run --project src/BlazorCanvas --launch-profile http --no-build`,
    redirect stdout and stderr to separate log files, and retain the process id. Poll
    `http://localhost:5054/login` for up to 30 seconds while confirming the process has not exited;
    accept status 200 only. If the host does not become ready, preserve both logs, record the failed
    host setup, and do not begin human verification. Do not install packages, use Playwright, use
    Selenium, start a second app process, alter PostgreSQL directly, modify production code, modify
    migrations, change schema, or add a new app harness.
  </action>
  <verify>
    <automated>docker compose up -d --wait; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; docker compose ps; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet build BlazorCanvas.sln --nologo; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet test tests\BlazorCanvas.Tests\BlazorCanvas.Tests.csproj --no-build --nologo --filter "FullyQualifiedName~CanvasInteractionCoordinatorTests|FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests|FullyQualifiedName~PreviewRenderSmokeTests|FullyQualifiedName~HomePreviewSourceTests"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <automated>$logRoot = Join-Path $env:TEMP ("BlazorCanvas-BC17-" + [guid]::NewGuid()); New-Item -ItemType Directory -Path $logRoot -ErrorAction Stop | Out-Null; $stdout = Join-Path $logRoot "app.stdout.log"; $stderr = Join-Path $logRoot "app.stderr.log"; $app = Start-Process -FilePath dotnet -ArgumentList @("run","--project","src/BlazorCanvas","--launch-profile","http","--no-build") -WorkingDirectory $PWD -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru -WindowStyle Hidden; $deadline = [DateTime]::UtcNow.AddSeconds(30); $ready = $false; while ([DateTime]::UtcNow -lt $deadline) { if ($app.HasExited) { throw "Acceptance host exited with code $($app.ExitCode). Logs: $stdout; $stderr" }; try { $response = Invoke-WebRequest -Uri "http://localhost:5054/login" -UseBasicParsing -ErrorAction Stop; if ($response.StatusCode -eq 200) { $ready = $true; break } } catch { $lastRequestError = $_.Exception.Message }; Start-Sleep -Milliseconds 500 }; if (-not $ready) { throw "Acceptance host did not return HTTP login status 200 before deadline. Logs: $stdout; $stderr. Last request error: $lastRequestError" }; "PID=$($app.Id); LOG_ROOT=$logRoot"</automated>
  </verify>
  <done>`17-UAT.md` exists; Docker, build, focused star smoke checks pass; exactly one local HTTP app process serves login; URL, PID, and log paths are available for evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the REG-02 star acceptance script</name>
  <files>.planning/phases/BC-17-regression-verification/17-UAT.md</files>
  <read_first>
    .planning/phases/BC-17-regression-verification/17-RESEARCH.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/BC-16-interaction-sync-test-guards/16-VERIFICATION.md
    .planning/milestones/v1.11-phases/BC-12-regression-verification/12-UAT.md
  </read_first>
  <action>
    Use Task 1's single running host at `http://localhost:5054`. In normal same-profile window A,
    open `/login` and create a fresh disposable account named `bc17-regression-YYYYMMDD-HHMMSS` with
    a non-sensitive disposable password. After window A redirects to the canvas, open normal
    same-profile window B at `/`. Confirm B uses the same account and same canvas without a second
    login prompt. Do not use private or incognito mode, a second browser profile, a real account, or a
    second application process.

    Complete the numbered observations below. Record pass or fail in `17-UAT.md` with evidence paths.
    Prefer a short video for the live glide check; screenshots are acceptable for static states. On
    the first failure, stop immediately; record the failing step, expected behavior, observed behavior,
    screenshot or video location, browser console observations if available, app stdout/stderr log
    paths, and whether refresh changes the state. Preserve the account and figures. Do not clean up,
    inspect or edit code, rerun the same account to chase a pass, change database rows, or mark REG-02
    complete.
  </action>
  <what-built>
    Earlier phases built and automatically verified star draw, registry-backed preview, edge clamp,
    immediate persistence, select, drag, delete, and type-blind cross-window sync. This checkpoint
    verifies the assembled browser experience that automated tests cannot replace.
  </what-built>
  <how-to-verify>
    1. Session baseline: Log in as the disposable user in window A, then open window B from the same
       normal browser profile. Expected: both windows show the same 1472 by 828 canvas, seven toolbar
       controls with Star between Triangle and Delete, no reconnect or error UI, and no second login
       prompt in B.

    2. Star arm, live preview, edge clamp, and refresh: In A, arm Star and drag a clear diagonal box.
       During the held pointer movement, expected: a five-point star preview follows the pointer, not
       a triangle or empty preview. Drag the creation gesture beyond a canvas edge before release.
       Expected: the preview and committed star remain inside the canvas edge. Refresh A. Expected: the
       same star reappears unchanged, with no Save button needed.

    3. Star selection, edge-clamped drag, and delete: Arm Pointer and click the star. Expected: exactly
       one blue-and-white dashed trace follows the star's own outline. Drag the selected star beyond a
       canvas edge and continue along the free axis. Expected: the star stops at the edge, slides along
       it without resizing, remains selected after drop, and persists after refresh. Delete it with the
       toolbar Delete action. Expected: it disappears, remains absent after refresh, and Delete disables
       when nothing is selected.

    4. Second-window live star glide: Draw or leave one committed star visible in both windows. Slowly
       drag the star in A for two to three seconds while watching B. Expected: B shows the star gliding
       through intermediate positions before pointer release and ending at A's final position, with no
       duplicate star, no jump-only update, and no disappearing/reappearing artifact. Capture at least
       one mid-drag frame or a short recording.

    5. UI-only cleanup on pass: If and only if all checks pass, delete remaining UAT figures through
       the UI and refresh both windows. Expected: both windows are empty and synchronized. Never use SQL,
       Docker volume reset, direct database changes, or a real account for cleanup.
  </how-to-verify>
  <acceptance_criteria>
    - REG-02 is approved only when all five observations match expected behavior and evidence is recorded.
    - `17-UAT.md` records disposable username, preflight status, each human result, evidence paths, and app log paths, but never the password.
    - On failure, the first failed step and preserved evidence are recorded and REG-02 remains incomplete.
    - Scope remains acceptance-only with no production code, package, schema, migration, browser automation, or direct database mutation.
  </acceptance_criteria>
  <resume-signal>Type approved if every check passes, or describe the first failed step with expected behavior, observed behavior, and evidence location.</resume-signal>
  <verify>
    <automated>Test-Path .planning\phases\BC-17-regression-verification\17-UAT.md; if (-not $?) { exit 1 }; dotnet build BlazorCanvas.sln --nologo; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <human-check>Complete all scripted observations using two normal same-profile browser windows against one local HTTP process and type approved only when all expectations pass.</human-check>
  </verify>
  <done>Human approval or the precise first failure is available with evidence; automated tests alone are not treated as REG-02 completion.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize UAT evidence and plan summary</name>
  <files>.planning/phases/BC-17-regression-verification/17-UAT.md, .planning/phases/BC-17-regression-verification/17-01-SUMMARY.md</files>
  <action>
    After the checkpoint resumes, update `17-UAT.md` from pending to complete only when the human typed
    approved and every REG-02 observation passed. Set the Summary counters so `total`, `passed`,
    `issues`, `pending`, `skipped`, and `blocked` match the recorded outcome. If any step failed,
    keep status failed or blocked, leave failed evidence intact, record the first failing step, and do
    not mark REG-02 complete.

    Create `17-01-SUMMARY.md` with Outcome, Preflight, Human UAT, Failure Handling, Verification, Scope
    Guard, and Next Phase Readiness sections. Include the disposable username, app URL, app process id,
    log paths, evidence paths, and exact pass/fail state. Exclude the password. Stop the retained app
    process after evidence capture. Confirm the git diff for this phase contains only planning/UAT
    artifacts and no production source, test source, package, schema, migration, Playwright, Selenium,
    or harness changes.
  </action>
  <verify>
    <automated>Test-Path .planning\phases\BC-17-regression-verification\17-UAT.md; if (-not $?) { exit 1 }; Test-Path .planning\phases\BC-17-regression-verification\17-01-SUMMARY.md; if (-not $?) { exit 1 }; git diff --name-only -- . ':!*.md' | Select-String -Pattern 'src/|tests/|\\.csproj$|packages\\.lock|package\\.json|playwright|selenium|migrations|schema' -Quiet; if ($?) { throw "Unexpected non-planning implementation changes detected" }</automated>
  </verify>
  <done>`17-UAT.md` and `17-01-SUMMARY.md` accurately record pass/fail evidence; REG-02 is complete only on full human approval; no production, package, schema, migration, browser automation, or harness changes were made.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

| Artifact | Produced by | Purpose |
|---|---|---|
| `.planning/phases/BC-17-regression-verification/17-UAT.md` | Task 1 creates, Tasks 2-3 update | Human REG-02 checklist and evidence record. |
| `.planning/phases/BC-17-regression-verification/17-01-SUMMARY.md` | Task 3 | Execution summary, preflight output, UAT verdict, evidence paths, failure handling, and scope guard. |

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser to local app | The human drives login, pointer gestures, toolbar actions, refresh, and visible acceptance judgment. |
| Window A to notifier to Window B | Two Blazor Server circuits must share one disposable-user session and one app process for valid glide observation. |
| Operator to local database | UAT creates disposable figures only through UI; direct mutation invalidates acceptance evidence. |
| Planner/executor to repository | This phase writes planning and evidence artifacts only; implementation edits would invalidate the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | UAT credentials | medium | mitigate | Use a disposable UAT username and non-sensitive password; record the username but never the password. |
| T-17-02 | Spoofing | Second browser window/session | medium | mitigate | Require two normal same-profile windows, confirm same account and canvas, and prohibit private/incognito windows for the main gate. |
| T-17-03 | Tampering | Acceptance data | medium | mitigate | Use UI-only setup and cleanup; prohibit SQL, Docker volume reset, direct database changes, or real-account reuse. |
| T-17-04 | Repudiation | Acceptance outcome | low | mitigate | `17-UAT.md` and `17-01-SUMMARY.md` record checklist results, evidence paths, app log paths, and first failure if any. |
| T-17-05 | Tampering | Repository/process scope | medium | mitigate | Confirm no production source, package, schema, migration, browser automation, or harness changes occur during the acceptance gate. |
| T-17-06 | Denial of Service | Local app process | low | accept | Preflight verifies Docker/build/tests; retained PID and stdout/stderr logs support diagnosis if the local host exits. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs are planned or allowed in this acceptance-only phase. |

No new production trust boundary, endpoint, auth behavior, input parser, schema path, or deployment surface is introduced.
</threat_model>

<verification>
1. Pre-UAT automation: `docker compose up -d --wait`, `docker compose ps`, `dotnet build BlazorCanvas.sln --nologo`, and the focused star smoke test filter pass before browser work begins.
2. Runtime setup: exactly one app process serves `http://localhost:5054/login`; both normal browser windows use the same disposable account and same process-local notifier path.
3. Human UAT: automated tests alone cannot complete this phase; REG-02 requires the human checklist in `17-UAT.md`.
4. Failure handling: if any human step fails, record the first failing step, expected and observed behavior, screenshots or video, browser console observations if available, and app logs; do not mark REG-02 complete.
5. API Coverage Decision Checkpoint: not applicable because detection is false for this phase; no API endpoint or Web API layer is added or changed.
6. Assumption-Delta Architecture Checkpoint: not applicable because detection is false for this phase; no architecture assumption changes from Phase 16 evidence are introduced.
7. Schema Push Detection Gate: not applicable because detection is false for this phase; no schema, migration, EF model, or package change is allowed.
8. Scope guard: final diff is limited to `17-UAT.md` and `17-01-SUMMARY.md` during execution, plus this plan artifact from planning.
</verification>

<success_criteria>
REG-02 is accepted only when a human records an approved two-window run proving Star arming, live
preview, edge-clamped draw, refresh persistence, star-outline selection trace, edge-clamped drag,
delete, and real-time second-window glide. Otherwise the phase summary contains the first precise
failure and preserved evidence for a separately authorized follow-up.
</success_criteria>

<source_audit>

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 17 goal | A human confirms on the running app that star behaves exactly like the existing shapes end-to-end. | Task 2 full browser UAT; Task 3 summary records verdict. | COVERED |
| REQ | REG-02 | Human acceptance confirms arm, draw with live preview, edge-clamp, refresh, select, drag, delete, and second-window live star glide. | Task 2 observations 1-4; Task 3 completion gate. | COVERED |
| RESEARCH | Pre-UAT smoke | Run Docker, build, and focused star guard checks before human time. | Task 1 automated preflight. | COVERED |
| RESEARCH | One app process | Use one `dotnet run` process because `CanvasSyncNotifier` is process-local. | Task 1 host setup; Task 2 prohibits second process. | COVERED |
| RESEARCH | Same-profile windows | Use two normal same-profile browser windows on the same account. | Task 2 session baseline and acceptance criteria. | COVERED |
| RESEARCH | Evidence-first failure handling | Preserve first failure, screenshots/video, console observations if available, and logs; no implementation work. | Tasks 2-3 failure rules. | COVERED |
| RESEARCH | Out-of-scope tooling | Do not add packages, migrations, schema changes, Playwright, Selenium, or new app harnesses. | Tasks 1-3 scope guards and verification checks. | COVERED |
| PATTERNS | BC-12 precedent | Regression verification should be a runbook with preflight, one host, blocking human verification, and evidence summary. | One sequential plan with auto/checkpoint/auto tasks. | COVERED |
| CONTEXT | none | No CONTEXT.md exists; user explicitly chose to continue without context. | No locked phase decisions are assumed beyond roadmap, requirements, research, state, and decisions docs. | COVERED |

</source_audit>

<output>
Create `.planning/phases/BC-17-regression-verification/17-01-SUMMARY.md` when done.
</output>
