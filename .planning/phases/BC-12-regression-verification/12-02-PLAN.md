---
phase: BC-12-regression-verification
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/BlazorCanvas/Components/Pages/DrawingPreviewSession.cs
  - src/BlazorCanvas/Components/Pages/Home.razor
  - tests/BlazorCanvas.Tests/Components/DrawingPreviewSessionTests.cs
  - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
autonomous: true
gap_closure: true
requirements: [REG-01]

must_haves:
  truths:
    - "While a user holds the primary pointer button to draw a figure, the originating browser tab visibly updates that figure's preview at the current pointer position."
    - "An in-progress preview remains circuit-local and is not delivered as a create or position-update message to another browser tab."
    - "The second tab receives the new figure only after the originating tab commits a valid draw gesture, while existing committed-figure glide behavior remains unchanged."
  artifacts:
    - path: "src/BlazorCanvas/Components/Pages/DrawingPreviewSession.cs"
      provides: "Circuit-local draw-gesture preview state and placement updates without persistence or synchronization dependencies."
    - path: "tests/BlazorCanvas.Tests/Components/DrawingPreviewSessionTests.cs"
      provides: "Automated local-preview lifecycle and geometry-update regression coverage."
  key_links:
    - from: "src/BlazorCanvas/Components/Pages/Home.razor"
      to: "src/BlazorCanvas/Components/Pages/DrawingPreviewSession.cs"
      via: "pointer-down begins and pointer-move updates the local session before the SVG FigureShape conditional renders its placement"
      pattern: "DrawingPreviewSession"
    - from: "src/BlazorCanvas/Components/Pages/Home.razor"
      to: "src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs"
      via: "only the completed gesture is passed to DrawAsync, whose canonical draw publication is the cross-tab boundary"
      pattern: "CommitDrawAsync"
---

<objective>
Close the REG-01 drawing-preview regression: render each in-progress shape immediately and only in
the tab that is drawing it, without changing committed figure persistence or cross-tab sync.

Purpose: The failed human acceptance observed that the local preview first became visible only at
pointer release. The correction restores the locked v1.1 interaction while preserving D-35 and
D-53's local-only preview boundary.
Output: A testable local preview session, Home pointer/render wiring that schedules visible updates
during the gesture, and regression tests for both the local lifecycle and commit-only notifier path.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-12-regression-verification/12-VERIFICATION.md
@.planning/phases/BC-12-regression-verification/12-01-SUMMARY.md
@src/BlazorCanvas/Components/Pages/Home.razor
@src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
@src/BlazorCanvas/Components/Canvas/FigureShape.razor
@tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate and prove the originating-tab drawing-preview lifecycle</name>
  <files>src/BlazorCanvas/Components/Pages/DrawingPreviewSession.cs, tests/BlazorCanvas.Tests/Components/DrawingPreviewSessionTests.cs</files>
  <behavior>
    - Test 1: Starting a rectangle gesture produces an active local placement at the press point; advancing the pointer replaces it with the registry's current gesture placement.
    - Test 2: The same lifecycle preserves the existing registry semantics for line, circle, and triangle, including their normal canvas-edge clamp behavior.
    - Test 3: Completing or clearing a session removes its placement and exposes the immutable completed gesture required by Home's later persistence call.
  </behavior>
  <action>
    First add failing xUnit coverage for a new `DrawingPreviewSession` in the Pages namespace. Then
    implement that small circuit-local state object around the existing `ShapeRegistry`: it must own
    the armed shape name, press coordinate, current cursor coordinate, and current `ShapePlacement`;
    it must create and refresh that placement solely through `IShapeDefinition.FromGesture`; and it
    must provide the completed type/press/cursor data that `Home.razor` needs to invoke its existing
    commit path. Keep it independent of `CanvasInteractionCoordinator`, repositories,
    `CanvasSyncNotifier`, `SyncMessage`, and `FigureRow`: a preview is not a figure and must not
    cross the circuit boundary. Preserve every existing shape, geometry formula, style, schema, and
    validation rule; this task introduces no new shape, protocol kind, database write, or UI control.
  </action>
  <verify>
    <automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~DrawingPreviewSessionTests"</automated>
  </verify>
  <done>Focused tests demonstrate that a local gesture placement exists before commit, changes as the cursor changes for all four existing shapes, and is cleared only after its completed gesture has been captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire immediate local SVG refresh and lock down the commit-only sync boundary</name>
  <files>src/BlazorCanvas/Components/Pages/Home.razor, tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs</files>
  <behavior>
    - Test 1: The Home pointer contract begins and updates `DrawingPreviewSession`, conditionally renders its placement through `FigureShape`, and requests a render after each local preview mutation while the primary button remains down.
    - Test 2: A preview lifecycle cannot publish a notifier message; the first create message for a new figure is emitted only when `CanvasInteractionCoordinator.DrawAsync` receives the completed gesture.
    - Test 3: The existing coordinator protocol still emits no position message for a new draw and continues to use position messages only for committed-figure dragging.
  </behavior>
  <action>
    Refactor `Home.razor` to replace its scattered `drawing`, preview type, placement, press, and
    cursor fields with `DrawingPreviewSession`. On shape-tool pointer-down begin the session; on
    pointer-move while its primary button is down update the local placement and explicitly request
    the interactive component render so the SVG patch is visible before pointer-up. Render
    `FigureShape` from the active session placement only in this originating circuit. On pointer-up,
    pointer-leave, or the existing lost-button guard, capture the completed gesture, clear the local
    preview, and call the unchanged coordinator draw method exactly once. Retain D-57's commit-on-
    leave behavior, the toolbar coordinate mapping, edge clamping through the registry, selection,
    and all existing drag handlers.

    Extend the existing coordinator test harness with a notifier observer that proves a valid
    committed draw produces the canonical create message and no position message. Add a focused
    Home source contract in the same test file that verifies the preview session is rendered and
    updated locally, that the preview handlers contain no notifier publication, and that the only
    handoff to `DrawAsync` is the completed-gesture path. This repository intentionally has no bUnit
    dependency, so keep the Razor-level assertion as a focused source contract rather than adding a
    browser or component-test framework. Do not alter `CanvasInteractionCoordinator`,
    `CanvasSyncNotifier`, `SyncMessage`, persistence, migrations, SVG figure geometry, style, or
    cross-tab drag behavior.
  </action>
  <verify>
    <automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~DrawingPreviewSessionTests|FullyQualifiedName~CanvasInteractionCoordinatorTests"</automated>
    <automated>dotnet build BlazorCanvas.sln --nologo -v q</automated>
  </verify>
  <done>The originating tab renders and refreshes a local preview during the draw gesture; preview code has no synchronization route; only a completed validated draw can create a remote figure; and existing committed drag synchronization tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local pointer gesture → circuit state | Browser pointer input changes only ephemeral preview state until the existing gateway validates a completed gesture. |
| Circuit state → cross-tab notifier | Only a persisted canonical figure may cross this boundary; preview geometry must remain local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | `Home.razor` draw handlers | medium | mitigate | Keep validation and persistence in `CanvasInteractionCoordinator.DrawAsync`; the preview session has no repository or notifier dependency. |
| T-12-02 | Information disclosure | `CanvasSyncNotifier` cross-tab delivery | medium | mitigate | Regression tests prove there is no preview publication and that creation occurs only after completed draw commit. |
| T-12-03 | Denial of service | Pointer-move render path | low | accept | Reuse the existing Blazor event loop and lightweight registry placement calculation; add no polling, transport, package, or background work. |
| T-12-SC | Tampering | Dependency installation | high | mitigate | Add no packages or test framework; use the existing xUnit project and full build gate. |
</threat_model>

<verification>
Run the two focused test classes, then build the solution. After automated completion, rerun the
entire seven-step two-window REG-01 human acceptance script from `12-01-PLAN.md`: the initiating
tab must show the evolving preview, the second tab must remain empty until commit, and the separate
committed-figure slow-drag glide observation must still pass.
</verification>

<success_criteria>
1. A pointer-down and pointer-move visibly update an initiating-tab-only preview for each existing shape.
2. No preview is persisted, broadcast, or visible in the second tab before the draw commits.
3. The second tab receives the canonical figure after commit and retains real-time glide for later drags.
4. Focused tests, the build, and the subsequent full REG-01 acceptance run pass without new features or schema changes.
</success_criteria>

<output>
Create `.planning/phases/BC-12-regression-verification/12-02-SUMMARY.md` when done.
</output>
