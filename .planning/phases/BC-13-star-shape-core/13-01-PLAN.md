---
phase: BC-13-star-shape-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlazorCanvas/Shapes/Star5Geometry.cs
  - src/BlazorCanvas/Shapes/Star5Shape.cs
  - tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
autonomous: true
requirements:
  - SHAPE-04
  - SHAPE-05
  - SHAPE-06
must_haves:
  truths:
    - "SHAPE-04: Star5Shape.FromGesture creates a point-up ten-point star from a corner-to-corner drag, with alternating outer and 0.382 inner vertices and stretchable width/height."
    - "SHAPE-05: Star5Shape.BoundsOf derives local bbox only by scanning the stored point list; changing only InnerRatio does not change bounds."
    - "SHAPE-06: Star5Shape.ToJson and TryParseGeometry round-trip canonical JSON exactly and reject payloads missing innerRatio."
    - "Star5Shape is not registered in DefaultShapes.CreateRegistry during Phase 13."
    - "No database, catalog seed, toolbar, renderer, persistence, sync, JavaScript preview, or decision-log files change in this phase."
  artifacts:
    - "src/BlazorCanvas/Shapes/Star5Geometry.cs"
    - "src/BlazorCanvas/Shapes/Star5Shape.cs"
    - "tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs"
  key_links:
    - "Star5Shape.TryParseGeometry -> GeometryJson.TryReadPointList and TryReadFiniteDouble"
    - "Star5Shape.ToJson -> GeometryJson.Serialise with points written before innerRatio"
    - "Star5Shape.FromGesture -> CanvasBounds clamp and PentagonShape polar point-generation pattern"
    - "Star5Shape.BoundsOf -> point-list min/max scan only"
---

<objective>
Add the pure C# core for the five-pointed star shape.

Purpose: Phase 13 proves the new figure type's shape-definition contract in isolation before later phases register, seed, draw, render, persist, or synchronize it.
Output: A Star5Geometry record, an unregistered Star5Shape implementation, and direct unit tests covering SHAPE-04, SHAPE-05, and SHAPE-06.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-13-star-shape-core/13-RESEARCH.md
@docs/DECISIONS.md
@src/BlazorCanvas/Shapes/IShapeDefinition.cs
@src/BlazorCanvas/Shapes/GeometryJson.cs
@src/BlazorCanvas/Shapes/TriangleShape.cs
@src/BlazorCanvas/Shapes/TriangleGeometry.cs
@src/BlazorCanvas/Shapes/RectangleShape.cs
@src/BlazorCanvas/Shapes/DefaultShapes.cs
@tests/BlazorCanvas.Tests/Shapes/PentagonShape.cs
@tests/BlazorCanvas.Tests/Shapes/TriangleShapeTests.cs
@tests/BlazorCanvas.Tests/Shapes/RectangleShapeTests.cs
@tests/BlazorCanvas.Tests/Shapes/ShapeRegistryExtensibilityTests.cs
@tests/BlazorCanvas.Tests/Shapes/DefaultShapesTests.cs
@tests/BlazorCanvas.Tests/Shapes/PointListPrimacyTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify direct Star5 shape behavior</name>
  <files>tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs</files>
  <behavior>
    - SHAPE-04: FromGesture with a normal and reversed corner-to-corner drag returns placement at the clamped min corner and ten ordered local points, with point 0 at top-centre, even indexes outer, odd indexes at 0.382 of the per-axis radius, and the shape stretching independently by width and height.
    - SHAPE-04: Out-of-bounds gesture coordinates are rounded away from zero and clamped to the inclusive 1472 by 828 CanvasBounds before star point derivation.
    - SHAPE-05: BoundsOf returns min/max extents from the ten stored points alone; a Star5Geometry with identical points and a different positive InnerRatio has identical bounds.
    - SHAPE-06: ToJson emits canonical minified JSON with properties ordered as points then innerRatio, TryParseGeometry preserves the ordered points and ratio, and ToJson after parse equals the original JSON byte-for-byte.
    - SHAPE-06: TryParseGeometry returns false with null geometry for missing innerRatio, wrong point count, non-array points, non-finite numbers, non-object JSON, non-numeric ratio, zero ratio, and negative ratio.
    - Direct extensibility: tests instantiate new Star5Shape directly and do not require DefaultShapes.CreateRegistry to contain star5.
  </behavior>
  <action>Create Star5ShapeTests following the local xUnit style in TriangleShapeTests and ShapeRegistryExtensibilityTests. Use helpers for expected ten-point generation so the tests assert exact LocalPoint sequences where arithmetic is deterministic and use Assert.Equal with a precision argument only for trigonometric coordinates that cannot be represented exactly. Add an assertion that DefaultShapes.CreateRegistry().Names remains exactly line, rectangle, circle, triangle to enforce the Phase 13 no-registration scope fence. Do not modify database test fixtures or add collection attributes.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter FullyQualifiedName~Star5ShapeTests</automated>
  </verify>
  <done>Star5ShapeTests expresses all SHAPE-04, SHAPE-05, and SHAPE-06 behavior directly against Star5Shape, and includes a no-registration assertion for DefaultShapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unregistered Star5Geometry and Star5Shape</name>
  <files>src/BlazorCanvas/Shapes/Star5Geometry.cs, src/BlazorCanvas/Shapes/Star5Shape.cs</files>
  <behavior>
    - SHAPE-04: Name is star5; FromGesture normalizes both CanvasPoint inputs like RectangleShape/TriangleShape, places the local origin at the clamped minimum corner, and generates ten LocalPoint values using theta = -Math.PI / 2 plus index * Math.PI / 5, radiusX = width / 2, radiusY = height / 2, and inner ratio 0.382 for odd indexes.
    - SHAPE-05: Star5Geometry stores IReadOnlyList&lt;LocalPoint&gt; Points and double InnerRatio; BoundsOf rejects the wrong geometry type and otherwise scans Points for min and max coordinates without consulting InnerRatio.
    - SHAPE-06: TryParseGeometry requires an object with exactly ten finite points and a finite positive innerRatio; ToJson writes points first and innerRatio second through GeometryJson helpers.
    - Drawability: IsDrawable returns true only for Star5Geometry with ten finite points, ten distinct vertices, non-zero area, positive local width, and positive local height.
  </behavior>
  <action>Add a sealed Star5Geometry record in the Shapes namespace. Add Star5Shape implementing IShapeDefinition with a public DefaultInnerRatio constant set to 0.382. Use the existing GeometryJson helpers for all JSON work; do not hand-build JSON strings and do not call System.Text.Json record serialization. Reuse the NormaliseGesturePoint pattern from RectangleShape/TriangleShape, including MidpointRounding.AwayFromZero and CanvasBounds inclusive clamping. Implement polygon-area validation using the ordered point list, matching the PentagonShape test-only proof. Leave DefaultShapes.cs unchanged and do not register Star5Shape here; Phase 14 owns registration and catalog seeding.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~Star5ShapeTests|FullyQualifiedName~DefaultShapesTests|FullyQualifiedName~ShapeRegistryExtensibilityTests|FullyQualifiedName~PointListPrimacyTests"</automated>
  </verify>
  <done>Star5Geometry and Star5Shape compile, targeted shape tests pass, DefaultShapes still exposes only line/rectangle/circle/triangle, and the implementation has no database or UI dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Verify additive phase boundary and full suite</name>
  <files>src/BlazorCanvas/Shapes/DefaultShapes.cs, .planning/phases/BC-13-star-shape-core/13-01-SUMMARY.md</files>
  <action>Run the targeted shape tests, verify DefaultShapes.cs has no diff, then run the full solution test suite. If dotnet test reports that src/BlazorCanvas/bin/Debug/net10.0/BlazorCanvas.exe is locked by a running BlazorCanvas process, stop the process or wait for it to exit and rerun the same test command; record that as a verification prerequisite in the summary, not as a source change. Do not alter catalog seeding, database schema, toolbar, renderer switch, persistence, sync, JavaScript preview, or docs/DECISIONS.md.</action>
  <verify>
    <automated>git diff --exit-code -- src/BlazorCanvas/Shapes/DefaultShapes.cs</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Full test suite remains green, DefaultShapes.cs is unchanged, and the plan summary documents the additive boundary and any process-lock prerequisite encountered during verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JSON -> Star5Shape.TryParseGeometry | Untrusted future star geometry JSON crosses into typed C# geometry validation. |
| browser gesture coordinates -> Star5Shape.FromGesture | Untrusted circuit-supplied coordinates become local geometry and placement. |
| typed geometry -> canonical JSON | Validated geometry is serialized for later storage/broadcast paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-BC13-01 | Tampering | Star5Shape.TryParseGeometry | medium | mitigate | Require object JSON, exactly ten finite points, finite positive innerRatio, and return false with null geometry for malformed data. |
| T-BC13-02 | Tampering | Star5Shape.ToJson | medium | mitigate | Serialize only from Star5Geometry through GeometryJson.Serialise, dropping unknown client fields by construction. |
| T-BC13-03 | Denial of Service | Star5Shape.TryParseGeometry | low | mitigate | Use existing GeometryJson fixed-count point parsing and finite-number checks; no recursive parsing or unbounded collections. |
| T-BC13-04 | Spoofing | DefaultShapes registry | low | accept | Phase 13 does not register star5, seed catalog rows, or expose user input paths for this shape; later phases must threat-model registration and persistence. |
| T-BC13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs are planned; if an executor adds one, stop and require Package Legitimacy Audit review before continuing. |
</threat_model>

<verification>
Run targeted shape verification first:

dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~Star5ShapeTests|FullyQualifiedName~DefaultShapesTests|FullyQualifiedName~ShapeRegistryExtensibilityTests|FullyQualifiedName~PointListPrimacyTests"

Then prove the phase boundary:

git diff --exit-code -- src/BlazorCanvas/Shapes/DefaultShapes.cs

Then run the full suite:

dotnet test BlazorCanvas.sln --no-restore

Verification prerequisite: if a running BlazorCanvas.exe process locks the apphost during dotnet test, stop that process or wait for it to exit, then rerun the exact full-suite command.
</verification>

<success_criteria>
SHAPE-04, SHAPE-05, and SHAPE-06 are covered by direct unit tests. Star5Shape exists and satisfies IShapeDefinition in isolation. Star5Geometry round-trips through canonical JSON. Bounds are a pure function of points. DefaultShapes.CreateRegistry remains unchanged at the four existing names. No database, catalog seed, toolbar, renderer, persistence, sync, JavaScript preview, or decision-log file changes are made. The full solution test suite remains green.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Star5Shape/Star5Geometry implement IShapeDefinition in isolation | Covered by Tasks 1 and 2 |
| GOAL | Point-up ten-point star from corner-to-corner drag | Covered by Task 1 behavior and Task 2 FromGesture implementation for SHAPE-04 |
| GOAL | TryParseGeometry/ToJson round-trip | Covered by Task 1 behavior and Task 2 JSON implementation for SHAPE-06 |
| GOAL | bbox derived purely from point list | Covered by Task 1 behavior and Task 2 BoundsOf implementation for SHAPE-05 |
| GOAL | Additive, zero database involvement | Covered by Task 3 boundary verification |
| GOAL | Do not register Star5Shape in DefaultShapes.CreateRegistry | Covered by Task 1 no-registration assertion and Task 3 git diff gate |
| GOAL | All 500 existing tests remain green | Covered by Task 3 full-suite verification |
| REQ SHAPE-04 | point-up five outer/five inner star, inner ratio 0.382, stretch to dragged box | Covered by Tasks 1 and 2 |
| REQ SHAPE-05 | points plus innerRatio persisted; render/bbox from points alone | Covered by Tasks 1 and 2 |
| REQ SHAPE-06 | canonical round-trip and missing innerRatio rejection | Covered by Tasks 1 and 2 |
| RESEARCH | Use TriangleShape point-list JSON and bounds pattern | Covered by Task 2 |
| RESEARCH | Use PentagonShape polar generation pattern | Covered by Task 2 |
| RESEARCH | Use GeometryJson helpers; avoid hand-built JSON | Covered by Task 2 |
| RESEARCH | Do not modify DefaultShapes registration | Covered by Tasks 1 and 3 |
| RESEARCH | No package installs | Covered by threat model T-BC13-SC |
| STATE | Phase 13 pure C#, zero database, additive, tests instantiate Star5Shape directly | Covered by all tasks |
| DECISIONS | D-60 geometry jsonb is per type and validated in C# | Covered by Task 2 TryParseGeometry |
| DECISIONS | D-67 bbox cache is computed from app geometry and not source of truth | Covered by Task 2 BoundsOf |
| DECISIONS | D-65 adding a figure type must not require schema change | Covered by zero database scope in Tasks 2 and 3 |
| CONTEXT | No CONTEXT.md; user selected continue without it | Exclusion: no locked D-XX phase-context decisions exist for Phase 13 |
| OUT OF SCOPE | catalog seed, database schema, toolbar, renderer switch, persistence, sync, JavaScript preview, decision docs | Excluded by Task 3 boundary action and success criteria |
</source_audit>

<output>
Create `.planning/phases/BC-13-star-shape-core/13-01-SUMMARY.md` when done.
</output>
