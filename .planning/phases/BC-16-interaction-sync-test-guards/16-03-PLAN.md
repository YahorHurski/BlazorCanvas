---
phase: BC-16-interaction-sync-test-guards
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
  - tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs
  - tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs
  - tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "A drift guard fails if star preview geometry or the inner-ratio constant reappears in Home.razor.js, and pins Star5Shape.DefaultInnerRatio as the single C# production source of the ratio (TEST-04 drift guard, D-70/D-71)."
    - "The whole-table bbox agreement scan includes a seeded star5 row and asserts its stored bbox_* equals a fresh Star5Shape.BoundsOf recompute of the stored points, with a deliberate star-row corruption probe proving the comparison bites (TEST-04 bbox agreement)."
    - "Degenerate star geometry — zero width or zero height — is rejected at the gateway boundary, while a positive one-unit sliver is accepted (TEST-04 boundary: the exact zero-extent threshold and one step either side)."
    - "Malformed star geometry — missing innerRatio, wrong point count, non-finite or non-positive ratio — is rejected at both the unit (Star5Shape) and gateway (FigureInputGateway) boundary rather than persisted (TEST-04 malformed rejection)."
    - "The inner-ratio constant is exercised as a float, and the bbox recompute uses exact equality over the stored point doubles, so precision/tie-breaking is pinned to the stored points, not a re-derived formula (TEST-04 precision)."
  artifacts:
    - path: "tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs"
      provides: "JS-vs-C# drift guard proving Home.razor.js owns no star preview geometry and pinning the single C# inner-ratio source."
    - path: "tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs"
      provides: "Star5 row seeded into the whole-table bbox agreement scan plus a star-row corruption probe."
    - path: "tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs"
      provides: "Gateway-boundary star5 degenerate and malformed rejection, valid star5 acceptance, and gesture parity cases."
    - path: "tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs"
      provides: "Explicit unit-boundary zero-extent-vs-sliver threshold assertion tied to TEST-04."
  key_links:
    - from: "src/BlazorCanvas/Components/Pages/Home.razor.js"
      to: "src/BlazorCanvas/Shapes/Star5Shape.cs"
      via: "The drift guard proves the inner-ratio constant lives only in Star5Shape, never in the JS lifecycle helper."
      pattern: "Star5Shape.DefaultInnerRatio"
    - from: "src/BlazorCanvas/Shapes/Star5Shape.cs"
      to: "tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs"
      via: "BoundsOf(points) is recomputed against every stored star row's bbox_* by the whole-table scan."
      pattern: "definition.BoundsOf(geometry"
---

<objective>
Pin this milestone's three silent-failure guards for the star: preview geometry must not drift back into Home.razor.js, every stored star bbox_* must agree with a fresh points recompute, and degenerate/malformed star geometry must be rejected at both the unit and gateway boundary.

Purpose: TEST-04 requires guards for the milestone's silent failure modes. Phase 15 moved visible preview geometry into DrawingPreviewSession/FigureShape and left Home.razor.js lifecycle-only; the bbox cache and validation gateway already guard the four shapes. This plan extends those existing guards to cover star5 without forking them.
Output: An extended JS-vs-C# drift guard, a star5 row folded into the whole-table bbox agreement scan, and star5 degenerate/malformed rejection at the gateway and unit boundaries.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-03-SUMMARY.md
@docs/DECISIONS.md
@src/BlazorCanvas/Components/Pages/Home.razor.js
@src/BlazorCanvas/Shapes/Star5Shape.cs
@src/BlazorCanvas/Shapes/Star5Geometry.cs
@src/BlazorCanvas/Shapes/FigureInputGateway.cs
@tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
@tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs
@tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs
@tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
</context>

<tasks>

<!-- planner-discipline-allow: 0.382 -->

<task type="auto" tdd="true">
  <name>Task 1: Extend the JS-vs-C# drift guard so star preview geometry cannot return to Home.razor.js</name>
  <files>tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/Home.razor.js
    - src/BlazorCanvas/Shapes/Star5Shape.cs
    - tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs
  </read_first>
  <behavior>
    - The drift guard reads Home.razor.js from the repository root and fails if it contains any star inner-ratio numeric literal, trigonometric preview math, SVG element creation, or point-list attribute writing (visible preview geometry drifting back into JS).
    - The drift guard asserts Home.razor.js keeps only lifecycle behavior: pointer capture, stale-preview cleanup, and the data-local-drawing-preview marker.
    - The guard pins Star5Shape.DefaultInnerRatio as the sole production source of the inner ratio, asserting its value equals the locked constant and that the JS helper does not restate it.
  </behavior>
  <action>Strengthen HomePreviewSourceTests for the TEST-04 drift guard, citing TEST-04 and D-70/D-71. Keep the existing negative assertions that Home.razor.js contains no star5 branch, no createElementNS, no points attribute writing, and no Math.cos/Math.sin, and add an assertion that the JS source contains no inner-ratio numeric literal so a reintroduced star formula fails the guard. Add a C# single-source assertion that reads Star5Shape.DefaultInnerRatio and confirms it equals the locked value, so the guard fails if the constant is deleted or the JS helper restates it. Reference the constant by name in the test rather than duplicating the number as a second literal source. Do not add Playwright, bUnit, a JS test framework, or a package install, and do not change Home.razor.js production behavior.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~HomePreviewSourceTests"</automated>
  </verify>
  <done>The drift guard fails if star preview geometry or the inner-ratio constant reappears in Home.razor.js, and pins Star5Shape.DefaultInnerRatio as the single C# source of the ratio.</done>
  <acceptance_criteria>
    - HomePreviewSourceTests pass.
    - The guard asserts Home.razor.js owns no star inner-ratio literal or preview geometry and references Star5Shape.DefaultInnerRatio by name as the single source.
    - No production Home.razor.js change is introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold a star5 row into the whole-table bbox agreement scan</name>
  <files>tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Shapes/Star5Shape.cs
    - src/BlazorCanvas/Shapes/FigureInputGateway.cs
    - src/BlazorCanvas/Data/V11/FigureRepository.cs
    - tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs
  </read_first>
  <behavior>
    - The representative seeded population includes a star5 row inserted through the gateway and repository so the whole-table scan visits a real star row.
    - The existing whole-table agreement scan recomputes bbox_* for the star row through the registered Star5Shape.BoundsOf over the stored points and finds no mismatch, using the same exact-equality comparison already used for the four shapes.
    - A deliberate corruption of a star row's bbox column is detected by the same FindMismatches probe, proving the star row is genuinely covered rather than skipped.
  </behavior>
  <action>Extend BboxCacheAgreementTests for TEST-04 by seeding a star5 row into SeedRepresentativePopulationAsync using the existing CreateInput gateway helper and repository InsertAsync, and expose that row on the SeededPopulation record so tests can target it. Reuse the existing InspectTableAsync whole-table scan and DefaultShapes.CreateRegistry (which already registers star5) so no separate star scanner is forked; the scan already recomputes BoundsOf per row. Add assertions that the seeded star row has no mismatch and that a deliberate bbox column corruption on the star row is caught by FindMismatchesAsync inside a rolled-back transaction, mirroring the existing corruption probe. Cite D-70/D-71 and TEST-04 in the new assertions. Do not modify production BoundsOf, the repository, schema, or migrations.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~BboxCacheAgreementTests"</automated>
  </verify>
  <done>A seeded star5 row participates in the whole-table bbox agreement scan, agrees exactly with a fresh Star5Shape.BoundsOf recompute, and its deliberate corruption is detected by the same probe.</done>
  <acceptance_criteria>
    - BboxCacheAgreementTests pass against PostgreSQL.
    - The seeded population inserts a star5 row and asserts its stored bbox_* agrees with a fresh BoundsOf recompute.
    - A star-row corruption probe fails the comparison, proving coverage; no production geometry or repository code is changed.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject degenerate and malformed star geometry at the gateway and unit boundary</name>
  <files>tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs, tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Shapes/FigureInputGateway.cs
    - src/BlazorCanvas/Shapes/Star5Shape.cs
    - tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs
    - tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
  </read_first>
  <behavior>
    - The gateway rejects star5 payloads that are malformed: missing innerRatio, wrong point count (nine or eleven points), non-array points, non-finite coordinates, and non-positive or non-finite innerRatio.
    - The gateway rejects degenerate star5 geometry whose points span zero width or zero height, and accepts a valid non-degenerate star5 payload and a positive one-unit sliver so the exact zero-extent threshold and one step either side are both pinned.
    - The gateway gesture path for star5 produces the same canonical geometry, style, and bounds as validating the equivalent star5 JSON payload (parity with the shared validation path).
    - The unit boundary explicitly pins the exact zero-extent-vs-sliver threshold for Star5Shape.IsDrawable, so both the unit and gateway boundaries reject degenerate star geometry.
  </behavior>
  <action>Extend FigureInputGatewayTests for TEST-04 by adding star5 cases: hostile/malformed star geometry into the HostileGeometryCases source (missing innerRatio, nine points, eleven points, non-array points, non-finite coordinate, zero and negative innerRatio, and a zero-extent degenerate point list), a valid star5 entry into ValidGeometryCases, and a star5 gesture entry into GestureCases so TryValidateGesture parity is covered. Add or extend a boundary assertion that a positive one-unit-extent star5 payload is accepted while a zero-width and a zero-height star5 payload are rejected, citing D-70/D-71 and TEST-04. In Star5ShapeTests, add one explicit test that pins the exact zero-extent-vs-sliver IsDrawable threshold (zero width rejected, zero height rejected, one-unit positive sliver accepted) and cites TEST-04, so the unit boundary is named alongside the gateway boundary. Do not modify FigureInputGateway or Star5Shape production code.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~FigureInputGatewayTests|FullyQualifiedName~Star5ShapeTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Degenerate and malformed star geometry is rejected at both the gateway and unit boundary, a valid star and a positive sliver are accepted, and gesture-vs-JSON parity holds.</done>
  <acceptance_criteria>
    - FigureInputGatewayTests and Star5ShapeTests pass, and the full solution tests pass.
    - The gateway rejects star5 missing-innerRatio, wrong-point-count, non-finite, and zero-extent payloads and accepts a valid star and a positive sliver.
    - The unit-boundary test names the exact zero-extent-vs-sliver threshold and cites TEST-04; no production shape or gateway code changes.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- tests/BlazorCanvas.Tests/Components/HomePreviewSourceTests.cs: extended TEST-04 drift guard asserting Home.razor.js owns no star preview geometry or inner-ratio literal and pinning Star5Shape.DefaultInnerRatio as the single source.
- tests/BlazorCanvas.Tests/Database/V11/BboxCacheAgreementTests.cs: a star5 row seeded into the whole-table agreement scan plus a star-row corruption probe.
- tests/BlazorCanvas.Tests/Shapes/FigureInputGatewayTests.cs: gateway-boundary star5 malformed/degenerate rejection, valid star acceptance, sliver boundary, and gesture parity cases.
- tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs: explicit unit-boundary zero-extent-vs-sliver threshold assertion tied to TEST-04.
- No production class, method, constant, field, schema, or external API symbol is created by this plan; it extends existing guards to cover star5.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-JS-to-server-preview | Home.razor.js is lifecycle-only; visible preview geometry must stay in the C# registry path. |
| client-supplied-geometry-to-gateway | Star geometry JSON and gestures cross FigureInputGateway before any persistence. |
| stored-bbox-cache-to-clamp | The bbox_* cache read by the edge clamp must equal a fresh recompute from the stored points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Tampering | Home.razor.js preview helper | medium | mitigate | Drift guard fails if star preview geometry or the inner-ratio constant reappears in the JS helper, keeping the C# registry the single source of visible geometry. |
| T-16-10 | Tampering | FigureInputGateway star5 validation | high | mitigate | Gateway-boundary tests prove malformed and degenerate star payloads are rejected before persistence, so the client cannot write a star the renderer or clamp would mishandle. |
| T-16-11 | Information Disclosure | bbox_* cache for star rows | medium | mitigate | Whole-table agreement scan proves every stored star bbox equals a fresh points recompute, so a drifted cache cannot silently clamp against the wrong edge. |
| T-16-12 | Denial of Service | Star5Shape.IsDrawable zero-extent boundary | low | mitigate | Unit and gateway boundary tests pin the exact zero-extent threshold so a degenerate star is rejected rather than persisted and repeatedly re-rendered. |
| T-16-SC | Tampering | package installs | low | accept | No npm, pip, cargo, or NuGet package install is planned; existing xUnit and PostgreSQL fixture dependencies are reused. |
</threat_model>

<verification>
Run the task verification commands. API Coverage Decision Checkpoint: detected false — this plan uses Blazor Server, in-process shape/gateway code, and PostgreSQL only, with no external API/SDK integration; do not create COVERAGE.md. Assumption-Delta Architecture Checkpoint: detected false — star5 is an existing shape; this plan extends existing guards with no model-shape shift. Schema Push Detection Gate: not applicable — no schema or migration files are modified; the bbox test reuses the existing fixture schema.
</verification>

<success_criteria>
- TEST-04 drift guard: a test fails if visible preview geometry or the inner-ratio constant drifts back into Home.razor.js, and the single C# source is pinned.
- TEST-04 bbox agreement: a seeded star5 row's stored bbox_* agrees exactly with a fresh Star5Shape.BoundsOf recompute in the whole-table scan, with a corruption probe proving coverage.
- TEST-04 rejection: degenerate (zero width or height) and malformed (missing innerRatio, wrong point count) star geometry is rejected at both the unit and gateway boundary, while a valid star and a positive sliver are accepted.
- The full solution test suite passes.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | This milestone's silent failure modes are pinned by tests | 16-03 | COVERED | Drift, bbox agreement, and degenerate/malformed rejection guards extended to star5.
REQ | TEST-04 | Preview geometry stays out of Home.razor.js; bbox_* agrees for star rows; degenerate/malformed star geometry rejected | 16-03 | COVERED | Drift guard (Task 1), bbox agreement (Task 2), gateway+unit rejection (Task 3).
RESEARCH | - | User explicitly skipped research | 16-03 | COVERED | No RESEARCH.md is required or referenced.
CONTEXT | - | User explicitly skipped discuss-phase | 16-03 | COVERED | Locked decisions taken from docs/DECISIONS.md (D-70/D-71) and STATE.md; guards reuse the v1.11 bbox-agreement pattern.
</source_audit>

<output>
Create `.planning/phases/BC-16-interaction-sync-test-guards/16-03-SUMMARY.md` when done.
</output>
