---
phase: BC-14-catalog-seed-toolbar-decisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlazorCanvas/Shapes/DefaultShapes.cs
  - src/BlazorCanvas/Data/V11/Transition/V11Schema.cs
  - src/BlazorCanvas/Data/V11/V11Cutover.cs
  - tests/BlazorCanvas.Tests/Shapes/DefaultShapesTests.cs
  - tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
  - tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs
autonomous: true
requirements:
  - MODEL-08
must_haves:
  truths:
    - Star5Shape is registered after triangle in DefaultShapes.CreateRegistry().
    - Startup converges an already completed public catalog to include star5.
    - Repeated startup seeding leaves exactly one star5 row.
  artifacts:
    - src/BlazorCanvas/Shapes/DefaultShapes.cs registers Star5Shape.
    - src/BlazorCanvas/Data/V11/Transition/V11Schema.cs supports closed schema-qualified seeding.
    - src/BlazorCanvas/Data/V11/V11Cutover.cs seeds completed public catalogs before returning.
    - tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs proves completed-catalog idempotency.
  key_links:
    - DefaultShapes.CreateRegistry().Names drives both seed order and accepted figure type names.
    - V11Cutover.EnsureAsync must seed public.figure_types when CatalogState.Completed is detected.
---

<objective>
Register the Phase 13 star shape and make the figure_types catalog converge from the registry on every startup.

Purpose: MODEL-08 requires a newly registered shape to become writable on an existing completed database without a migration or manual SQL.
Output: Registry exposure, schema-qualified idempotent seed logic, and database tests proving one star5 row after repeated completed-catalog startups.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-RESEARCH.md
@.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-PATTERNS.md
@.planning/phases/BC-13-star-shape-core/13-01-SUMMARY.md
@src/BlazorCanvas/Shapes/DefaultShapes.cs
@src/BlazorCanvas/Shapes/ShapeRegistry.cs
@src/BlazorCanvas/Shapes/Star5Shape.cs
@src/BlazorCanvas/Data/V11/Transition/V11Schema.cs
@src/BlazorCanvas/Data/V11/V11Cutover.cs
@tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs
@tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs
@tests/BlazorCanvas.Tests/Shapes/DefaultShapesTests.cs
@tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify star registry and completed-catalog seed convergence</name>
  <files>tests/BlazorCanvas.Tests/Shapes/DefaultShapesTests.cs, tests/BlazorCanvas.Tests/Shapes/Star5ShapeTests.cs, tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs</files>
  <behavior>
    - DefaultShapes.CreateRegistry().Names returns line, rectangle, circle, triangle, star5 in that order.
    - Registry-wide shape contract tests include a star5 corner-to-corner gesture fixture.
    - The temporary Phase 13 no-registration fence is removed or inverted so it expects star5 in the default registry.
    - Additive and fresh-users-only cutover tests assert five figure_types rows.
    - A completed public catalog missing star5 gains star5 when V11Cutover.EnsureAsync runs.
    - Two consecutive V11Cutover.EnsureAsync calls against a completed public catalog leave count(*) where name = 'star5' equal to one.
  </behavior>
  <action>Write the failing tests first for MODEL-08. Update the two existing V11CutoverTests catalog-size assertions from 4 to 5. Replace the completed-catalog exact snapshot expectation with a narrower convergence/idempotency proof: prepare a completed public catalog, remove or omit only the star5 catalog row within the scratch database setup/test, call EnsureAsync with DefaultShapes.CreateRegistry(), assert public.figure_types contains star5, call EnsureAsync again, and assert exactly one star5 row. Do not add a migration, manual deployment SQL, or a schema push step.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~DefaultShapesTests|FullyQualifiedName~Star5ShapeTests|FullyQualifiedName~V11CutoverTests"</automated>
  </verify>
  <done>Tests fail before production changes for missing registration/completed-catalog seeding, then define all MODEL-08 acceptance cases.</done>
  <acceptance_criteria>MODEL-08 has executable regression coverage for registry exposure, completed public catalog convergence, and repeated-startup idempotency.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Star5Shape and seed closed schema targets</name>
  <files>src/BlazorCanvas/Shapes/DefaultShapes.cs, src/BlazorCanvas/Data/V11/Transition/V11Schema.cs, src/BlazorCanvas/Data/V11/V11Cutover.cs</files>
  <behavior>
    - DefaultShapes registers Star5Shape immediately after TriangleShape.
    - V11Schema seeding can target v11.figure_types and public.figure_types using code-owned schema choices only.
    - Figure type names are inserted through Npgsql parameters, never concatenated into SQL values.
    - CatalogState.Completed seeds public.figure_types inside the startup transaction, commits, and returns.
    - Additive, fresh, and legacy cutover paths keep their existing v11 schema seed semantics.
  </behavior>
  <action>Implement the production side for MODEL-08. Add Star5Shape to DefaultShapes.CreateRegistry() after TriangleShape. Generalize V11Schema.SeedFigureTypesAsync or add a public-specific helper so the SQL target schema is selected only from closed code-owned values such as v11 and public; keep registry names bound through @name. In V11Cutover.EnsureAsync, replace the completed-state rollback/return with seeding public.figure_types from the registry, committing the transaction, and returning. Preserve the advisory lock, transaction boundary, CatalogState.Invalid refusal, and all existing cutover stages. Do not implement star drawing, preview, renderer, persistence write path, selection, drag, delete, or sync behavior in this plan.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~DefaultShapesTests|FullyQualifiedName~Star5ShapeTests|FullyQualifiedName~V11CutoverTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Registry names include star5, completed startup inserts missing star5 into public.figure_types, and two startup runs leave one star5 row.</done>
  <acceptance_criteria>Success criteria 1, 2, and 3 are satisfied with no migration and no manual SQL deployment step.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry-to-database | Application-owned registry names are written into PostgreSQL figure_types during startup. |
| startup-to-public-schema | Startup cutover code mutates the existing public catalog when CatalogState.Completed is detected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | V11Schema.SeedFigureTypesAsync | high | mitigate | Bind each figure type name through an Npgsql @name parameter and keep ON CONFLICT (name) DO NOTHING for idempotency. |
| T-14-02 | Tampering | schema-qualified seed target | high | mitigate | Permit only code-owned schema target values, public and v11; never accept schema text from configuration, request data, registry entries, or tests. |
| T-14-03 | Denial of Service | V11Cutover.EnsureAsync completed path | medium | mitigate | Keep the advisory transaction lock while seeding the completed catalog so concurrent startups serialize and converge. |
| T-14-SC | Tampering | package installs | low | accept | No npm, pip, cargo, or NuGet package install is planned; existing Npgsql/xUnit dependencies are reused. |
</threat_model>

<verification>
Run the focused test command, then the full solution test command in Task 2. API Coverage Decision Checkpoint: not applicable because Phase 14 adds no external API integration and no API connector surface; do not create COVERAGE.md. Schema Push Detection Gate: not applicable because no Payload/Prisma/Drizzle/Supabase/TypeORM schema files are touched and this plan adds no migration.
</verification>

<success_criteria>
- DefaultShapes.CreateRegistry() exposes star5 in canonical seed order after triangle.
- The two V11CutoverTests catalog-count assertions expect five figure_types rows.
- A completed public catalog missing star5 gains it on startup.
- Two consecutive startup runs leave exactly one star5 row.
- Full solution tests pass without migration or manual SQL steps.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Star5Shape joins registry and figure_types seeds star5 idempotently on startup | 14-01 | COVERED | Registry plus completed-catalog seed convergence.
REQ | MODEL-08 | Seed figure_types from registry at every startup with no migration/manual SQL | 14-01 | COVERED | Tests include completed public catalog and two-run idempotency.
RESEARCH | - | Register Star5Shape and update hard-coded catalog size tests | 14-01 | COVERED | DefaultShapes and V11CutoverTests included.
RESEARCH | - | Use parameterized ON CONFLICT seed with closed schema targets | 14-01 | COVERED | Threat model and Task 2 require both.
CONTEXT | - | No CONTEXT.md exists; user chose to continue without context | 14-01 | COVERED | No D-NN phase-context decisions are invented.
</source_audit>

<output>
Create `.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-01-SUMMARY.md` when done.
</output>
