---
phase: 10-geometry-draw-drag-sync-rework-no-edge-clamp-regression
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - tests/BlazorCanvas.Tests/Geometry/NormalisationTests.cs
  - tests/BlazorCanvas.Tests/Geometry/GeometryCodecTests.cs
  - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
  - tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs
  - tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs
  - tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs
  - tests/BlazorCanvas.Tests/Database/TypeWhitelistAndPersistenceTests.cs
  - tests/BlazorCanvas.Tests/UnitTest1.cs
autonomous: true
requirements: [TEST-02, STOR-02, STOR-03]
user_setup: []

must_haves:
  truths:
    - "The line-normalisation landmine test carries over unchanged in intent and is now asserted all the way to storage: a diagonal drawn up-and-right stores as an anchor at the first endpoint with a NEGATIVE dy, and never as the opposite diagonal with a positive dy (D-41, TEST-02)."
    - "TEST-01's circle inscribed-square round-trip is re-expressed as a geometry {r} assertion: a circle drawn centre-out stores its centre as the anchor and its radius as {r}, and decodes back exactly (TEST-02, D-49 test 2)."
    - "The test suite asserts the exact serialised geometry literals for every type, so a serialiser change, a member rename, or added whitespace cannot pass silently (TEST-02 precision)."
    - "Every geometry member the suite asserts is integer-valued — no geometry JSON anywhere in the suite contains a decimal point (D-20, TEST-02 precision)."
    - "The test suite contains, for each of the four types, an assertion exactly at the zero-extent threshold and one at a one-pixel extent (TEST-02 boundary; the assertions themselves live in MinSizeGuardTests from 10-01)."
    - "Schema-shape assertions assert the anchor+geometry model against the LIVE PostgreSQL catalog: a uuid id defaulting to gen_random_uuid(), integer x and y, jsonb geometry, numeric z, exactly seven columns, no created_at, the single figures_type_is_known CHECK, and the (user_id, z) index (STOR-01 carried, TEST-02)."
    - "No CHECK constraint on the live figures table references geometry or any retired bounding-box column name; the only remaining CHECK is the type whitelist (D-59 item 9, D-46)."
    - "The database test helpers speak the anchor+geometry model natively — a raw insert takes an anchor and a geometry JSON string, not four bounding-box integers (TEST-02)."
    - "The test class that used to prove the C# guard mirrored three database CHECKs is renamed and reduced to what it still proves — the type whitelist round-trip and figure persistence across a container teardown — because D-59 deleted the CHECKs it mirrored and STOR-03 moved that proof code-side (TEST-02)."
    - "The empty template test stub is removed; every test in the suite asserts something."
  artifacts:
    - "Reworked tests/BlazorCanvas.Tests/Geometry/NormalisationTests.cs — the landmine asserted through to stored geometry."
    - "Reworked tests/BlazorCanvas.Tests/Geometry/GeometryCodecTests.cs — exact literals, integer-only members, off-canvas anchors."
    - "Reworked tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs — anchor+geometry raw-insert helper."
    - "Reworked tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs and SchemaShapeTests.cs."
    - "tests/BlazorCanvas.Tests/Database/TypeWhitelistAndPersistenceTests.cs, replacing GuardMirrorsChecksTests.cs."
    - "Deleted: tests/BlazorCanvas.Tests/UnitTest1.cs."
  key_links:
    - "The landmine assertion now spans DrawGesture, Normalisation and GeometryCodec in one test, so no single-class refactor can break the diagonal without a red test."
    - "SchemaShapeTests reads PostgreSQL's own catalog rather than the EF model, so it proves what the Phase-9 migration actually did to a live database rather than what the C# model claims."
    - "The renamed persistence test still exercises a real container teardown, which is the only place in the suite that proves the named volume survives — it must not be lost in the rename."
  prohibitions:
    - "MUST NOT satisfy this test rework by deleting or weakening a test whose intent still holds. TEST-02 says the edge-clamp tests are removed OR REPURPOSED, and every deletion in this phase must be one of: (a) the tested behaviour no longer exists by an explicit decision, named by decision id in the commit or a comment; or (b) the assertion is carried over somewhere else, named. Turning a red test green by narrowing its assertion, replacing a specific expected value with a looser predicate, or quietly dropping a case from a theory's data set converts a real regression into a silent one — which is precisely the failure mode D-49 created this suite to prevent."
---

<objective>
Finish TEST-02: bring the parts of the suite that still speak the retired bounding-box model onto
anchor+geometry, re-express TEST-01's three silent-failure tests, and retire the test class that used to
mirror database CHECK constraints D-59 deleted.

Two of TEST-01's three originals were already handled: the clamp-maths tests went with the clamp in
10-01 and 10-02, and the circle round-trip was re-expressed as a `{r}` assertion in 10-01. This plan
carries the third — the line-normalisation landmine — and asserts it end to end, from the gesture
through normalisation to the exact stored JSON.

Purpose: TEST-02, and a suite in which nothing asserts against a model that no longer exists.
Output: reworked geometry and database tests, a renamed persistence test class, no template stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-09-schema-entity-data-preserving-migration/09-05-SUMMARY.md
@tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
@tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs
@tests/BlazorCanvas.Tests/Geometry/NormalisationTests.cs
</context>

<operational_note>
Every `Database`-collection test in this plan needs the Compose PostgreSQL container up
(`docker compose up -d --wait` from the repository root, host port 5433 per the recorded D-27
deviation) and the suite pointed at the migrated Phase-9 database:

`BLAZORCANVAS_TEST_CONNECTION=Host=localhost;Port=5433;Database=canvas_phase09;Username=postgres;Password=postgres`

The repository default in `DatabaseFixture` still names `canvas`, which in this workspace holds an
unrelated future schema (see `09-04-SUMMARY.md`). Do NOT change that default to a workspace-specific
name; set the environment variable instead.

`FiguresWrittenViaEfCore_SurviveContainerTeardown` tears the container down and back up as part of its
assertion. It is slow and it will interrupt any other process using that database — run it deliberately,
not concurrently with anything else.
</operational_note>

<planner_assumptions>
- **PA-12 — `GuardMirrorsChecksTests` is renamed rather than left in place.** No CONTEXT.md pinned this.
  The name asserts a relationship — the C# guard mirrors three database CHECKs — that D-59 deliberately
  destroyed, and a misleading test name is a trap for the next reader. The two live tests inside it are
  unrelated to each other and to the old name; the new name says what they actually prove.
- **PA-13 — `DatabaseFixture`'s raw-insert helper takes an anchor and a geometry string.** The current
  four-integer signature is a bounding-box artefact that quietly re-encodes through `GeometryCodec`,
  which defeats the helper's whole purpose: it exists so a test can hand PostgreSQL a value the C# path
  would never produce.
- **PA-6 — no component-test package is added.** D-49 keeps the test project deliberately narrow, and
  introducing a Razor component-test harness for this phase would be a new dependency with its own
  legitimacy question. The component-level behaviours are carried to 10-06's live checkpoint instead.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The line landmine, asserted through to stored geometry</name>
  <files>tests/BlazorCanvas.Tests/Geometry/NormalisationTests.cs, tests/BlazorCanvas.Tests/Geometry/GeometryCodecTests.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Geometry/NormalisationTests.cs (the landmine test, the per-type cases, and the CanvasBoundsTests and FigureTypeNamesTests classes that also live in this file)
    - tests/BlazorCanvas.Tests/Geometry/GeometryCodecTests.cs (the round-trip theory and the four exact-literal tests)
    - src/BlazorCanvas/Geometry/Normalisation.cs (unchanged this phase — the swap-the-whole-pair rule)
    - src/BlazorCanvas/Geometry/DrawGesture.cs and GeometryCodec.cs (post-10-01)
    - docs/DECISIONS.md — D-41 including its v1.11 re-expression banner, D-49 (the three silent-failure tests), D-20
  </read_first>
  <action>
    Keep every existing `NormalisationTests` case: `Normalisation` itself is unchanged by this phase and
    D-41's landmine carries over verbatim in intent. Do not weaken or delete any of them.

    Add the end-to-end landmine assertion TEST-02 asks for. Drive a line gesture drawn up-and-right —
    press at (0,100), release at (100,0) — through `DrawGesture.Build` and then `GeometryCodec.Encode`,
    and assert the stored anchor is (0,100) and the stored geometry is the exact literal with a dx of
    100 and a dy of MINUS 100. Assert explicitly that the stored form is NOT the anchor (0,0) with a
    positive dy — the axis-sorted opposite diagonal. Add the mirror case for a line drawn the other way,
    press at (100,0) and release at (0,100), asserting it normalises to the same stored form. Add a
    third case for a down-and-right diagonal so a sign error in either direction fails.

    Leave the `CanvasBoundsTests` and `FigureTypeNamesTests` classes in that file alone.

    In `GeometryCodecTests`, extend the round-trip theory's data with anchors and extents that are now
    legal under STOR-04: a negative anchor, an anchor past 1472 on x, an anchor past 828 on y, and a
    line whose delta carries it off-canvas. Add a theory asserting that for every type the serialised
    geometry string contains no decimal point and no exponent character — every member is an integer
    (D-20). Keep the four exact-literal tests and, if any type lacks one, add it so all four are pinned:
    the rectangle and triangle by their w and h members, the circle by its r member, the line by its dx
    and dy members.
  </action>
  <acceptance_criteria>
    - `dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~NormalisationTests|FullyQualifiedName~GeometryCodecTests"` exits 0 with zero failed tests.
    - `NormalisationTests` contains an assertion that a line gesture from (0,100) to (100,0) stores an anchor of (0,100) and a geometry whose dy member is `-100`.
    - That same test asserts the stored geometry is not the positive-dy, origin-anchored form.
    - `NormalisationTests` still contains every case it had before this plan; the file's test count does not decrease.
    - `GeometryCodecTests` round-trips at least one case with a negative anchor and at least one with an anchor beyond 1472 or 828.
    - `GeometryCodecTests` contains a theory asserting no serialised geometry string contains a `.` character.
    - All four types have an exact-literal assertion on their serialised geometry.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~NormalisationTests|FullyQualifiedName~GeometryCodecTests"</automated>
  </verify>
  <done>The D-41 landmine is pinned from gesture to stored JSON in both directions, off-canvas anchors round-trip, and every stored geometry member is provably an integer.</done>
</task>

<task type="auto">
  <name>Task 2: Database helpers and schema assertions speak anchor+geometry</name>
  <files>tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs, tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs, tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs (TryInsertRawFigureAsync, TryInsertFigureAsync, the private encode-for-raw-insert switch, the connection-string resolution and the InitializeAsync failure message)
    - tests/BlazorCanvas.Tests/Database/CheckConstraintTests.cs (the rejected-type cases, the accepted-line cases and the well-formed-figure cases)
    - tests/BlazorCanvas.Tests/Database/SchemaShapeTests.cs (the twelve live-catalog assertions added in 09-05)
    - src/BlazorCanvas/Data/CanvasDbContext.cs (the single figures_type_is_known CHECK, the jsonb mapping, the (user_id, z) index and the table comment)
    - docs/DECISIONS.md — §THE SCHEMA, D-46, D-59 items 8 and 9, D-42
  </read_first>
  <action>
    Rework `DatabaseFixture`'s raw-insert helper to the model's own shape: it takes a raw `type` literal,
    an integer anchor pair, and a geometry JSON string, and inserts them verbatim through the DbContext
    inside a transaction that is always rolled back. Delete the private four-integer encode switch — its
    only job was translating a bounding box, and a helper that re-encodes through `GeometryCodec` cannot
    hand PostgreSQL a value the C# path would never produce, which is the whole reason the helper exists.
    Keep the convenience overload used by the type-whitelist test, changing it to take a type and a
    geometry string. Update the class doc: the geometry CHECKs it describes are gone (D-59 item 9), the
    `type` whitelist is what remains, and the crefs to the guard and the normaliser must still resolve.
    Leave the connection-string resolution and the loud `InitializeAsync` failure message alone.

    Update `CheckConstraintTests` to the new helper. Keep the two rejection cases — an unknown type
    literal and a PascalCase literal, the accidental `Enum.ToString()` — since the whitelist CHECK is
    deliberately retained through and after the backfill (D-59 item 8). Keep the accepted cases for a
    horizontal line, a vertical line and an up-and-right diagonal, expressing each as an anchor plus a
    `{dx,dy}` geometry, and keep the well-formed circle, rectangle and triangle cases as an anchor plus
    their respective geometry. Rewrite the class doc: this file no longer proves "the database refuses
    an illegal geometry" — D-59 removed those CHECKs — it proves the type whitelist still discriminates
    and that well-formed rows insert.

    In `SchemaShapeTests`, verify the twelve assertions still hold against the live catalog and correct
    the class doc's stale reference to a Phase-1 migration. Add one assertion that no CHECK constraint on
    `figures` mentions any retired bounding-box column name, alongside the existing one that no CHECK
    mentions geometry. Add one assertion that the `figures` table's row for the `z` column reports
    `numeric` and is NOT NULL, and one that the `geometry` column is NOT NULL — the model's two
    strongest remaining database-level guarantees.
  </action>
  <acceptance_criteria>
    - `dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~CheckConstraintTests|FullyQualifiedName~SchemaShapeTests"` exits 0 with zero failed tests, run with the container up and `BLAZORCANVAS_TEST_CONNECTION` set per the operational note.
    - `DatabaseFixture`'s raw-insert helper takes a type literal, two integers and a string, and no `Box`.
    - `grep -c 'x1' tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs` returns `0`.
    - `CheckConstraintTests` still asserts that the literal `oval` and the literal `Circle` are both rejected by `figures_type_is_known` with a check-violation SQL state.
    - `CheckConstraintTests` still asserts that a horizontal line, a vertical line and an up-and-right diagonal all INSERT successfully.
    - `SchemaShapeTests` asserts that the count of CHECK constraints on `figures` whose definition mentions a retired bounding-box column name is 0.
    - `SchemaShapeTests` asserts `geometry` and `z` are both NOT NULL.
    - `dotnet build BlazorCanvas.sln` exits 0 with `0 Warning(s)`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~CheckConstraintTests|FullyQualifiedName~SchemaShapeTests"</automated>
  </verify>
  <done>The database helpers insert an anchor and a geometry string directly, the constraint tests prove what the retained type whitelist still does, and the live-catalog assertions cover the full D-59 shape including the two NOT NULL guarantees.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the CHECK-mirroring test class, drop the stub, and prove the whole suite green</name>
  <files>tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs, tests/BlazorCanvas.Tests/Database/TypeWhitelistAndPersistenceTests.cs, tests/BlazorCanvas.Tests/UnitTest1.cs</files>
  <read_first>
    - tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs (the eight box fields, the dead Matrix member data, the two live tests, and the docker-compose teardown helpers)
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs (post-Task-2 helper signatures)
    - tests/BlazorCanvas.Tests/UnitTest1.cs (the empty template stub)
    - tests/BlazorCanvas.Tests/Geometry/MinSizeGuardTests.cs (where the per-type guard proof now lives, from 10-01)
    - docs/DECISIONS.md — D-50, D-59 item 9, D-27
  </read_first>
  <action>
    Move `GuardMirrorsChecksTests` to `TypeWhitelistAndPersistenceTests.cs` with a matching class name,
    keeping the `Database` collection attribute. Delete the eight boundary-probing box fields and the
    `Matrix` member data — the theory that consumed them was already removed in 09-05 and they now name
    constraints that no longer exist. Keep the well-formed constant the type-literal test needs, but
    express it as the anchor and geometry the new fixture helper takes.

    Keep both live tests exactly in intent. The type-literal round-trip proves the four literals
    `FigureTypeNames.ToDbValue` produces are exactly the four `figures_type_is_known` accepts (D-46), and
    the container-teardown test proves EF-written figures survive a `docker compose down` and back up on
    the named volume (D-27) — that is the only place in the suite proving volume persistence, so keep its
    helpers, its cleanup and its cascade-delete assertion intact.

    Rewrite the class doc. The old text calls this "the decisive test of the phase" because it ran the
    C# guard and three database CHECK constraints against a shared matrix. D-59 deleted those CHECKs and
    STOR-03 moved geometry well-formedness entirely into code, so the guard's proof now lives in
    `MinSizeGuardTests`. State that, and state what this file still proves.

    Delete `tests/BlazorCanvas.Tests/UnitTest1.cs` — an empty template stub that asserts nothing.

    Then run the whole suite and the whole build. This is the phase's regression gate: every requirement
    in Phase 10 ends here, and a green filtered run is not a substitute for a green full run.
  </action>
  <acceptance_criteria>
    - `dotnet build BlazorCanvas.sln` exits 0 and reports `0 Warning(s)` and `0 Error(s)`.
    - `dotnet test BlazorCanvas.sln --nologo` exits 0 with zero failed and zero skipped tests, run with the Compose container up and `BLAZORCANVAS_TEST_CONNECTION` set per the operational note.
    - `test ! -f tests/BlazorCanvas.Tests/Database/GuardMirrorsChecksTests.cs` succeeds.
    - `test ! -f tests/BlazorCanvas.Tests/UnitTest1.cs` succeeds.
    - `tests/BlazorCanvas.Tests/Database/TypeWhitelistAndPersistenceTests.cs` exists, carries the `Database` collection attribute, and contains both the type-literal round-trip test and the container-teardown persistence test.
    - The container-teardown test still asserts the inserted ids come back in the same order after the restart and still cleans up its user via cascade delete.
    - The migration round-trip test from 09-06 still passes untouched — `dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~MigrationRoundTrip"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test BlazorCanvas.sln --nologo</automated>
  </verify>
  <done>The CHECK-mirroring class is renamed to what it actually proves with its dead matrix removed, the template stub is gone, the Phase-9 migration proof still passes, and the full suite is green on a clean build.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces or reshapes the following symbols (new symbols are excluded from drift verification):

- `DatabaseFixture` raw-insert helper — signature changed to (type literal, anchor x, anchor y, geometry
  JSON string); its convenience overload changed to (FigureType, geometry JSON string); the private
  bounding-box encode switch deleted.
- `TypeWhitelistAndPersistenceTests` — new file and class name replacing `GuardMirrorsChecksTests`;
  retains the type-literal round-trip and the container-teardown persistence test.
- New test members: the end-to-end line-landmine assertions in `NormalisationTests`; the off-canvas
  round-trip cases and the integer-only-members theory in `GeometryCodecTests`; the retired-column-name
  CHECK assertion and the two NOT NULL assertions in `SchemaShapeTests`.
- Deleted: `GuardMirrorsChecksTests` (class and file), `UnitTest1` (class and file), and the eight
  boundary box fields plus the `Matrix` member data.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → live PostgreSQL | The suite connects with the Compose superuser credentials and can create, migrate and drop databases; `MigrationRoundTripTests` creates and drops disposable databases on the same server. |
| test process → Docker daemon | One test issues `docker compose down` and `up` against the repository's compose file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16 | Denial of service | container-teardown test | medium | mitigate | The test stops and restarts the shared PostgreSQL container, which would interrupt anything else using it, and a failure between down and up leaves it stopped. Mitigated by keeping the existing bring-back-up step and the thirty-second reachability wait, and by the operational note telling the operator to run it deliberately rather than concurrently. |
| T-10-17 | Tampering | test-suite integrity | high | mitigate | A rework that quietly narrows an assertion converts a real regression into a silent one — the exact failure mode D-49 created this suite to prevent. Mitigated by the prohibition above, by the acceptance criterion that `NormalisationTests`' test count does not decrease, and by requiring each deletion to be justified either by a named decision or by a named carry-over location. |
| T-10-18 | Elevation of privilege | test connection credentials | low | accept | The suite uses the local Compose superuser on a throwaway development database, per D-08's recorded posture for this project. No production credential exists. |
| T-10-19 | Information disclosure | hard-coded connection strings in test source | low | accept | The literals name a local container with well-known development credentials (D-27); the environment-variable override exists so no real credential is ever committed. |
| T-10-SC | Tampering | package installs | low | accept | No NuGet, npm, pip or cargo package is added in this phase — in particular, no component-test harness is introduced (PA-6, D-49). The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `dotnet build BlazorCanvas.sln` is clean: 0 warnings, 0 errors.
- `dotnet test BlazorCanvas.sln --nologo` is green with zero failed and zero skipped tests.
- `dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~MigrationRoundTrip"` still passes —
  Phase 9's MIG-02 proof is untouched by the whole of Phase 10.
- No source file under `src/` is modified by this plan.
</verification>

<success_criteria>
Nothing in the suite asserts against the retired bounding-box model; TEST-01's three silent-failure
tests are all accounted for (clamp maths retired with the clamp, circle round-trip re-expressed as a
`{r}` assertion, line landmine carried over and extended to storage); the schema assertions cover the
full D-59 shape against a live catalog; and `dotnet build` plus `dotnet test` are both green.
</success_criteria>

<output>
Create `.planning/phases/BC-10-geometry-draw-drag-sync-rework-no-edge-clamp-regression/10-05-SUMMARY.md` when done.
</output>
