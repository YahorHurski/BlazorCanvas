---
phase: BC-08-architecture-constraint-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DECISIONS.md
  - .planning/PROJECT.md
  - .planning/intel/constraints.md
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "The former application-authored-script restriction is retired, and current policy permits JavaScript or interop only when a later decision elects to use it."
    - "D-06, D-18, D-33, D-37, and D-57 retain their product rationale while naming MVP simplicity or their independent behavioural rationale instead of an active technology prohibition."
    - "The derived CONSTRAINT-env record agrees with the authoritative ADR and cannot reimpose the retired rule on future planning."
    - "The repository audit classifies each retained policy-family match as permissive, historical, or superseded, and the phase introduces no runtime, source, test, static-asset, interop, or JavaScript change."
    - "The unchanged solution builds and its full test suite passes."
  artifacts:
    - path: docs/DECISIONS.md
      provides: "Authoritative v1.1 amendment and corrected rationale at D-06, D-18, D-33, D-37, and D-57."
    - path: .planning/PROJECT.md
      provides: "Current-project summary that mirrors the permissive amendment without promising a future feature."
    - path: .planning/intel/constraints.md
      provides: "Derived CONSTRAINT-env wording that records the retirement and documentation-only scope fence."
  key_links:
    - from: .planning/intel/constraints.md
      to: docs/DECISIONS.md
      via: "CONSTRAINT-env cites the ADR as its source and describes the v1.1 retirement consistently."
      pattern: "MVP simplicity|retired|permissive"
    - from: .planning/PROJECT.md
      to: docs/DECISIONS.md
      via: "The project constraint and decision summaries mirror the five authoritative ADR motivations."
      pattern: "MVP simplicity"
---

<objective>
Complete ARCH-01 by reconciling the authoritative ADR, current project summary, and active derived constraint, then record reproducible evidence that the policy cleanup did not change the product surface.

Purpose: prevent a stale planning constraint from being mistaken for an active architectural prohibition while preserving explicitly labelled historical evidence.
Output: three consistent documentation artifacts plus a phase summary containing the audit, diff-scope, build, and test results.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/DECISIONS.md
@.planning/intel/constraints.md
@.planning/phases/BC-08-architecture-constraint-cleanup/08-RESEARCH.md
@.planning/phases/BC-07-selection-lifecycle-restyle/07-01-SUMMARY.md
@.planning/phases/BC-07-selection-lifecycle-restyle/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile active policy artifacts with the ADR amendment</name>
  <read_first>
    - docs/DECISIONS.md: v1.1 milestone note and D-06, D-18, D-33, D-37, and D-57 sections
    - .planning/PROJECT.md: Out of Scope, Constraints, and decision-table mirrors for the same five decisions
    - .planning/intel/constraints.md: v1.1 amendment banner and CONSTRAINT-env
    - .planning/REQUIREMENTS.md: ARCH-01 and its scope fences
  </read_first>
  <files>docs/DECISIONS.md, .planning/PROJECT.md, .planning/intel/constraints.md</files>
  <action>Use docs/DECISIONS.md as the authoritative record. Reconcile the current project summary and CONSTRAINT-env against it: describe the former prohibition as retired and the policy as permissive, preserve the actual product or geometry rationale of D-06, D-18, D-33, D-37, and D-57, and make MVP simplicity or the independently stated behaviour rationale their active motivation. Replace the derived environment constraint that presents the former rule as hard and load-bearing with a concise current statement covering retirement, the five corrected motivations, and the documentation-only boundary. Retain historical material only when its surrounding text explicitly identifies it as historical or superseded. Do not introduce a script, interop call, source/test/static-asset edit, runtime behaviour change, input gesture, or keyboard feature.</action>
  <verify>
    <automated>rg -n -i -C 2 'MVP simplicity|retired|permissive|allowed|D-06|D-18|D-33|D-37|D-57|CONSTRAINT-env' docs/DECISIONS.md .planning/PROJECT.md .planning/intel/constraints.md</automated>
  </verify>
  <done>The authoritative ADR, current project summary, and derived constraint state the same permissive policy; all five required decisions have corrected active motivations; and CONSTRAINT-env no longer describes the retired rule as an active hard constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Run the classified policy audit and documentation-only regression gates</name>
  <read_first>
    - .planning/ROADMAP.md: Phase 8 success criteria
    - .planning/REQUIREMENTS.md: ARCH-01 out-of-scope fences
    - .planning/phases/BC-08-architecture-constraint-cleanup/08-RESEARCH.md: audit commands, classification rules, and scope allowlist
  </read_first>
  <files>docs/DECISIONS.md, .planning/PROJECT.md, .planning/intel/constraints.md</files>
  <action>Run the tracked-project policy-family audit from the research and inspect the context of every match. Keep only permissive wording or material that explicitly identifies itself as historical or superseded; correct an active contradiction only in the three scoped documentation artifacts. Then enforce the three-path diff allowlist and additionally fail if the diff contains production, test, Razor, project, static-asset, or JavaScript paths. Run the complete solution build and test commands after the documentation audit passes, explicitly checking `$LASTEXITCODE` immediately after each command and exiting nonzero before continuing if either gate fails. Record the exact commands, the classification outcome, changed paths, and build/test results in the execution summary. Do not create an analyzer, lint utility, runtime fixture, script file, or any product-surface change as audit evidence.</action>
  <verify>
    <automated>$policyMatches = rg -n -i --hidden --no-ignore --glob '!.git/**' --glob '!**/bin/**' --glob '!**/obj/**' --glob '!.vs/**' 'no hand-authored JavaScript|no JS|hand-authored|no application-authored JavaScript|no JavaScript anywhere' .; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; $policyMatches; $allowed = @('docs/DECISIONS.md','.planning/PROJECT.md','.planning/intel/constraints.md'); $changed = git diff --name-only; $unexpected = $changed | Where-Object { $_ -and $_ -notin $allowed }; if ($unexpected) { $unexpected; exit 1 }; $runtimePaths = $changed | Where-Object { $_ -match '^(src|tests|wwwroot)/|\.(cs|razor|csproj|js)$' }; if ($runtimePaths) { $runtimePaths; exit 1 }; dotnet build BlazorCanvas.sln; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet test BlazorCanvas.sln --nologo; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Every retained policy-family hit is explicitly permissive, historical, or superseded; the implementation diff is limited to the three approved documentation artifacts; and the full build and test suite pass without a runtime or JavaScript addition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authoritative ADR to derived planning constraint | A stale derived statement can be treated as an active architectural restriction by future planners. |
| Documentation-only diff to application surface | An unreviewed source or asset edit could be hidden inside a policy-cleanup change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-BC08-01 | Tampering | CONSTRAINT-env and current policy summaries | medium | mitigate | Reconcile the derived/current artifacts with the ADR and inspect every audit match in context. |
| T-BC08-02 | Tampering | Runtime and static-asset surface | medium | mitigate | Enforce the exact three-path diff allowlist before running the existing build and tests. |
| T-BC08-03 | Repudiation | Phase close-out evidence | low | mitigate | Record exact audit, diff, build, and test commands/results in 08-01-SUMMARY.md. |
</threat_model>

<verification>
1. Inspect D-06, D-18, D-33, D-37, and D-57 alongside PROJECT.md and CONSTRAINT-env for aligned current rationale.
2. Run the policy-family audit, classifying each result by its surrounding context rather than deleting historical evidence.
3. Check the documentation-only path allowlist, then run `dotnet build BlazorCanvas.sln` and `dotnet test BlazorCanvas.sln --nologo`.
</verification>

<success_criteria>
- ARCH-01 is fully covered, including the active derived constraint, without an application or JavaScript change.
- The ADR, project summary, and constraint inventory agree that the former prohibition is retired and that the five decisions rest on MVP simplicity or their independent behavioural rationale.
- The reproducible audit, diff-scope check, full build, and full test suite provide close-out evidence.
</success_criteria>

<output>
Create `.planning/phases/BC-08-architecture-constraint-cleanup/08-01-SUMMARY.md` when done.
</output>
