---
phase: 14
slug: catalog-seed-toolbar-decisions
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-22
reviewed_at: 2026-07-22
---

# Phase 14 - UI Design Contract

> Visual and interaction contract for Phase 14: Catalog Seed, Toolbar & Decisions. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Scope

Phase 14 changes one visible surface: the existing canvas toolbar gains one armable star tool button.
The phase does not implement star drawing preview, star rendering, star persistence writes, star
selection, star drag, star delete behavior, or cross-tab star sync; those belong to Phases 15 and 16.

The toolbar must show exactly this left-to-right tool/action order:

`[pointer] [line] [rectangle] [circle] [triangle] [star] [delete]`

The star button sits between triangle and delete. The 48px toolbar strip, the 40x40 icon-button
treatment, the right-aligned logout form, and the delete action semantics remain unchanged.

Primary visual anchor is the armed toolbar tool, shown by the accent-filled 40x40 button; otherwise
the canvas remains visually dominant.

Sources: `.planning/REQUIREMENTS.md` CANV-04, `.planning/ROADMAP.md` Phase 14 success criteria,
`14-RESEARCH.md`, `Toolbar.razor`, `Toolbar.razor.css`, `Tool.cs`, `docs/DECISIONS.md` D-16/D-33/D-43/D-56/D-58.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none - Blazor Server / Razor Components with component-scoped CSS |
| Preset | not applicable |
| Component library | none - hand-authored Razor components and `*.razor.css` |
| Icon library | none - inline SVG icons using `stroke="currentColor"` |
| Font | System UI stack: `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` |

shadcn is not initialized and is not applicable: this is not a React, Next.js, or Vite project.
No npm package, icon package, web font, or third-party registry is introduced for this phase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap between adjacent toolbar buttons; top/bottom toolbar button margin |
| sm | 8px | Toolbar left inset before the pointer button |
| md | 16px | Horizontal space around the toolbar divider before the logout group |
| lg | 24px | Divider height; retained shared layout token |
| xl | 32px | Existing card/modal padding token; not newly used by this toolbar change |
| 2xl | 48px | Locked toolbar strip height and canvas Y offset |
| 3xl | 64px | Existing page-level spacing token; not newly used by this toolbar change |

Exceptions:

- Toolbar buttons, including the new star button, are exactly `40px` by `40px`, derived from the
  locked 48px strip minus `4px` top and bottom margin.
- Toolbar icons, including the new star glyph, are exactly `20px` by `20px`, centered inside the
  40x40 hit target.
- The vertical divider remains `1px` wide by `24px` high.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 600 (semibold) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.25 |
| Display | 28px | 600 (semibold) | 1.2 |

Exactly four sizes and two weights are declared. Phase 14 adds no visible text to the toolbar; the
new star button is icon-only with an accessible name. Do not add visible labels, tooltips, captions,
or instructional copy to the toolbar.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` | Canvas surface; unchanged and visually dominant |
| Secondary (30%) | `#DCE0E5` | Page background and the entire 48px toolbar strip |
| Accent (10%) | `#1D4ED8` | Armed tool button fill; focus-visible ring on focusable controls |
| Destructive | `#B91C1C` | Enabled Delete button icon tint on hover/focus only |

Accent reserved for: the currently armed toolbar tool's filled background and every
`:focus-visible` outline. The star button uses accent only when `Tool.Star` is armed or when it has
keyboard focus; it does not introduce a new color.

Supporting colors carried forward:

| Token | Value | Usage |
|-------|-------|-------|
| `text.primary` | `#1F2937` | Default toolbar icon color |
| `text.on-accent` | `#FFFFFF` | Icon color on an armed toolbar tool |
| `hover.tint` | `#CBD1D8` | Toolbar button hover background |
| `disabled.grey` | `#9CA3AF` | Disabled Delete icon color |
| `border.subtle` | `#B8BFC7` | Toolbar divider |

---

## Toolbar Component Contract

Layout:

- Root remains one flex row: `display: flex`, `align-items: center`, `height: 48px`, `width: 100%`,
  `background: #DCE0E5`, `box-sizing: border-box`.
- Keep `.toolbar-left` at `width: 8px`.
- Keep the star button visually identical in size, margin, radius, stroke width, hover, focus, and
  armed treatment to pointer, line, rectangle, circle, and triangle.
- Keep Delete immediately after star and before the divider.
- Keep the divider after Delete and before Logout.
- Keep `.logout-form { margin-left: auto; margin-right: 4px; }` so Logout remains right-aligned.
- Do not add a toolbar border, second row, wrapping behavior, label text, menu, flyout, dynamic
  split button, or additional spacing to make room for star.

Button order and semantics:

| Position | Control | Type | State Contract |
|----------|---------|------|----------------|
| 1 | Pointer | armable tool | `aria-pressed` reflects `Armed == Tool.Pointer`; default on page load |
| 2 | Line | armable tool | `aria-pressed` reflects `Armed == Tool.Line` |
| 3 | Rectangle | armable tool | `aria-pressed` reflects `Armed == Tool.Rectangle` |
| 4 | Circle | armable tool | `aria-pressed` reflects `Armed == Tool.Circle` |
| 5 | Triangle | armable tool | `aria-pressed` reflects `Armed == Tool.Triangle` |
| 6 | Star | armable tool | `aria-pressed` reflects `Armed == Tool.Star`; maps to shape name `star5` |
| 7 | Delete | action button | Disabled when no figure is selected; does not become an armable tool |
| Right edge | Logout | POST form submit | Remains a real `/logout` form with antiforgery token |

Per-button states:

| State | Trigger | Treatment |
|-------|---------|-----------|
| Default | Unarmed enabled tool | Transparent background; icon `#1F2937`; 20x20 inline SVG |
| Hover | `:hover` | Background `#CBD1D8`; `transition: 120ms ease` |
| Focus-visible | `:focus-visible` | `outline: 2px solid #1D4ED8`; `outline-offset: 2px` |
| Armed | Button's tool equals `Armed` | Background `#1D4ED8`; icon `#FFFFFF`; exactly one armable tool is armed |
| Disabled | Delete with `DeleteEnabled == false` | Transparent background; icon `#9CA3AF`; `cursor: not-allowed`; no action |
| Delete enabled hover/focus | Delete enabled and hovered/focused | Icon color `#B91C1C`; normal hover background |

Star icon:

- Use an inline SVG, not an external icon dependency.
- Use `width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5"`.
- Draw a recognizable five-point star outline with `stroke-linejoin="round"` and no fill.
- The glyph must fit visually within the same optical bounds as the triangle icon; it must not touch
  the 20x20 viewBox edge.
- Accessible name: `aria-label="Draw star"`.

Interaction:

- Clicking Star invokes `ArmedChanged.InvokeAsync(Tool.Star)` and makes Star the only armed tool.
- Arming Star follows the same deselection behavior as other non-delete toolbar tool presses.
- Delete remains an action, not a mode; do not add `Tool.Delete`.
- Logout remains outside the seven-button tool/action count.
- Keyboard activation uses native `<button>` semantics; no JavaScript shortcut is introduced.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable - Phase 14 adds an icon-only armable toolbar tool, not a labelled CTA |
| Star accessible label | `Draw star` |
| Empty state heading | Not applicable - an empty canvas remains a blank white canvas |
| Empty state body | Not applicable - no placeholder text overlays the drawing surface |
| Error state | `The change could not be saved. The canvas will be reloaded from the database.` |
| Destructive confirmation | Delete: no confirmation dialog; deletion remains immediate on click when enabled |

Do not add visible explanatory copy for the star button. The toolbar's language remains icon-only,
with accessible labels supplying names for assistive technology.

---

## UI Considerations

Applicable state considerations resolved: 12 covered, 0 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| loading | Toolbar strip | covered | No toolbar loading treatment is introduced; the seven controls render synchronously from static Razor markup. |
| error | Toolbar strip | covered | Toolbar rendering has no separate error state; backend catalog seed failures use the existing app failure path rather than hiding or disabling Star. |
| overflow | Toolbar strip | covered | The seventh button fits in the unchanged 48px-high, full-width flex toolbar without wrapping; Logout remains pushed right by `margin-left: auto`. |
| long-text | Toolbar strip | covered | Toolbar controls remain icon-only; Logout retains existing text and right alignment. |
| long-text | Star toolbar button | covered | Star has no visible label; accessible label stays short (`Draw star`) and does not affect layout. |
| long-text | Delete action button | covered | Delete remains icon-only with existing accessible name; no visible text can overflow. |
| empty | Canvas surface | covered | Empty canvas remains blank white with no placeholder; this phase does not change canvas empty state. |
| loading | Canvas surface | covered | Phase 14 adds no canvas-loading UI; startup/database loading behavior remains unchanged. |
| error | Canvas surface | covered | Existing save-failure copy and reload flow remain unchanged; Phase 14 introduces no new canvas error surface. |
| populated | Canvas surface | covered | Populated canvas remains dominated by existing figures; Phase 14 does not render persisted stars yet. |
| overflow | Save failure copy | covered | Existing modal/copy container remains the owner of wrapping; Phase 14 does not change its text length or layout. |
| long-text | Save failure copy | covered | The locked error sentence remains short enough to wrap normally in the existing container. |

Dismissed as not applicable:

- Partial: the toolbar is a fixed control set; partial catalog seed states are backend concerns and
  must not surface as missing/disabled toolbar buttons in this phase.
- Zero-one-many: the toolbar count is fixed at seven controls; dynamic toolbar behavior is deferred
  to v1.2.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable - shadcn not initialized |
| third-party | none | not applicable - no third-party blocks or registries |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS with non-blocking focal-point recommendation incorporated
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved by gsd-ui-checker on 2026-07-22.
