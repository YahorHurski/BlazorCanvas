---
phase: BC-14-catalog-seed-toolbar-decisions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlazorCanvas/Tools/Tool.cs
  - src/BlazorCanvas/Components/Canvas/Toolbar.razor
  - tests/BlazorCanvas.Tests/Tools/ToolMapTests.cs
  - tests/BlazorCanvas.Tests/Components/ToolbarSourceTests.cs
autonomous: true
requirements:
  - CANV-04
must_haves:
  truths:
    - Toolbar order is pointer, line, rectangle, circle, triangle, star, delete.
    - Star is an armable tool and maps to the registry name star5.
    - Delete remains an action button outside the Tool enum.
    - The 48px toolbar strip and right-aligned antiforgery logout form remain unchanged.
  artifacts:
    - src/BlazorCanvas/Tools/Tool.cs contains Tool.Star and ToolMap.ToShapeName(Tool.Star) returns star5.
    - src/BlazorCanvas/Components/Canvas/Toolbar.razor contains a star button between triangle and delete.
    - tests/BlazorCanvas.Tests/Tools/ToolMapTests.cs proves star mapping and delete absence.
    - tests/BlazorCanvas.Tests/Components/ToolbarSourceTests.cs pins toolbar order and logout form preservation from source.
  key_links:
    - Toolbar.razor @bind-Armed uses the Tool enum; ToolMap.ToShapeName converts armable tools to registry names.
    - Logout must remain a POST form with antiforgery hidden input, not a Blazor action button.
---

<objective>
Expose an armable star toolbar tool without changing toolbar layout or logout semantics.

Purpose: CANV-04 requires the seventh toolbar control before Phase 15 can draw a star.
Output: Tool.Star, star5 mapping, star toolbar button between triangle and delete, and source-level regression tests.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-RESEARCH.md
@.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-PATTERNS.md
@.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-UI-SPEC.md
@src/BlazorCanvas/Tools/Tool.cs
@src/BlazorCanvas/Components/Canvas/Toolbar.razor
@src/BlazorCanvas/Components/Canvas/Toolbar.razor.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify Tool.Star mapping and toolbar source contract</name>
  <files>tests/BlazorCanvas.Tests/Tools/ToolMapTests.cs, tests/BlazorCanvas.Tests/Components/ToolbarSourceTests.cs</files>
  <behavior>
    - ToolMap.ToShapeName(Tool.Star) returns star5.
    - Tool enum values contain Pointer, Line, Rectangle, Circle, Triangle, Star and do not contain Delete.
    - Toolbar source orders Tool.Star after Tool.Triangle and before the delete-button markup.
    - Star button uses ToolButtonClass(Tool.Star), aria-pressed for Armed == Tool.Star, aria-label Draw star, and ArmedChanged.InvokeAsync(Tool.Star).
    - Toolbar source still contains a method="post" action="/logout" form and antiforgery hidden input.
    - Toolbar stylesheet still declares height: 48px and logout-form margin-left: auto.
  </behavior>
  <action>Create narrow xUnit tests without adding a component-testing package. Put pure enum/mapping assertions in a tools test file. Put Razor/CSS source-contract assertions in a components test file that reads Toolbar.razor and Toolbar.razor.css as text from the repository root or test output path, following existing project test conventions. Assert order by comparing index positions rather than brittle whole-file snapshots. Do not create a dynamic toolbar, split button, visible label, tooltip, or JavaScript shortcut.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~ToolMapTests|FullyQualifiedName~ToolbarSourceTests"</automated>
  </verify>
  <done>Focused tests fail until Tool.Star and the star toolbar button exist while delete/logout constraints stay pinned.</done>
  <acceptance_criteria>CANV-04 is expressed as executable tests for order, arming semantics, mapping, layout preservation, and logout preservation.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add star as an armable toolbar tool</name>
  <files>src/BlazorCanvas/Tools/Tool.cs, src/BlazorCanvas/Components/Canvas/Toolbar.razor</files>
  <behavior>
    - Tool.Star is added after Tool.Triangle.
    - ToolMap.ToShapeName maps Tool.Star to star5 and keeps Tool.Pointer mapped to null.
    - Delete remains outside the Tool enum.
    - Toolbar.razor renders the star button between triangle and delete using the existing armable button pattern.
    - Star button is icon-only, uses a 20x20 inline SVG star outline, and has aria-label Draw star.
  </behavior>
  <action>Implement the CANV-04 toolbar change. Add Tool.Star after Tool.Triangle and return star5 from ToolMap.ToShapeName. Insert one armable star button in Toolbar.razor immediately after the triangle button and immediately before the existing delete button. Match the existing button attributes and event pattern, use ToolButtonClass(Tool.Star), set aria-pressed from Armed == Tool.Star, use aria-label Draw star, and call ArmedChanged.InvokeAsync(Tool.Star). Use an inline 20x20 currentColor SVG outline that fits the existing optical bounds. Leave Toolbar.razor.css unchanged unless a test proves the existing 48px row or right-aligned logout cannot be preserved; if a CSS edit is made, keep height 48px and logout-form margin-left auto. Do not implement star preview, renderer, persistence writes, selection, drag, delete behavior, or cross-tab sync.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~ToolMapTests|FullyQualifiedName~ToolbarSourceTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Toolbar shows the required seven controls in order and Star arms exclusively through the same @bind-Armed path as the other tools.</done>
  <acceptance_criteria>Success criterion 4 is satisfied without changing toolbar height, delete action semantics, or logout antiforgery form semantics.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-Blazor-circuit | User clicks toolbar buttons that mutate server-side armed tool state. |
| toolbar-to-logout-endpoint | Logout leaves the interactive toolbar through a POST form and antiforgery token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Spoofing | Toolbar.razor logout control | high | mitigate | Preserve the existing POST /logout form and antiforgery hidden input; do not convert logout to an interactive EventCallback button. |
| T-14-05 | Tampering | ToolMap.ToShapeName | medium | mitigate | Map Tool.Star to the closed registry string star5; keep Tool.Pointer null and do not add Tool.Delete. |
| T-14-06 | Denial of Service | Toolbar layout | low | mitigate | Reuse the existing 40x40 button pattern inside the 48px flex row and source-test the 48px strip/right-aligned logout invariants. |
| T-14-SC | Tampering | package installs | low | accept | No package install is planned; inline SVG and xUnit source tests reuse existing project dependencies. |
</threat_model>

<verification>
Run the focused tests and the full solution test command. API Coverage Decision Checkpoint: not applicable because Phase 14 adds no external API integration and no API connector surface; do not create COVERAGE.md.
</verification>

<success_criteria>
- Tool.Star exists and maps to star5.
- Delete is not present in Tool.
- Toolbar order is pointer, line, rectangle, circle, triangle, star, delete.
- Star button arms/unarms exclusively through the same bound Armed state as other tools.
- Toolbar.razor.css preserves the 48px strip and logout remains right-aligned through the existing form.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Seventh toolbar button arms the star tool | 14-02 | COVERED | Tool enum, ToolMap, Toolbar.razor, and tests included.
REQ | CANV-04 | Seven-button toolbar with star between triangle and delete | 14-02 | COVERED | UI-SPEC order and semantics are pinned.
RESEARCH | - | Keep delete out of Tool enum and preserve logout/48px layout | 14-02 | COVERED | Task 1 tests and threat model cover both.
RESEARCH | - | No external icon/component package | 14-02 | COVERED | Inline SVG required; no package installs.
CONTEXT | - | No CONTEXT.md exists; user chose to continue without context | 14-02 | COVERED | No D-NN phase-context decisions are invented.
</source_audit>

<output>
Create `.planning/phases/BC-14-catalog-seed-toolbar-decisions/14-02-SUMMARY.md` when done.
</output>
