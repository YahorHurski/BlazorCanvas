---
phase: BC-16-interaction-sync-test-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
autonomous: true
requirements:
  - FIG-08
  - SYNC-04
must_haves:
  truths:
    - "A click (press-to-release under 3px) on a persisted star5 row selects it and writes nothing (D-48), and re-selecting the same star is a no-op."
    - "Dragging a star5 toward a canvas edge clamps the delta against the star's bbox_* and slides along the edge, persisting exactly one UPDATE on drop (D-24/D-36)."
    - "Deleting the selected star removes it locally and broadcasts a single delete; DeleteAsync with nothing selected is a silent no-op (FIG-08 idempotency)."
    - "An incoming move for a star id this circuit does not hold is ignored and never inserts a star (D-40 update-only), and a star drag whose persist returns zero rows broadcasts a delete instead of resurrecting it."
    - "While this circuit is dragging a star, every incoming draw, move, delete, and rollback is discarded (D-54 discard-ALL), and this circuit never re-applies its own star draw or move (D-53 echo filter)."
  artifacts:
    - path: "tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs"
      provides: "Coordinator-boundary star5 select, click-vs-drag, edge-clamped drag, delete, D-40 update-only, D-54 discard-all, and echo-filter coverage."
  key_links:
    - from: "src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs"
      to: "src/BlazorCanvas/Sync/CanvasSyncNotifier.cs"
      via: "BeginDrag/ContinueDrag/CommitDragAsync/DeleteAsync publish SyncMessage without any star-specific branch."
      pattern: "SyncMessage.Move|SyncMessage.Delete"
    - from: "src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs"
      to: "src/BlazorCanvas/Geometry/V11Movement.cs"
      via: "ContinueDrag clamps the drag delta against the star row bbox_* the same way it clamps the four existing shapes."
      pattern: "V11Movement.ClampPosition"
---

<objective>
Prove a persisted star5 selects, drags with edge clamping, deletes, and obeys the D-40/D-53/D-54 sync rules through the same type-blind coordinator path as the four existing shapes — with no star-specific branch.

Purpose: FIG-08 requires selection, edge-clamped drag, and delete to match the four shapes exactly; SYNC-04 requires the star to obey the unchanged D-53 contract. The coordinator is already type-blind, so this plan pins that a star5 row travels the shared path and honours the resurrection, discard-all, and echo rules.
Output: Focused coordinator tests covering star select, click-vs-drag, clamp, delete, idempotency, D-40 update-only, D-54 discard-all, and the echo filter.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-01-SUMMARY.md
@docs/DECISIONS.md
@src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
@src/BlazorCanvas/Sync/CanvasSyncNotifier.cs
@src/BlazorCanvas/Sync/SyncMessage.cs
@src/BlazorCanvas/Shapes/Star5Shape.cs
@src/BlazorCanvas/Tools/Tool.cs
@tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify star5 select, click-vs-drag, and delete parity at the coordinator boundary</name>
  <files>tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
    - src/BlazorCanvas/Sync/SyncMessage.cs
    - src/BlazorCanvas/Shapes/Star5Shape.cs
    - src/BlazorCanvas/Tools/Tool.cs
    - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
  </read_first>
  <behavior>
    - Loading a canvas that already holds a star5 row and calling BeginDrag on it sets SelectedId to that star's id (D-31/D-48 select on press).
    - A BeginDrag followed by CommitDragAsync with no ContinueDrag beyond the 3px threshold performs no move callback and publishes no move or delete (D-48 click writes nothing).
    - Calling BeginDrag twice on the same star id leaves exactly one selection and inserts no figure (idempotent re-select).
    - DeleteAsync while the star is selected removes it from Figures, clears SelectedId, and publishes exactly one delete message carrying only the star id (D-33).
    - DeleteAsync when SelectedId is null returns silently, changes no Figures, and publishes nothing (FIG-08 idempotency: deleting an already-deleted star is a no-op).
  </behavior>
  <action>Add coordinator tests for FIG-08 star selection and deletion, citing D-31, D-48, D-33, and D-58 in test names or local comments. Seed the coordinator with a star5 row using the existing Create helper and its insert lambda (draw a star5 through DrawAsync, or add a star5 FigureRow to the backing rows list before LoadAsync); do not add a star-specific branch to CanvasInteractionCoordinator. Reuse the existing notifier-observer pattern to capture publications. Assert the select, click-writes-nothing, idempotent re-select, delete-broadcasts-once, and delete-when-empty-is-silent behaviors against the star row. Keep every assertion on the shared BeginDrag/CommitDragAsync/DeleteAsync methods.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~CanvasInteractionCoordinatorTests"</automated>
  </verify>
  <done>Coordinator tests prove star5 selects on press, a sub-threshold click persists nothing, re-selecting is idempotent, delete removes and broadcasts once, and deleting with nothing selected is a silent no-op.</done>
  <acceptance_criteria>
    - CanvasInteractionCoordinatorTests pass.
    - New test methods reference star5 and cite D-31, D-48, and D-33 in names or comments.
    - No production star-specific branch is added to CanvasInteractionCoordinator.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify star5 edge-clamped drag, D-40 update-only, D-54 discard-all, and echo filter</name>
  <files>tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
    - src/BlazorCanvas/Sync/CanvasSyncNotifier.cs
    - src/BlazorCanvas/Sync/SyncMessage.cs
    - src/BlazorCanvas/Geometry/V11Movement.cs
    - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
  </read_first>
  <behavior>
    - Dragging a star5 positioned near the right/bottom edge past the canvas bound clamps the delta so the star's persisted x/y keep its bbox_* fully inside CanvasBounds, and a figure pinned to one edge still slides along the other axis (D-24/D-36 per-axis clamp).
    - A star drag that moves at least 3px calls the move persistence callback exactly once and publishes a trailing-edge move before persistence.
    - When the move persistence callback returns zero rows, the star is removed from Figures, SelectedId is cleared if it matched, and a delete is broadcast (D-40: a stale star is not resurrected).
    - Publishing a move for a star id this circuit does not hold inserts nothing (D-40 update-only; unknown figure ignored entirely).
    - While a star drag is in progress, publishing draw, move, delete, and rollback messages changes nothing in this circuit (D-54 discard-ALL, not only messages about the dragged star).
    - After this circuit draws or drags a star, its own draw and move broadcasts delivered back to it are ignored so no duplicate star appears (D-53 echo filter via the per-circuit sender GUID).
  </behavior>
  <action>Add coordinator tests for FIG-08 drag parity and SYNC-04 sync rules, citing D-24, D-36, D-40, D-53, and D-54 in test names or comments. Reuse the existing Create helper's optional clock and move overrides: pass a move callback returning zero to exercise the resurrection guard, and the default returning one for the clamp/throttle path. Drive ContinueDrag past CanvasBounds to assert the clamped persisted position keeps the star's bbox inside the surface and that per-axis sliding still works. Assert exactly one move persistence call and a trailing-edge move publication on a real drag. For D-40 update-only, publish a move for a fresh unknown Guid and assert Figures is unchanged. For D-54, begin a star drag then publish each kind and assert no change. For the echo filter, assert the circuit's own star draw yields a single local figure. Do not modify CanvasInteractionCoordinator, the notifier, or the sync message contract.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~CanvasInteractionCoordinatorTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Star drag clamps at the edge and persists one UPDATE, a zero-row star move broadcasts a delete without resurrection, unknown-id moves insert nothing, mid-drag discards every kind, and the circuit ignores its own star broadcasts.</done>
  <acceptance_criteria>
    - CanvasInteractionCoordinatorTests and the full solution tests pass.
    - New test methods cite D-24/D-36 (clamp), D-40 (update-only/no resurrection), D-54 (discard-all), and D-53 (echo filter).
    - No production coordinator, notifier, or SyncMessage change is introduced.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs: new star5 test methods for select/click-vs-drag/delete/idempotency (Task 1) and edge-clamped drag, D-40 update-only, D-54 discard-all, and echo filter (Task 2).
- No production class, method, constant, field, schema, or external API symbol is created by this plan; it verifies existing type-blind coordinator/notifier symbols carry star5 correctly.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-Blazor-circuit | Pointer select/drag/delete gestures on a star enter server-side coordinator handling. |
| circuit-to-circuit notifier | Star move/delete broadcasts reach other tabs for the same owner. |
| remote-message-to-local-state | An inbound star draw/move/delete/rollback is applied to this circuit's figure list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | CanvasInteractionCoordinator.ApplyRemoteMessageCore move handling | high | mitigate | Pin D-40: an inbound star move for an unknown id inserts nothing, so a forged or late move cannot resurrect a deleted star. |
| T-16-02 | Tampering | CanvasInteractionCoordinator drag receipt during a star drag | medium | mitigate | Pin D-54: while dragging a star, every inbound kind is discarded so a stale broadcast cannot overwrite the in-flight local state. |
| T-16-03 | Spoofing | CanvasSyncNotifier echo of the circuit's own star broadcasts | low | mitigate | Pin D-53 echo filter: the per-circuit sender GUID makes the circuit ignore its own star draw/move so no duplicate star appears. |
| T-16-04 | Denial of Service | CanvasInteractionCoordinator.CommitDragAsync zero-row path | medium | mitigate | Pin the zero-row star move broadcasting a delete and clearing local state rather than looping or crashing. |
| T-16-SC | Tampering | package installs | low | accept | No npm, pip, cargo, or NuGet package install is planned; existing xUnit dependencies are reused. |
</threat_model>

<verification>
Run both task verification commands. API Coverage Decision Checkpoint: detected false — Phase 16 uses Blazor Server, an in-process coordinator/notifier, and PostgreSQL only, with no external API/SDK integration; do not create COVERAGE.md. Assumption-Delta Architecture Checkpoint: detected false — star5 was added as the fifth shape in Phases 13-15; this plan only proves its interaction/sync parity, with no singular-to-plural, required-to-optional, or derived-to-chosen shift. Schema Push Detection Gate: not applicable — no ORM, schema, or migration files are modified.
</verification>

<success_criteria>
- FIG-08: star5 select, click-vs-drag, edge-clamped drag, and delete are proven through the shared coordinator path.
- SYNC-04: star5 honours D-40 update-only, D-54 discard-all, and the D-53 echo filter at the coordinator boundary.
- No star-specific branch is introduced in the coordinator, notifier, or sync contract.
- The full solution test suite passes.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | A persisted star selects, drags with edge clamping, and deletes exactly like the four shapes | 16-01 | COVERED | Coordinator tests exercise the shared select/drag/delete path for star5.
GOAL | - | The star obeys the unchanged D-53 sync contract across tabs | 16-01 | COVERED | D-40 update-only, D-54 discard-all, and echo filter are pinned at the coordinator boundary; two-circuit relay is 16-02.
REQ | FIG-08 | Select, edge-clamped drag, delete, and blue+white dashed trace on the star's own outline | 16-01 | COVERED | Behavioral select/drag/delete here; the SelectionTrace star5 source contract is already pinned by V11RenderContractTests.
REQ | SYNC-04 | Star draw/glide/delete under the unchanged D-53 contract | 16-01 | PARTIAL | Coordinator sync rules here; two-circuit final-public relay is covered by 16-02.
RESEARCH | - | User explicitly skipped research | 16-01 | COVERED | No RESEARCH.md is required or referenced.
CONTEXT | - | User explicitly skipped discuss-phase | 16-01 | COVERED | Locked decisions taken from docs/DECISIONS.md (D-24/D-31/D-33/D-36/D-40/D-48/D-53/D-54) and STATE.md.
</source_audit>

<output>
Create `.planning/phases/BC-16-interaction-sync-test-guards/16-01-SUMMARY.md` when done.
</output>
