---
phase: BC-16-interaction-sync-test-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
autonomous: true
requirements:
  - SYNC-04
  - FIG-08
must_haves:
  truths:
    - "A star drawn in circuit A appears in circuit B as a draw, glides in B through throttled moves with a guaranteed trailing edge during a drag, and disappears from B on delete — under the unchanged D-53 contract."
    - "A star draw replayed from another circuit creates no second row in state or in public.figures (D-53 draw is insert-or-update, applied idempotently)."
    - "A move or rollback for a star id no circuit holds is ignored everywhere and never inserts (D-40 update-only)."
    - "A star deleted directly in the database by circuit B and then dragged in circuit A is removed from every circuit via a delete broadcast rather than resurrected (D-40 zero-row-move guard)."
    - "A persisted star round-trips select, edge-clamped drag persisting one trailing coordinate, and delete through the real FigureRepository, leaving public.figures empty."
  artifacts:
    - path: "tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs"
      provides: "Two-circuit final-public star draw/glide/delete relay, duplicate-draw and unknown-move guards, D-40 zero-row resurrection guard, and a persisted select/drag-clamp/delete round-trip."
  key_links:
    - from: "src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs"
      to: "src/BlazorCanvas/Data/V11/FigureRepository.cs"
      via: "Draw/Move/Delete callbacks call the real repository Insert/Move/Delete for a star row with no star-specific branch."
      pattern: "repository.MoveAsync|repository.DeleteAsync"
    - from: "src/BlazorCanvas/Sync/CanvasSyncNotifier.cs"
      to: "src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs"
      via: "A committed star draw/move/delete is relayed to circuit B and applied under D-40/D-53/D-54."
      pattern: "SyncMessage.Draw|SyncMessage.Move|SyncMessage.Delete"
---

<objective>
Prove a star synchronises live across two circuits — draw, real-time glide, and delete — through the real final-public FigureRepository under the unchanged D-53 contract, and that no duplicate insert or zero-row move can resurrect it.

Purpose: SYNC-04 requires a star to appear on draw, glide during a drag, and disappear on delete in the user's other tabs under D-53; FIG-08 requires the persisted select/drag-clamp/delete round-trip to match the four shapes. The final-public harness already proves this for a rectangle, so this plan pins the same evidence for star5.
Output: Focused final-public two-circuit integration tests for star draw/glide/delete relay, duplicate-draw and unknown-move guards, the D-40 zero-row resurrection guard, and a persisted select/drag/delete round-trip.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/BC-15-draw-preview-render-persist-a-star/15-01-SUMMARY.md
@docs/DECISIONS.md
@src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
@src/BlazorCanvas/Data/V11/FigureRepository.cs
@src/BlazorCanvas/Sync/CanvasSyncNotifier.cs
@src/BlazorCanvas/Sync/SyncMessage.cs
@src/BlazorCanvas/Shapes/Star5Shape.cs
@tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
@tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove two-circuit star draw, glide, and delete relay through the final-public repository</name>
  <files>tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
    - src/BlazorCanvas/Sync/SyncMessage.cs
    - src/BlazorCanvas/Data/V11/FigureRepository.cs
    - tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
  </read_first>
  <behavior>
    - Drawing a star5 in circuit A inserts one public.figures row and delivers it to circuit B as a draw message whose Figure carries the canonical star row and whose X/Y are null (D-53 draw).
    - Replaying that draw from an unrelated sender GUID adds no second row to circuit B's Figures and no second row to public.figures (idempotent insert-or-update).
    - Dragging the star in circuit A across several throttled ContinueDrag steps delivers multiple move messages to circuit B ending with the trailing-edge coordinate, and persists only the final coordinate (D-47 trailing edge, D-11 one UPDATE per drag).
    - A move and a rollback for a fresh unknown star id are ignored in both circuits and in public.figures (D-40 update-only).
    - Deleting the star in circuit A empties Figures in both circuits and public.figures, and every move/delete/rollback message carries a null Figure (D-53 identity-only kinds); no message of kind preview is ever published.
  </behavior>
  <action>Add a final-public two-circuit integration test for SYNC-04 that mirrors the existing rectangle draw/move/delete relay test but draws star5 via harness.A.Coordinator.DrawAsync("star5", press, cursor). Use the existing CreateHarnessAsync, the shared notifier observer, and a controllable clock where a throttled multi-step glide is asserted. Assert the star appears in harness.B.Coordinator.Figures and harness.A.Repository.LoadAsync, that a replayed draw creates no duplicate row, that a throttled drag relays moves ending at the trailing coordinate and persists only that coordinate, that unknown-id move/rollback are ignored, and that delete empties both circuits and the table. Assert no published message has kind preview. Do not modify schema, migrations, V11Schema, the coordinator, or package references.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests"</automated>
  </verify>
  <done>A star drawn in circuit A relays a canonical draw to B, glides via trailing-edge moves, persists one coordinate, ignores unknown-id and duplicate messages, and disappears on delete under D-53 — all through the real repository.</done>
  <acceptance_criteria>
    - Focused final-public tests pass against PostgreSQL.
    - The star relay test asserts a single row after a duplicate draw replay and a trailing-edge glide coordinate.
    - The test asserts no preview-kind message is published and contains no schema, migration, or coordinator change.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the D-40 star resurrection guard and a persisted select/drag-clamp/delete round-trip</name>
  <files>tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
    - src/BlazorCanvas/Data/V11/FigureRepository.cs
    - src/BlazorCanvas/Geometry/V11Movement.cs
    - tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
    - tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs
  </read_first>
  <behavior>
    - A star drawn in circuit A and then deleted directly through circuit B's repository leaves circuit A holding a genuinely stale star row.
    - When circuit A drags that stale star and commits, the persist returns zero rows, so A removes the star, clears its selection, and broadcasts a delete that also clears B's stale copy — the star is not resurrected in either circuit or in public.figures (D-40).
    - A persisted star selected in circuit A, dragged past a canvas edge, and committed persists a single UPDATE whose position keeps the star's bbox_* inside CanvasBounds (D-24/D-36), and the moved row matches an independent repository reload.
    - Deleting that star afterward leaves public.figures empty and both circuits with no figures.
  </behavior>
  <action>Add a final-public integration test for FIG-08 and the D-40 resurrection guard that mirrors the existing rectangle zero-row-move test but uses star5. Draw a star in circuit A, delete it directly via harness.B.Repository.DeleteAsync to make A stale, then drag and commit in A and assert both circuits and the table are empty with A's selection cleared. In the same or a sibling test, prove a persisted star select/edge-clamped-drag/delete round-trip: begin a drag on the star row, ContinueDrag past CanvasBounds, commit, assert the persisted position clamps the star inside the surface and equals an independent FigureRepository.LoadAsync result, then DeleteAsync and assert emptiness. Reuse CreateHarnessAsync and existing helpers; do not add a star-specific repository branch, schema change, or Save button.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>A stale star dragged after a remote delete is cleared everywhere without resurrection, and a persisted star select/edge-clamped-drag/delete round-trip matches a fresh repository reload and ends with an empty table.</done>
  <acceptance_criteria>
    - Focused final-public tests and the full solution tests pass.
    - The resurrection test asserts both circuits and public.figures are empty after the stale drag commits.
    - The round-trip test asserts the clamped persisted star position equals an independent LoadAsync result and contains no schema or repository change.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs: new star5 two-circuit relay test (Task 1) and star5 resurrection guard plus persisted select/drag-clamp/delete round-trip test(s) (Task 2).
- No production class, method, constant, field, schema, or external API symbol is created by this plan; it verifies the existing type-blind repository/coordinator/notifier path carries star5 across circuits.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blazor-circuit-to-PostgreSQL | Star draw/move/delete are persisted as public.figures writes through FigureRepository. |
| circuit-to-circuit notifier | A committed star draw/move/delete is relayed to a second circuit for the same owner. |
| stale-local-state-to-database | A circuit holding a star that another circuit already deleted must reconcile without resurrecting it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | FigureRepository.MoveAsync zero-row result for a star | high | mitigate | Pin D-40: a star move that affects zero rows broadcasts a delete so a remotely deleted star is not resurrected in any circuit or in public.figures. |
| T-16-06 | Tampering | CanvasInteractionCoordinator draw replay for a star | medium | mitigate | Pin that a replayed star draw inserts no second row in state or persistence, so a duplicated broadcast cannot fork the canvas. |
| T-16-07 | Tampering | FigureRepository.InsertAsync / MoveAsync star position | medium | mitigate | Reuse the existing bounded z-collision retry, parameterized writes, and delta clamp; no star-specific persistence path is added. |
| T-16-08 | Information Disclosure | CanvasSyncNotifier star relay payload | low | mitigate | Assert move/delete/rollback carry a null Figure and no preview kind is ever published, so geometry/style never ride an identity-only message. |
| T-16-SC | Tampering | package installs | low | accept | No npm, pip, cargo, or NuGet package install is planned; existing xUnit and PostgreSQL fixture dependencies are reused. |
</threat_model>

<verification>
Run both task verification commands. API Coverage Decision Checkpoint: detected false — this plan uses Blazor Server, in-process repositories/notifier, and PostgreSQL only, with no external API/SDK integration; do not create COVERAGE.md. Assumption-Delta Architecture Checkpoint: detected false — star5 is an existing fifth shape; this plan proves its cross-circuit sync parity with no model-shape shift. Schema Push Detection Gate: not applicable — no schema or migration files are modified.
</verification>

<success_criteria>
- SYNC-04: a star appears live in a second circuit on draw, glides via trailing-edge moves during a drag, and disappears on delete under the unchanged D-53 contract.
- FIG-08: a persisted star select/edge-clamped-drag/delete round-trip matches the four shapes and a fresh repository reload.
- D-40: a stale star dragged after a remote delete is cleared everywhere and never resurrected; unknown-id and duplicate messages insert nothing.
- The full solution test suite passes.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | A star glides live across a user's open tabs on draw, drag, and delete | 16-02 | COVERED | Two-circuit final-public relay proves draw/glide/delete for star5.
GOAL | - | A star selects, drags with edge clamping, and deletes exactly like the four shapes | 16-02 | COVERED | Persisted select/drag-clamp/delete round-trip through the real repository.
REQ | SYNC-04 | Star draw/glide/delete under the unchanged D-53 contract | 16-02 | COVERED | Draw relay, trailing-edge glide, identity-only kinds, no preview broadcast, and D-40 update-only.
REQ | FIG-08 | Select, edge-clamped drag, and delete parity for a persisted star | 16-02 | COVERED | Integration round-trip plus the D-40 resurrection guard; coordinator-level parity is 16-01.
RESEARCH | - | User explicitly skipped research | 16-02 | COVERED | No RESEARCH.md is required or referenced.
CONTEXT | - | User explicitly skipped discuss-phase | 16-02 | COVERED | Locked decisions taken from docs/DECISIONS.md (D-11/D-24/D-36/D-40/D-47/D-53/D-54) and STATE.md.
</source_audit>

<output>
Create `.planning/phases/BC-16-interaction-sync-test-guards/16-02-SUMMARY.md` when done.
</output>
