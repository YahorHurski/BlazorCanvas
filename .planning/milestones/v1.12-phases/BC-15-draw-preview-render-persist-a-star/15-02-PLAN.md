---
phase: BC-15-draw-preview-render-persist-a-star
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlazorCanvas/Components/Canvas/FigureShape.razor
  - tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs
autonomous: true
requirements:
  - RENDER-02
must_haves:
  truths:
    - "Persisted star rows render as a polygon from Star5Geometry.Points."
    - "The star renderer is inside the same translate(x,y) rotate(...) local transform as the existing shapes."
    - "Malformed or unparseable persisted star geometry fails closed instead of tearing down the circuit."
    - "The renderer source switch contains a star5 geometry branch, so stars do not disappear silently."
  artifacts:
    - path: "src/BlazorCanvas/Components/Canvas/FigureShape.razor"
      provides: "Committed and preview Star5Geometry polygon rendering under the local transform."
    - path: "tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs"
      provides: "Source contract pinning star rendering and local-transform behavior."
  key_links:
    - from: "src/BlazorCanvas/Data/V11/FigureRow.cs"
      to: "src/BlazorCanvas/Components/Canvas/FigureShape.razor"
      via: "Figure.Type resolves through DefaultShapes.CreateRegistry and Figure.GeometryJson parses into Star5Geometry."
      pattern: "case Star5Geometry star:"
    - from: "src/BlazorCanvas/Components/Canvas/FigureShape.razor"
      to: "src/BlazorCanvas/Shapes/Star5Geometry.cs"
      via: "Points(star.Points) emits ordered local polygon vertices."
      pattern: "points=\"@Points(star.Points)\""
---

<objective>
Pin the committed star renderer so persisted star rows display from local geometry under the storage-model transform.

Purpose: RENDER-02 requires a committed star to render immediately and after reload instead of disappearing from the renderer dispatch.
Output: Renderer source contract tests and, if needed, the FigureShape Star5Geometry polygon branch.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/DECISIONS.md
@src/BlazorCanvas/Components/Canvas/FigureShape.razor
@src/BlazorCanvas/Components/Canvas/SelectionTrace.razor
@src/BlazorCanvas/Shapes/Star5Geometry.cs
@src/BlazorCanvas/Shapes/Star5Shape.cs
@tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify committed star local-render contract</name>
  <files>tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs</files>
  <read_first>
    - src/BlazorCanvas/Components/Canvas/FigureShape.razor
    - src/BlazorCanvas/Components/Canvas/SelectionTrace.razor
    - src/BlazorCanvas/Shapes/Star5Geometry.cs
    - src/BlazorCanvas/Shapes/Star5Shape.cs
    - tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs
  </read_first>
  <behavior>
    - FigureShape.razor contains the invariant translate/rotate transform for every persisted geometry.
    - FigureShape.razor has a Star5Geometry switch branch.
    - The Star5Geometry branch renders polygon points from star.Points.
    - The branch uses the same fill, stroke, stroke width, fill opacity, stroke opacity, pointerdown, and stopPropagation attributes as TriangleGeometry.
    - FigureShape parses persisted JSON through the registered definition and does not render unparseable geometry.
  </behavior>
  <action>Strengthen V11RenderContractTests for RENDER-02. Keep the existing source-contract style and add assertions that the Star5Geometry branch in FigureShape is not merely present but renders a polygon from Points(star.Points) with the committed figure attributes and remains inside the existing transform group. Add a fail-closed assertion that the persisted Figure path still goes through Registry.TryGet(Figure.Type), definition.TryParseGeometry, and the JsonException catch. Do not rely on a browser screenshot in this plan.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~V11RenderContractTests"</automated>
  </verify>
  <done>Renderer tests fail if star5 is absent from FigureShape, rendered from a non-point formula, rendered outside the transform, or made exception-prone for malformed persisted geometry.</done>
  <acceptance_criteria>
    - Focused renderer tests pass.
    - Tests assert Star5Geometry and Points(star.Points) in FigureShape.razor.
    - Tests assert the persisted Figure path uses registry parse and JsonException fail-closed handling.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure FigureShape renders Star5Geometry like the existing polygon shape</name>
  <files>src/BlazorCanvas/Components/Canvas/FigureShape.razor</files>
  <read_first>
    - src/BlazorCanvas/Components/Canvas/FigureShape.razor
    - src/BlazorCanvas/Components/Canvas/SelectionTrace.razor
    - src/BlazorCanvas/Shapes/Star5Geometry.cs
    - src/BlazorCanvas/Shapes/TriangleGeometry.cs
    - tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs
  </read_first>
  <behavior>
    - Star5Geometry renders as a polygon using Points(star.Points).
    - It shares the committed style/pointer attributes used by TriangleGeometry.
    - It remains under Transform equal to translate(Number(X), Number(Y)) rotate(Number(Rotation)).
    - It works for both Figure and PreviewPlacement because both paths assign Geometry before the switch.
  </behavior>
  <action>If FigureShape already satisfies the new tests, leave production code unchanged. If it does not, add or correct only the Star5Geometry switch branch to mirror the TriangleGeometry polygon branch while reading ordered points from star.Points. Preserve the existing registry parse path, StyleGateway.Parse, PreviewPlacement path, invariant-culture number formatting, and fail-closed JsonException handling. Do not add a type-name string switch, MarkupString, raw SVG injection, or a renderer branch outside the transform group.</action>
  <verify>
    <automated>dotnet test tests/BlazorCanvas.Tests/BlazorCanvas.Tests.csproj --no-restore --filter "FullyQualifiedName~V11RenderContractTests"</automated>
    <automated>dotnet test BlazorCanvas.sln --no-restore</automated>
  </verify>
  <done>Committed and preview Star5Geometry values render through FigureShape as the same point-list polygon under the local transform.</done>
  <acceptance_criteria>
    - RENDER-02 source contract passes.
    - Full solution tests pass.
    - FigureShape contains no raw stored SVG rendering and no separate star coordinate formula.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
- src/BlazorCanvas/Components/Canvas/FigureShape.razor: Star5Geometry committed/preview polygon renderer under the existing transform, if not already present.
- tests/BlazorCanvas.Tests/Components/V11RenderContractTests.cs: strengthened RENDER-02 assertions for star local-render dispatch, point-list rendering, style parity, and fail-closed persisted JSON parsing.
- No schema, repository, notifier, or toolbar symbol is created by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database-to-renderer | Persisted geometry JSON leaves PostgreSQL and is parsed before SVG attributes are emitted. |
| renderer-to-browser-dom | Local geometry becomes SVG element attributes in the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Tampering | FigureShape.razor persisted geometry path | high | mitigate | Continue parsing through the registered IShapeDefinition and render only typed Star5Geometry, never raw stored SVG or unvalidated JSON text. |
| T-15-06 | Information Disclosure | FigureShape.razor corrupt-row handling | low | mitigate | Keep corrupt geometry fail-closed with JsonException handling and no user-visible reflection of stored payload text. |
| T-15-07 | Elevation of Privilege | SVG rendering attributes | medium | mitigate | Emit only numeric point attributes and validated style fields; do not introduce MarkupString or hand-built raw markup. |
| T-15-SC | Tampering | package installs | low | accept | No package install is planned; existing Razor and xUnit source tests are reused. |
</threat_model>

<verification>
Run the focused renderer test command and the full solution test command. API Coverage Decision Checkpoint: detected false, because this plan has no external API integration. Assumption-Delta Architecture Checkpoint: no model-shape shift; star5 follows D-70 and D-71. Schema Push Detection Gate: not applicable because no schema or migration files are modified.
</verification>

<success_criteria>
- RENDER-02 is pinned by source-contract tests.
- Star5Geometry renders from its ordered local points inside the translate/rotate transform.
- Malformed persisted star JSON does not render and does not crash the component.
- Full solution tests pass.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Committed star renders correctly after commit and reload | 15-02 | COVERED | Renderer dispatch and local transform are pinned.
REQ | RENDER-02 | Persisted star renders from local geometry under translate/rotate | 15-02 | COVERED | FigureShape source contract covers the switch branch and transform.
RESEARCH | - | User explicitly skipped research | 15-02 | COVERED | No RESEARCH.md is required or referenced.
CONTEXT | - | User explicitly continued without Phase 15 CONTEXT.md | 15-02 | COVERED | Locked D-70/D-71 decisions are cited from docs/DECISIONS.md.
</source_audit>

<output>
Create `.planning/phases/BC-15-draw-preview-render-persist-a-star/15-02-SUMMARY.md` when done.
</output>
