---
phase: BC-12-regression-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REG-01]

must_haves:
  truths:
    - "Human acceptance confirms every visible canvas behavior is indistinguishable from v1.1."
    - "A slow drag visibly glides through intermediate positions in a second normal same-profile browser window."
    - "Build, full test suite, Docker, HTTPS certificate, and a single app process are healthy before human acceptance."
    - "A failed run preserves evidence and adds no implementation work."
  artifacts:
    - path: ".planning/phases/BC-12-regression-verification/12-01-SUMMARY.md"
      provides: "REG-01 human acceptance outcome, checklist, and evidence locations."
  key_links:
    - from: "One local dotnet run process"
      to: "Two normal same-profile browser windows"
      via: "shared cookie session and the process-local CanvasSyncNotifier"
      pattern: "https://localhost:7281"
---

<objective>
Run the final acceptance-only REG-01 regression gate: prove on the running application that the
v1.11 storage rewrite changed no user-facing behavior.

Purpose: Phase 10 and Phase 11 automated evidence proves persistence, renderer, and protocol
contracts. This plan supplies the separate human visual and two-window acceptance those tests cannot infer.
Output: 12-01-SUMMARY.md recording approval or the exact failed observation and its preserved evidence.
No product code, schema, migration, test framework, browser automation, or feature work is created.
</objective>

<execution_context>
@C:/Users/EGOR/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/EGOR/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-12-regression-verification/12-RESEARCH.md
@.planning/phases/BC-11-renderer-sync-cutover/11-VERIFICATION.md
@.planning/phases/BC-11-renderer-sync-cutover/11-01-SUMMARY.md
@.planning/phases/BC-11-renderer-sync-cutover/11-02-SUMMARY.md
@.planning/phases/BC-11-renderer-sync-cutover/11-03-SUMMARY.md
@.planning/phases/BC-11-renderer-sync-cutover/11-04-SUMMARY.md
@.planning/phases/BC-11-renderer-sync-cutover/11-05-SUMMARY.md
@.planning/phases/BC-10-storage-schema-migration-persistence-layer/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated preflight and start one local acceptance host</name>
  <files>none; this is verification-only, with process logs placed under the system temporary directory</files>
  <action>
    Run these PowerShell commands from the repository root in order: docker compose up -d --wait;
    dotnet build BlazorCanvas.sln --nologo -v q; dotnet test BlazorCanvas.sln --nologo; and
    dotnet dev-certs https --check --trust. Stop on a non-zero exit, record the output as failed
    preflight in the summary, and do not begin human verification. Do not install packages, use browser
    automation, start a second server, alter PostgreSQL directly, or modify code, migrations, schemas,
    or tests.

    After preflight passes, create a unique directory under the system temporary path. Start exactly one
    hidden process with dotnet run --project src/BlazorCanvas --launch-profile https --no-build, redirect
    standard output and standard error to separate temporary log files, and retain the process id. Poll
    https://localhost:7281/login for up to 30 seconds while confirming that the process has not exited;
    accept status 200 only. If the deadline expires or the process exits, preserve both log files, include
    their paths and any available content in the failed-preflight evidence, and do not begin human
    verification. Keep a ready process and its logs through the checkpoint; record its URL, process id,
    and log paths in the summary without recording a password.
  </action>
  <verify>
    <automated>docker compose up -d --wait; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet build BlazorCanvas.sln --nologo -v q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet test BlazorCanvas.sln --nologo; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet dev-certs https --check --trust; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <automated>$logRoot = Join-Path $env:TEMP ("BlazorCanvas-BC12-" + [guid]::NewGuid()); New-Item -ItemType Directory -Path $logRoot -ErrorAction Stop | Out-Null; $stdout = Join-Path $logRoot "app.stdout.log"; $stderr = Join-Path $logRoot "app.stderr.log"; $app = Start-Process -FilePath dotnet -ArgumentList @("run","--project","src/BlazorCanvas","--launch-profile","https","--no-build") -WorkingDirectory $PWD -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru -WindowStyle Hidden; $deadline = [DateTime]::UtcNow.AddSeconds(30); $ready = $false; while ([DateTime]::UtcNow -lt $deadline) { if ($app.HasExited) { throw "Acceptance host exited with code $($app.ExitCode). Logs: $stdout; $stderr" }; try { $response = Invoke-WebRequest -Uri "https://localhost:7281/login" -UseBasicParsing -ErrorAction Stop; if ($response.StatusCode -eq 200) { $ready = $true; break } } catch { $lastRequestError = $_.Exception.Message }; Start-Sleep -Milliseconds 500 }; if (-not $ready) { throw "Acceptance host did not return HTTPS login status 200 before deadline. Logs: $stdout; $stderr. Last request error: $lastRequestError" }; "PID=$($app.Id); LOG_ROOT=$logRoot"</automated>
  </verify>
  <done>Docker, build, full suite, and HTTPS checks pass; exactly one local HTTPS process serves login and its diagnostic log location is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the full four-shape and two-window regression script</name>
  <files>none; verification only, with the result written to 12-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/BC-12-regression-verification/12-RESEARCH.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/BC-11-renderer-sync-cutover/11-VERIFICATION.md
  </read_first>
  <action>
    Use Task 1's one host. In normal same-profile window A only, open https://localhost:7281/login and
    create a fresh disposable account named bc12-regression-YYYYMMDD-HHMMSS using a non-sensitive
    disposable password. After A redirects to its empty canvas, open normal same-profile window B at
    https://localhost:7281/ (or navigate or refresh B there if it was already open) and confirm it reuses
    A's session and shows the same empty canvas without another login prompt. Do not use private mode or a
    second application process. Use zoom 100 percent and create figures only through the toolbar and canvas.

    Capture the initial two-window state and single-process evidence. Complete every numbered observation
    below and record seen versus expected behavior. Capture screenshots or a short recording for the
    four-shape state, edge slide, selection trace, a mid-drag state in window B, and deletion followed by
    refresh. On success, delete all test figures through the UI, refresh both windows, and record approved
    with evidence paths in the summary. On the first failure, stop; preserve figures, account name,
    screenshots or recording, logs, and exact divergent step. Do not clean up, rerun the same account,
    inspect code, or attempt a fix in this phase. Stop only the retained process after evidence capture.
  </action>
  <what-built>
    Phase 11 cut the app over to final-public v1.11 renderer, persistence, and UUID position sync. Its
    automated suite and two-circuit tests passed; this checkpoint verifies the remaining human-visible
    invariant that the rewrite is invisible.
  </what-built>
  <how-to-verify>
    1. Session baseline: Create the disposable account in window A then open the same URL in B. Expected:
       both show the same empty white 1472 by 828 canvas, six tools, no reconnect or error UI, and no
       second login prompt.

    2. Four shapes: Draw a diagonal line upper-left, non-square rectangle upper-middle, centre-out circle
       upper-right, and non-zero upright triangle below. Expected: each black-on-white shape appears in
       both windows with its direction or geometry preserved. Refresh one window and confirm the figures
       and order remain.

    3. Edge clamp and slide: Draw a circle close to a corner and drag outward; it remains inside the
       canvas. With Pointer, drag the rectangle past the right edge and continue vertically at the edge.
       Expected: it stops and slides vertically as a whole without resize, sticking, boundary crossing,
       or divergence in B.

    4. Drag, persistence, deletion: Drag each non-edge-test figure visibly in open space. Expected:
       each translates as a whole, remains selected after drag, and persists after either refresh. Delete
       one selected figure in A. Expected: it disappears in both, remains absent after B refresh, and
       Delete is disabled when no figure is selected.

    5. Selection lifecycle: Click each visible figure with Pointer. Expected: exactly one has a topmost
       white-underlay and blue dashed trace on its own outline, no red figure outline, and no trace that
       blocks input. Deselect by blank canvas, arming a shape tool, and a non-Delete toolbar control.
       Expected: every route clears the trace; Delete removes a selected figure.

    6. Live glide: Leave one visible figure. Drag it slowly in A for two to three seconds through several
       intermediate positions while watching B. Expected: B visibly glides through intermediate locations
       before pointer release and ends at A's final position, without duplicate or jump-only update.
       Capture before, during, and after evidence including a mid-drag frame.

    7. UI-only cleanup on pass: Delete remaining test figures through A and refresh both windows.
       Expected: both are empty and synchronized. Never use SQL, direct database changes, Docker volume
       reset, or a real account.
  </how-to-verify>
  <acceptance_criteria>
    - All seven checks match expected behavior, including visible intermediate glide in B; type approved only then.
    - Summary records disposable username, preflight, each result, and evidence locations, but never a password.
    - A failure records expected versus seen behavior and preserves evidence with no cleanup or implementation work.
    - The scope remains acceptance-only with no code, schema, feature, package, browser-automation, or database-mutation work.
  </acceptance_criteria>
  <resume-signal>Type approved if every check passes, or describe the first failed step with expected behavior, observed behavior, and evidence location.</resume-signal>
  <verify>
    <automated>dotnet build BlazorCanvas.sln --nologo -v q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; dotnet test BlazorCanvas.sln --nologo; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <human-check>Complete all seven scripted observations using two normal same-profile browser windows against one local HTTPS process and type approved only when all expectations pass.</human-check>
  </verify>
  <done>REG-01 has a human-approved, evidenced result for all four shapes, edge clamping, dragging, deletion, selection, refresh persistence, and two-window glide; otherwise its precise failed run is preserved for a separately authorized gap plan.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Human confirms the rewrite is invisible on the running app. | Task 2 visual and two-window script. | COVERED |
| REQ | REG-01 | Four shapes, edge clamp, dragging, deleting, selection, and two-window glide match v1.1. | Task 2 checks 2 through 6 plus refresh. | COVERED |
| RESEARCH | — | Preflight, one HTTPS process, same-profile windows, disposable UI-only account, evidence, and failure preservation. | Tasks 1 and 2. | COVERED |
| CONTEXT | — | No CONTEXT.md exists; user selected planning without it. | No locked phase decisions exist. | COVERED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser to local app | The human drives pointer, selection, and delete behavior and judges visible results. |
| Window A to notifier to Window B | Two circuits must share one disposable-user session and one app process for valid glide observation. |
| Operator to local database | The run creates disposable figures only through UI; direct mutation could invalidate the proof or harm unrelated data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | Acceptance data | medium | mitigate | Fresh disposable account, UI-only setup and cleanup, no SQL, volume reset, or real-account reuse. |
| T-12-02 | Spoofing | Second window/session | medium | mitigate | Require normal same-profile windows, confirm shared session/canvas, and prohibit private windows. |
| T-12-03 | Repudiation | Acceptance outcome | low | accept | Summary records checklist, account name, preflight output, and evidence paths; password is excluded. |
| T-12-04 | Denial of service | Local app process | low | accept | Build/test/container preflight and retained process id plus temporary logs support diagnosis. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed in this acceptance-only phase. |

No threat is at or above the configured ASVS L1 blocking threshold.
</threat_model>

<verification>
1. Docker, build, full-suite, certificate, and single-host checks pass before manual work.
2. The human approves only after all seven visible-behavior checks pass and evidence is recorded.
3. A failure ends the run with preserved evidence and no corrective implementation in this phase.
</verification>

<success_criteria>
REG-01 is accepted only when a human records an approved two-window run proving four-shape drawing,
edge clamping, dragging, deleting, selection, refresh persistence, and real-time glide are
indistinguishable from v1.1. Otherwise the summary contains the first precise failure and evidence
for a separately authorized follow-up plan.
</success_criteria>

<output>
Create .planning/phases/BC-12-regression-verification/12-01-SUMMARY.md when the checkpoint concludes.
</output>
