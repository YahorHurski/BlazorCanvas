---
phase: BC-11-renderer-sync-cutover
plan: 04
type: execute
gap_closure: true
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/BlazorCanvas/Data/V11/V11Cutover.cs
  - tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs
  - tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs
autonomous: true
requirements: [SYNC-02, SYNC-03]
must_haves:
  truths:
    - "A guarded disposable scratch database proves that legacy-only, additive, and fresh-users-only catalogs reach one completed final-public v1.11 catalog, while an invalid catalog is rejected without promotion."
    - "Every successful cutover, including a completed-public rerun, leaves public.canvases, public.figure_types, and public.figures with their exact final column sets; neither the v11 schema nor legacy x1/y1/x2/y2 coordinate columns remain."
    - "An exception injected at seed, migration, after DROP public.figures, or after the first ALTER TABLE SET SCHEMA proves the single transaction leaves no partial schema, data, catalog, or legacy-table mutation behind."
  artifacts:
    - tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs
    - tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs
  key_links:
    - "V11Cutover's test-only internal stage seam fails inside the same transaction that performs schema apply, seed, migration, promotion, and cleanup."
    - "The scratch catalog inspection executes against public tables after V11Cutover, rather than relying on the shared already-completed DatabaseFixture catalog."
---

<objective>
Close the transactional cutover proof gap with isolated, disposable PostgreSQL databases. Prove every supported starting catalog reaches the final public v1.11 model and every injected failure rolls its own transaction back completely; do not alter runtime behaviour or broaden the migration boundary.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build a guarded disposable cutover-database harness and a transaction-local failure seam</name>
  <read_first>
    - `src/BlazorCanvas/Data/V11/V11Cutover.cs` — preserve its advisory lock, catalog state machine, single transaction, and completed-state rollback/no-op.
    - `src/BlazorCanvas/Data/V11/Transition/V11Schema.cs` and `V11DataMigration.cs` — use their actual public/v11 DDL and legacy replay contract; do not duplicate production schema SQL in the tests.
    - `tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs` — retain the shared completed catalog for ordinary database tests; scratch cutover catalogs must not mutate it.
  </read_first>
  <files>src/BlazorCanvas/Data/V11/V11Cutover.cs, tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs, tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs</files>
  <action>Add an internal, tests-visible V11Cutover overload or stage callback whose only purpose is to invoke a supplied asynchronous failure probe at explicit `after-schema-apply`, `after-type-seed`, `after-migration`, `after-drop-public-figures`, and `after-promote-canvases` stages while the existing transaction is still open; `after-promote-canvases` must run only after the DROP and the first `ALTER TABLE v11.canvases SET SCHEMA public`. The public `EnsureAsync` path must pass no probe and retain exactly its current behaviour. Add `V11CutoverScratchDatabase`, which derives a random database name from `DatabaseFixture.ConnectionString`, connects through the maintenance database to CREATE/DROP it, supplies a disposable NpgsqlDataSource, creates the users-only EF prerequisite, and offers explicit legacy-only, additive, fresh-users-only, completed-public, and invalid catalog setup helpers. Give the harness a deterministic `SnapshotAsync` that captures every non-system `public`/`v11` relation, its columns/constraints/index definitions, and canonical ordered row data, so rollback tests can compare the complete application catalog and data snapshot byte-for-byte. Its disposal must terminate only connections to its generated database before dropping it, never the shared `canvas` database. Add a smoke test that the helper can create and dispose an isolated catalog.</action>
  <verify><automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~V11CutoverTests"</automated></verify>
  <done>Focused cutover tests can create an isolated catalog and inject a deterministic exception at each cutover stage without changing production callers or the shared database fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Prove all valid catalog starts promote to the final public schema</name>
  <read_first>
    - `tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs` — extend the two existing completed/order tests instead of creating a second competing cutover suite.
    - `src/BlazorCanvas/Data/V11/Transition/LegacyFigureConversion.cs` and `V11DeterministicId.cs` — seed a representative legacy rectangle/line through the actual conversion path and assert its deterministic identity survives promotion.
    - `src/BlazorCanvas/Data/V11/FigureRepository.cs` and `CanvasRepository.cs` — final runtime depends exclusively on public tables and their exact columns.
  </read_first>
  <files>tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs, tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs</files>
  <action>Add sequential scratch-database tests for: (1) legacy-only users plus legacy figures, asserting migrated row count, deterministic UUID/canvas ownership, geometry/style/z survival, and promoted final-public columns; (2) an additive catalog after an initial migration, asserting rerun completes without duplicate canvases, types, or figures; (3) a fresh users-only catalog, asserting install creates/seeds/promotes final tables without reading or requiring public.figures; (4) a completed-public catalog, snapshotting its rows and catalog before calling the normal public `EnsureAsync` and proving the post-call snapshot is identical (the completed path is a no-op); and (5) each invalid partial catalog shape, asserting an InvalidOperationException and no destructive promotion. Centralize a `AssertFinalPublicCatalogAsync` helper that queries `information_schema.columns` and `to_regnamespace`/`to_regclass`: require exactly `canvases(id, owner_id, name, width, height, background, created_at)`, `figure_types(name)`, and `figures(id, canvas_id, type, x, y, rotation, geometry, style, z, bbox_x, bbox_y, bbox_w, bbox_h, created_at)` in `public`; assert `figures.id` is `uuid`; and reject `v11`, `x1`, `y1`, `x2`, `y2`, and legacy type-CHECK constraints. Reuse this exact assertion for every successful starting state.</action>
  <verify><automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~V11CutoverTests"</automated></verify>
  <done>Legacy-only, additive, fresh-users-only, and completed-public catalogs are demonstrated to finish in (or preserve) the exact final public model; invalid catalogs fail before destructive state changes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove seed, migration, and promotion failures roll back atomically</name>
  <read_first>
    - `src/BlazorCanvas/Data/V11/V11Cutover.cs` — failure injection must occur after the normal transaction/advisory lock begins and before commit; never catch-and-continue.
    - `tests/BlazorCanvas.Tests/Database/V11/V11CutoverScratchDatabase.cs` — use its catalog snapshots, not assumptions about a test execution order.
    - `.planning/phases/BC-11-renderer-sync-cutover/11-VERIFICATION.md` — the exact missing proof is no partial schema/data/catalog mutation at seed, migration, and promotion failures.
  </read_first>
  <files>src/BlazorCanvas/Data/V11/V11Cutover.cs, tests/BlazorCanvas.Tests/Database/V11/V11CutoverTests.cs</files>
  <action>For independent fresh or legacy scratch catalogs, invoke the internal failure seam to throw after type seeding, after legacy migration, immediately after `DROP TABLE public.figures`, and immediately after `ALTER TABLE v11.canvases SET SCHEMA public` (before the remaining table promotions). For every thrown call, take the harness's complete pre-cutover application catalog/data snapshot and assert byte-for-byte equality after the exception: no promoted public v1.11 table, no partial v11 schema/table/type/figure/canvas rows, no dropped legacy `public.figures`, and no changed legacy row count or coordinates. Then rerun the normal public `EnsureAsync` on the same database, assert `AssertFinalPublicCatalogAsync`, and prove the rollback left a retryable state. Keep failure control test-only and do not add a runtime configuration switch.</action>
  <verify><automated>dotnet build BlazorCanvas.sln --nologo -v q; dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~V11CutoverTests"</automated></verify>
  <done>Each seed, migration, post-DROP, and post-first-promotion exception demonstrates full rollback and a subsequent normal retry succeeds; no partial cutover can be served.</done>
</task>

</tasks>

<verification><automated>dotnet build BlazorCanvas.sln --nologo -v q; dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~V11CutoverTests"</automated></verification>
<success_criteria>
- All supported catalog starts either promote to exactly the final public v1.11 storage or fail safely.
- The final catalog assertions rule out both the temporary v11 schema and the old legacy-coordinate model.
- Failure proof covers seed, migration, and promotion inside one rollback-safe transaction.
</success_criteria>
