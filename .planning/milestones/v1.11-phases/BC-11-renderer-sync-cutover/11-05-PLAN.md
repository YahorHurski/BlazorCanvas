---
phase: BC-11-renderer-sync-cutover
plan: 05
type: execute
gap_closure: true
wave: 5
depends_on: ["11-04"]
files_modified:
  - src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs
  - src/BlazorCanvas/Components/Pages/Home.razor
  - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
  - tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
autonomous: true
requirements: [SYNC-02, SYNC-03]
must_haves:
  truths:
    - "Two independent coordinators backed by final public FigureRepository rows persist and relay canonical UUID draw, delete, and move operations without duplicate draw insertion or unknown-move resurrection."
    - "The real circuit delivery boundary rejects a remote notification at receipt while drag is active, so a queued InvokeAsync callback cannot apply that message after drag commit; a source contract proves Home makes that receipt decision before enqueueing."
    - "A final-public zero-row move removes the stale row for all peers, and a failed move rolls back, exposes the reload modal, reloads authoritative database rows, and converges both peers."
  artifacts:
    - tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs
    - tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs
  key_links:
    - "Home captures the coordinator's receipt-time remote-delivery decision before InvokeAsync queues state work."
    - "The integration tests call FigureRepository against public.figures through two coordinator instances connected by CanvasSyncNotifier."
---

<objective>
Close the final-public, two-circuit sync proof gap. Keep the existing coordinator as the state seam, make the Home InvokeAsync boundary safely preserve D-54 across scheduling, and exercise draw/delete/move, staleness, throttle, and failure/reload outcomes against the promoted PostgreSQL tables.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Make D-54 survive the queued Blazor delivery boundary</name>
  <read_first>
    - `src/BlazorCanvas/Components/Pages/Home.razor` — `HandleRemoteMessage` is the notifier-to-InvokeAsync bridge; preserve disposal handling and StateHasChanged behaviour.
    - `src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs` — current `ApplyRemoteMessage` checks IsDragging too late for a message received during drag but invoked after pointer-up.
    - `tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs` — preserve existing direct D-54 and echo-filter coverage while adding a controllable queued-delivery regression.
  </read_first>
  <files>src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs, src/BlazorCanvas/Components/Pages/Home.razor, tests/BlazorCanvas.Tests/Components/CanvasInteractionCoordinatorTests.cs</files>
  <action>Introduce a narrow internal receipt-time coordinator method such as `TryAuthorizeRemoteDelivery(SyncMessage)` that returns an immutable nullable delivery token: reject echo messages and every incoming kind while a drag is active, otherwise authorize exactly the received message for later application. Pair it with an internal authorized-apply method so the queued callback cannot re-evaluate a now-changed drag state; keep `ApplyRemoteMessage` defensive for direct callers. Change Home so `HandleRemoteMessage` obtains the token synchronously before calling `InvokeAsync`, returns without enqueueing on rejection, and the queued callback applies only that already-authorized token while retaining disposal handling and `StateHasChanged`. Because this test project intentionally has no bUnit dependency, add a deterministic source/contract test in `CanvasInteractionCoordinatorTests` that locates `Home.razor`, isolates `HandleRemoteMessage`, and asserts the authorization call occurs before `InvokeAsync` and the callback invokes only the authorized-apply method. Add a controllable deferred-callback coordinator test that attempts draw/delete/move receipt during an active drag, commits before draining the queue, and proves no callback was authorized or can change figures/selection; retain a non-drag queued-delivery test proving one authorized token is applied exactly once.</action>
  <verify><automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~CanvasInteractionCoordinatorTests"</automated></verify>
  <done>A source contract proves Home authorizes receipt before InvokeAsync, and deferred-callback tests prove D-54 cannot be bypassed by scheduling delay while normal remote delivery still applies once.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise persisted final-public draw, delete, move, and protocol rules through two coordinators</name>
  <read_first>
    - `tests/BlazorCanvas.Tests/Database/DatabaseFixture.cs` — use its final public DataSource and create isolated owner/canvas/figure rows; do not reintroduce v11 schema SQL.
    - `src/BlazorCanvas/Data/V11/FigureRepository.cs` and `CanvasRepository.cs` — wire the coordinator callbacks to the actual public-table methods, including canvas ownership predicates.
    - `src/BlazorCanvas/Sync/SyncMessage.cs`, `CanvasSyncNotifier.cs`, and `CanvasInteractionCoordinator.cs` — assert D-53 message shapes, D-40 update-only receive, and D-47 throttle/trailing-edge ordering through production classes.
  </read_first>
  <files>tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs</files>
  <action>Add a sequential `[Collection("Database")]` integration suite that creates a unique real user and its deterministic final-public canvas, builds two independent CanvasInteractionCoordinator instances with separate FigureRepository/CanvasRepository callback closures and a shared CanvasSyncNotifier, and subscribes both as two circuits. Persist a draw from circuit A, reload/observe circuit B, and assert one canonical FigureRow with the same UUID, local geometry, style, and z; replay the draw message and assert no duplicate. Persist a move and delete and assert both coordinators plus `public.figures` converge. Observe emitted messages to assert D-53 draw/move/delete/rollback shapes, publish unknown move/rollback UUIDs to prove D-40 update-only no insertion, and use a controlled clock plus several moves inside 50ms to assert D-47 emits no extra position event but commits the final trailing x/y before exactly one `MoveAsync` persistence call.</action>
  <verify><automated>dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests"</automated></verify>
  <done>Final public persistence, not an in-memory row list, proves canonical two-circuit draw/delete/move convergence, no duplicate draw, D-40, D-47, and D-53.</done>
</task>

<task type="auto">
  <name>Task 3: Prove final-public stale deletion and failed-save reload convergence</name>
  <read_first>
    - `src/BlazorCanvas/Components/Pages/CanvasInteractionCoordinator.cs` — retain the existing zero-row delete broadcast, rollback publication, modal state, and ReloadAsync canonical snapshot logic.
    - `src/BlazorCanvas/Data/V11/FigureRepository.cs` — a zero-row MoveAsync is the D-10 ownership-scoped UPDATE result; do not convert it into an upsert.
    - `tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs` — extend the same real public-table two-circuit harness so the result cannot be satisfied by fake delegates alone.
  </read_first>
  <files>tests/BlazorCanvas.Tests/Database/V11/FinalPublicCanvasSyncIntegrationTests.cs</files>
  <action>Add one database-backed stale-row test that loads the same persisted figure into both circuits, then deletes it directly through circuit B's final-public `FigureRepository.DeleteAsync` while deliberately withholding the notifier delivery to A (use the repository directly rather than `B.DeleteAsync`, or temporarily unsubscribe A). This leaves A's loaded row genuinely stale. Commit A's retained drag and assert the affected-row-zero path removes A's row and selection, emits a delete notification observed by B, leaves no row in either coordinator or `public.figures`, and never calls insert or creates a replacement row. Add a failed-save test that makes A's move callback throw only after its local optimistic drag while preserving the actual database row, asserts rollback reaches B and A shows the save-failed modal, then calls A.ReloadAsync and B's normal reload/delivery path; assert both coordinators exactly equal the authoritative public repository snapshot, the modal is closed, and no stale optimistic coordinates remain. Run the focused integration suite plus the full solution; do not add Phase 12's human visual check to this automated plan.</action>
  <verify><automated>dotnet build BlazorCanvas.sln --nologo -v q; dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests|FullyQualifiedName~CanvasInteractionCoordinatorTests"; dotnet test BlazorCanvas.sln --nologo</automated></verify>
  <done>Database-backed D-10 and D-52 tests prove stale removal, rollback modal, reload, and peer convergence with final public rows.</done>
</task>

</tasks>

<verification><automated>dotnet build BlazorCanvas.sln --nologo -v q; dotnet test BlazorCanvas.sln --nologo --filter "FullyQualifiedName~V11CutoverTests|FullyQualifiedName~FinalPublicCanvasSyncIntegrationTests|FullyQualifiedName~CanvasInteractionCoordinatorTests"; dotnet test BlazorCanvas.sln --nologo</automated></verification>
<success_criteria>
- Real public-table operations demonstrate draw/delete/move replication across two independently constructed circuit coordinators.
- D-40, D-47, D-53, and receipt-time D-54 have executable proof at the production seam.
- Final-public stale and failed-save paths converge every peer to the database snapshot.
</success_criteria>
