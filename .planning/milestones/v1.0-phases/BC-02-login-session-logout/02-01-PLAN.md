---
phase: 02-login-session-logout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlazorCanvas/Components/App.razor
  - src/BlazorCanvas/wwwroot/app.css
  - src/BlazorCanvas/Components/Layout/MainLayout.razor
  - src/BlazorCanvas/Components/Layout/MainLayout.razor.css
  - src/BlazorCanvas/Components/Layout/NavMenu.razor
  - src/BlazorCanvas/Components/Layout/NavMenu.razor.css
  - src/BlazorCanvas/Components/Pages/Counter.razor
  - src/BlazorCanvas/Components/Pages/Weather.razor
  - src/BlazorCanvas/wwwroot/lib/bootstrap
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup: []

must_haves:
  truths:
    - "The app shell renders with NO Bootstrap sidebar, NO NavMenu, and NO Home/Counter/Weather demo chrome — a blank pass-through layout that lets each page own 100% of its visual chrome (resolved user decision 1)."
    - "The page body has zero margin so a full-viewport-width 48px toolbar (built in 02-03) sits flush at document (0,0), preserving D-43's canvasY = PageY − 48 constant for Phase 3."
    - "The solution still compiles and the framework error-boundary / validation styling still works after the scaffold is stripped."
  artifacts:
    - "src/BlazorCanvas/Components/Layout/MainLayout.razor — gutted to @Body + the #blazor-error-ui block only"
    - "src/BlazorCanvas/Components/App.razor — no Bootstrap stylesheet link"
    - "src/BlazorCanvas/wwwroot/app.css — Blazor framework essentials + margin:0 reset, no Bootstrap-derived rules"
  key_links:
    - "App.razor <link> tags → app.css and BlazorCanvas.styles.css must remain; only the Bootstrap link is removed"
    - "MainLayout.razor no longer references <NavMenu /> → NavMenu.razor and its demo pages are safe to delete"
---

<objective>
Drop the default Blazor Web App template scaffold so that `/login` (02-03) and the authenticated
toolbar shell (02-03) can be built purely from the locked design tokens in `02-UI-SPEC.md`, with each
page owning its own chrome via component-scoped CSS — no shared Bootstrap sidebar, no demo pages.

This implements **resolved user decision 1** (DROP the default scaffold) and closes UI-SPEC Open
Question 4 / RESEARCH Open Question 1. It is a prerequisite for the login card and toolbar looking
correct against the UI-SPEC (AUTH-01 login surface, AUTH-03 toolbar surface).

Purpose: Remove leftover template chrome that both UI-SPECs explicitly reject, and establish the
zero-margin page baseline the 48px toolbar depends on (D-43).
Output: A stripped, pass-through `MainLayout`, a Bootstrap-free `App.razor`/`app.css`, and the deletion
of the NavMenu + Counter + Weather demo scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/BC-02-login-session-logout/02-UI-SPEC.md
@.planning/phases/BC-02-login-session-logout/02-PATTERNS.md
@src/BlazorCanvas/Components/App.razor
@src/BlazorCanvas/Components/Layout/MainLayout.razor
@src/BlazorCanvas/Components/Layout/MainLayout.razor.css
@src/BlazorCanvas/wwwroot/app.css
</context>

<artifacts_this_phase_produces>
Full BC-02 artifact set (produced across plans 02-01, 02-02, 02-03):
- `src/BlazorCanvas/Auth/UsernameNormalizer.cs` — `public static string Normalize(string?)` (D-44), plan 02-02
- `src/BlazorCanvas/Program.cs` — `AddAuthentication().AddCookie()`, `AddAuthorization()`, `AddCascadingAuthenticationState()`, `UseAuthentication()`/`UseAuthorization()`, `MapPost("/logout", ...)`, plan 02-02
- `src/BlazorCanvas/Components/Pages/Login.razor` (+ `Login.razor.css`) — static-SSR login page + handler, plan 02-03
- `src/BlazorCanvas/Components/Pages/Home.razor` (modified) (+ `Home.razor.css`) — InteractiveServer `[Authorize]` canvas shell + 48px toolbar + logout form, plan 02-03
- `src/BlazorCanvas/Components/_Imports.razor` (modified) — auth `@using` directives, plan 02-03
- `tests/BlazorCanvas.Tests/Auth/UsernameNormalizerTests.cs` — normalizer unit tests, plan 02-02
This plan (02-01) produces the gutted `MainLayout`, Bootstrap-free `App.razor`/`app.css`, and removes the demo scaffold that the above build on.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove Bootstrap from the app shell</name>
  <files>src/BlazorCanvas/Components/App.razor, src/BlazorCanvas/wwwroot/app.css, src/BlazorCanvas/wwwroot/lib/bootstrap</files>
  <read_first>
    - src/BlazorCanvas/Components/App.razor (the file being modified — note the three `<link rel="stylesheet">` tags)
    - src/BlazorCanvas/wwwroot/app.css (the file being modified — Bootstrap-coupled rules vs framework essentials)
    - .planning/phases/BC-02-login-session-logout/02-UI-SPEC.md (Design System + Scaffold note: hand-authored Razor + component-scoped CSS, no npm/Bootstrap)
  </read_first>
  <action>
    Delete the entire `src/BlazorCanvas/wwwroot/lib/bootstrap` directory (all bundled Bootstrap CSS).
    In `App.razor`, remove ONLY the `<link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />`
    line; keep the `app.css` link, the `BlazorCanvas.styles.css` link (Blazor's bundled component-scoped
    CSS), `<ResourcePreloader />`, `<ImportMap />`, `<HeadOutlet />`, `<Routes />`, `<ReconnectModal />`,
    and the `blazor.web.js` script exactly as-is.
    In `app.css`, reduce the file to the Blazor framework essentials plus a page reset: keep
    `.blazor-error-boundary` (+ its `::after`), `.validation-message`, `.valid.modified`, `.invalid`,
    and `h1:focus { outline: none; }`; ADD a `html, body { margin: 0; }` reset (page margin 0 per D-43,
    so the 48px toolbar spans the full viewport with no gutter). Remove every Bootstrap-derived rule:
    the `a, .btn-link` color rule, `.btn-primary`, `.btn:focus`/`.btn:active:focus`, `.content`,
    `.darker-border-checkbox`, and both `.form-floating > .form-control...` rules (they reference the
    Bootstrap `--bs-secondary-color` custom property). Do not introduce any new color tokens here — all
    login/toolbar colors come from the component-scoped CSS built in 02-03 per `02-UI-SPEC.md`.
  </action>
  <acceptance_criteria>
    - `dotnet build BlazorCanvas.sln` succeeds with no errors.
    - `src/BlazorCanvas/wwwroot/lib/bootstrap` no longer exists on disk.
    - `grep -c 'bootstrap' src/BlazorCanvas/Components/App.razor` returns 0.
    - `grep -c 'bs-' src/BlazorCanvas/wwwroot/app.css` returns 0 (no Bootstrap custom-property references remain).
    - `app.css` contains a `margin: 0` reset on `html, body` and still contains the `.blazor-error-boundary` rule.
    - App.razor still contains a stylesheet link for `app.css` and for `BlazorCanvas.styles.css`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build BlazorCanvas.sln -c Debug --nologo -v q; test ! -d src/BlazorCanvas/wwwroot/lib/bootstrap; grep -c 'bootstrap' src/BlazorCanvas/Components/App.razor | grep -qx 0; grep -c 'bs-' src/BlazorCanvas/wwwroot/app.css | grep -qx 0</automated>
  </verify>
  <done>Bootstrap is fully removed from the shell (bundle deleted, link removed, app.css cleaned) with a margin:0 page reset in place, and the solution builds.</done>
</task>

<task type="auto">
  <name>Task 2: Gut MainLayout to a pass-through and delete the demo pages/nav</name>
  <files>src/BlazorCanvas/Components/Layout/MainLayout.razor, src/BlazorCanvas/Components/Layout/MainLayout.razor.css, src/BlazorCanvas/Components/Layout/NavMenu.razor, src/BlazorCanvas/Components/Layout/NavMenu.razor.css, src/BlazorCanvas/Components/Pages/Counter.razor, src/BlazorCanvas/Components/Pages/Weather.razor</files>
  <read_first>
    - src/BlazorCanvas/Components/Layout/MainLayout.razor (the file being modified — currently `.page` > `.sidebar` > `<NavMenu />` + `main` shell)
    - src/BlazorCanvas/Components/Layout/MainLayout.razor.css (the file being modified — sidebar/top-row/media-query rules vs the `#blazor-error-ui` block)
    - src/BlazorCanvas/Components/Routes.razor (confirms `DefaultLayout="typeof(Layout.MainLayout)"` and `NotFoundPage="typeof(Pages.NotFound)"` — both must keep resolving)
    - .planning/phases/BC-02-login-session-logout/02-PATTERNS.md (MainLayout gut guidance: strip to `@Body`, keep `#blazor-error-ui`, delete NavMenu/demo pages)
  </read_first>
  <action>
    Rewrite `MainLayout.razor` to `@inherits LayoutComponentBase` followed by `@Body` and the existing
    `#blazor-error-ui` `<div>` block (kept verbatim — it is framework infrastructure, unrelated to
    Bootstrap). Remove the `.page`/`.sidebar`/`<NavMenu />`/`main`/`.top-row`/`.content` wrapper entirely.
    Rewrite `MainLayout.razor.css` to contain ONLY the `#blazor-error-ui` and `#blazor-error-ui .dismiss`
    rules (delete `.page`, `main`, `.sidebar`, `.top-row` and all `@media` breakpoint blocks — they
    styled the deleted sidebar/top-row).
    Delete these four demo-scaffold files: `Components/Layout/NavMenu.razor`,
    `Components/Layout/NavMenu.razor.css`, `Components/Pages/Counter.razor`,
    `Components/Pages/Weather.razor`. Do NOT delete `Pages/NotFound.razor` or `Pages/Error.razor` — they
    are framework infrastructure wired to `UseStatusCodePagesWithReExecute("/not-found")` and
    `UseExceptionHandler("/Error")` in `Program.cs` and to `Routes.razor`'s `NotFoundPage`. Leave
    `Routes.razor` unchanged (its plain `RouteView` + `MainLayout` references still resolve).
  </action>
  <acceptance_criteria>
    - `dotnet build BlazorCanvas.sln` succeeds with no errors (proves nothing else referenced NavMenu/Counter/Weather).
    - `MainLayout.razor` contains `@Body` and does NOT contain `NavMenu` or `sidebar`.
    - `grep -c 'NavMenu\|sidebar' src/BlazorCanvas/Components/Layout/MainLayout.razor` returns 0.
    - The four files NavMenu.razor, NavMenu.razor.css, Counter.razor, Weather.razor no longer exist.
    - `Pages/NotFound.razor` and `Pages/Error.razor` still exist.
    - `MainLayout.razor.css` still contains the `#blazor-error-ui` selector.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build BlazorCanvas.sln -c Debug --nologo -v q; grep -c 'NavMenu\|sidebar' src/BlazorCanvas/Components/Layout/MainLayout.razor | grep -qx 0; test ! -f src/BlazorCanvas/Components/Layout/NavMenu.razor; test ! -f src/BlazorCanvas/Components/Pages/Counter.razor; test ! -f src/BlazorCanvas/Components/Pages/Weather.razor; test -f src/BlazorCanvas/Components/Pages/NotFound.razor</automated>
  </verify>
  <done>MainLayout is a blank pass-through (`@Body` + error-ui), its CSS holds only the error-ui rules, the NavMenu + Counter + Weather demo scaffold is deleted, NotFound/Error are retained, and the solution builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → browser | Static assets (CSS bundles, demo pages/routes) served to any visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-101 | Information Disclosure | `/counter`, `/weather` demo routes + NavMenu | low | mitigate | Delete demo pages/components so no unused, unauthenticated routes remain reachable in the shipped app |
| T-02-102 | Tampering | bundled Bootstrap CSS in `wwwroot/lib/bootstrap` | low | mitigate | Remove the unused third-party CSS bundle from `wwwroot`, shrinking shipped static-asset surface |
| T-02-1SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs occur in this phase (RESEARCH: cookie auth / antiforgery / Components.Authorization are all in-box with `net10.0`) — supply-chain gate N/A |
</threat_model>

<verification>
- `dotnet build BlazorCanvas.sln` succeeds.
- No Bootstrap reference remains in `App.razor` or `app.css`; the `wwwroot/lib/bootstrap` bundle is deleted.
- `MainLayout` is a pass-through (`@Body` + `#blazor-error-ui`); no sidebar/NavMenu/top-row markup or CSS remains.
- Demo pages Counter/Weather and NavMenu are deleted; NotFound/Error retained.
- `html, body { margin: 0 }` reset is present (D-43 baseline for the 48px toolbar).
</verification>

<success_criteria>
The template scaffold is gone: the app shell is a blank pass-through layout ready for each page to
supply its own chrome, Bootstrap is fully removed, the page has zero margin, and the solution compiles.
This unblocks 02-03's login card and toolbar renders against `02-UI-SPEC.md`.
</success_criteria>

<output>
Create `.planning/phases/BC-02-login-session-logout/02-01-SUMMARY.md` when done.
</output>
